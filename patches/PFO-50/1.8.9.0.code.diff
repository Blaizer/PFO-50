--- original/gml_GlobalScript_scr01_Meta.gml
+++ patched/gml_GlobalScript_scr01_Meta.gml
@@ -20,3 +20,4 @@
     global.mGameTitleMusic[arg0] = -1;
     global.mGameTitleMusicLoop[arg0] = false;
+    global.mGameOnlineFavoredPlayerFunction[arg0] = scrFunction(o01_Game, scr01_GetFavoredPlayer, 0);
 }
--- original/gml_GlobalScript_scr07_BossXCutscene.gml
+++ patched/gml_GlobalScript_scr07_BossXCutscene.gml
@@ -46,6 +46,8 @@
 function scr07_BossXRoom()
 {
-    static _room = 
+    if (is_undefined(global.s07_bossXCutsceneRoom))
     {
+        global.s07_bossXCutsceneRoom = 
+        {
         room_x1: 3072,
         room_x2: 3456,
@@ -356,5 +358,5 @@
         }
     };
-    
-    return _room;
+    }
+    return global.s07_bossXCutsceneRoom;
 }
--- original/gml_GlobalScript_scr07_DrawMapUI.gml
+++ patched/gml_GlobalScript_scr07_DrawMapUI.gml
@@ -54,6 +54,6 @@
         _height = o07__Game.screenHeight;
     }
-    var _scol = (floor(current_time div 120) % 3) == 0;
-    var _dfls = (floor(current_time div 120) % 4) < 2;
+    var _scol = (floor(global.pfo_current_time div 120) % 3) == 0;
+    var _dfls = (floor(global.pfo_current_time div 120) % 4) < 2;
     if (room == rm07_GravGuns4)
     {
--- original/gml_GlobalScript_scr07_DrawModUI.gml
+++ patched/gml_GlobalScript_scr07_DrawModUI.gml
@@ -57,5 +57,5 @@
                 draw_set_color(global.palette[9]);
                 draw_rectangle(arg0 + 97, arg1 + 29 + (64 * i), arg0 + 96 + 127, arg1 + 28 + (64 * i) + 47, 0);
-                if ((floor(current_time * 0.01) % 2) == 0)
+                if ((floor(global.pfo_current_time * 0.01) % 2) == 0)
                 {
                     scrSetFont(global.fontTerminal);
--- original/gml_GlobalScript_scr08_Meta.gml
+++ patched/gml_GlobalScript_scr08_Meta.gml
@@ -20,3 +20,5 @@
     global.mGameTitleMusic[arg0] = -1;
     global.mGameTitleMusicLoop[arg0] = false;
+    global.mGamePlayersSupported[arg0] = 2;
+    global.mGameOnlineFavoredPlayerFunction[arg0] = scrFunction(o08_mg_Mas, scr08_GetFavoredPlayer, 0);
 }
--- original/gml_GlobalScript_scr09_Meta.gml
+++ patched/gml_GlobalScript_scr09_Meta.gml
@@ -20,3 +20,5 @@
     global.mGameTitleMusic[arg0] = -1;
     global.mGameTitleMusicLoop[arg0] = false;
+    global.mGameOnlineHUDHAlign[arg0] = 1;
+    global.mGameOnlineHUDVAlign[arg0] = 0;
 }
--- original/gml_GlobalScript_scr12_Meta.gml
+++ patched/gml_GlobalScript_scr12_Meta.gml
@@ -18,3 +18,5 @@
     global.mGameTitleMusic[arg0] = bgm00_lonestar_stingTitle;
     global.mGameTitleMusicLoop[arg0] = false;
+    global.mGameOnlineHUDHAlign[arg0] = 2;
+    global.mGameOnlineHUDVAlign[arg0] = 0;
 }
--- original/gml_GlobalScript_scr13_Meta.gml
+++ patched/gml_GlobalScript_scr13_Meta.gml
@@ -20,3 +20,4 @@
     global.mGameTitleMusic[arg0] = bgm00_lonestar_stingTitle;
     global.mGameTitleMusicLoop[arg0] = false;
+    global.mGameOnlineFavoredPlayerFunction[arg0] = scrFunction(o13_Game, scr13_GetFavoredPlayer);
 }
--- original/gml_GlobalScript_scr14_Cleanup.gml
+++ patched/gml_GlobalScript_scr14_Cleanup.gml
@@ -1,4 +1,5 @@
 function scr14_Cleanup()
 {
+    global.onlineSimultaneousTurns = false;
     with (o14_Game)
     {
--- original/gml_GlobalScript_scr14_GetSponsee.gml
+++ patched/gml_GlobalScript_scr14_GetSponsee.gml
@@ -1,56 +1,27 @@
-function scr14_GetSponsee()
+function scr14_GetSponsee(arg0, arg1, arg2)
 {
-    sponsee[0] = -4;
-    sponsee[1] = -4;
-    sponsee[2] = -4;
-    scrShuffle(quibbles);
-    var listPos = 0;
-    var sponseeNum = 0;
-    do
+    sponsee[arg0][0] = -4;
+    sponsee[arg0][1] = -4;
+    sponsee[arg0][2] = -4;
+    var sponseeNum;
+    for (sponseeNum = 0; sponseeNum < arg2 && ds_list_size(arg1) > 0; sponseeNum++)
     {
-        var inCurrentRace = false;
-        if (ds_list_find_value(quibbles, listPos) == currQ[0] || ds_list_find_value(quibbles, listPos) == currQ[1] || ds_list_find_value(quibbles, listPos) == currQ[2])
-        {
-            inCurrentRace = true;
+        sponsee[arg0][sponseeNum] = ds_list_find_value(arg1, 0);
+        ds_list_delete(arg1, 0);
         }
-        var alreadySponsored = false;
-        if (ds_list_find_value(quibbles, listPos).sponsor != -1)
-        {
-            alreadySponsored = true;
-        }
-        var inFutureRace = false;
-        for (var j = deckIndex; j < (roundsToWin * 3); j++)
-        {
-            if (ds_list_find_value(quibbleDeck, j) == ds_list_find_value(quibbles, listPos))
-            {
-                inFutureRace = true;
-            }
-        }
-        if (!inCurrentRace && !alreadySponsored && inFutureRace)
-        {
-            sponsee[sponseeNum] = ds_list_find_value(quibbles, listPos);
-            listPos++;
-            sponseeNum++;
-        }
-        else
-        {
-            listPos++;
-        }
-    }
-    until (sponseeNum == 3 || listPos == NUM_QUIBBLES);
-    sponseeCost[0] = 0;
-    sponseeCost[1] = 0;
-    sponseeCost[2] = 0;
+    sponseeCost[arg0][0] = 0;
+    sponseeCost[arg0][1] = 0;
+    sponseeCost[arg0][2] = 0;
     for (var j = 0; j < 3; j++)
     {
-        if (sponsee[j] != -4)
+        if (sponsee[arg0][j] != -4)
         {
             var wp = 0;
-            if (sponsee[j].races > 0)
+            if (sponsee[arg0][j].races > 0)
             {
-                wp = sponsee[j].wins / sponsee[j].races;
+                wp = sponsee[arg0][j].wins / sponsee[arg0][j].races;
                 wp *= 100;
             }
-            sponseeCost[j] = round(sponsee[j].qSpeed + wp);
+            sponseeCost[arg0][j] = round(sponsee[arg0][j].qSpeed + wp);
         }
     }
--- original/gml_GlobalScript_scr14_GetStable.gml
+++ patched/gml_GlobalScript_scr14_GetStable.gml
@@ -1,28 +1,16 @@
-function scr14_GetStable()
+function scr14_GetStable(arg0)
 {
-    stable[0] = -4;
-    stable[1] = -4;
-    stableRetired[0] = true;
-    stableRetired[1] = true;
-    var j = 0;
-    for (var i = 0; i < NUM_QUIBBLES; i++)
+    stableRetired[arg0][0] = true;
+    stableRetired[arg0][1] = true;
+    for (var j = 0; j < 2; j++)
     {
-        if (ds_list_find_value(quibbles, i).sponsor == currPlayer)
-        {
-            stable[j] = ds_list_find_value(quibbles, i);
             var k = deckIndex - 3;
             while (k < (roundsToWin * 3))
             {
-                if (ds_list_find_value(quibbleDeck, k) == ds_list_find_value(quibbles, i))
+            if (ds_list_find_value(quibbleDeck, k) == stable[arg0][j])
                 {
-                    stableRetired[j] = false;
+                stableRetired[arg0][j] = false;
                 }
                 k++;
-            }
-            j++;
-            if (j > 1)
-            {
-                break;
-            }
         }
     }
--- original/gml_GlobalScript_scr14_MessageSet.gml
+++ patched/gml_GlobalScript_scr14_MessageSet.gml
@@ -1,4 +1,8 @@
 function scr14_MessageSet(arg0)
 {
+    if (numPlayers == 2 && global.onlineSimultaneousTurns && currPlayer != pfo_client_get_player_index())
+    {
+        exit;
+    }
     scrMessageSet(arg0);
     scrMessageInsertBreaks(336);
--- original/gml_GlobalScript_scr14_Meta.gml
+++ patched/gml_GlobalScript_scr14_Meta.gml
@@ -20,3 +20,6 @@
     global.mGameTitleMusic[arg0] = bgm14_title;
     global.mGameTitleMusicLoop[arg0] = true;
+    global.mGameOnlineHUDHAlign[arg0] = 2;
+    global.mGameOnlineHUDVAlign[arg0] = 0;
+    global.mGameOnlineFavoredPlayerFunction[arg0] = scrFunction(o14_Game, scr14_GetFavoredPlayer);
 }
--- original/gml_GlobalScript_scr18_Meta.gml
+++ patched/gml_GlobalScript_scr18_Meta.gml
@@ -20,3 +20,5 @@
     global.mGameTitleMusic[arg0] = -1;
     global.mGameTitleMusicLoop[arg0] = false;
+    global.mGameOnlineHUDHAlign[arg0] = 0;
+    global.mGameOnlineHUDVAlign[arg0] = 2;
 }
--- original/gml_GlobalScript_scr19_Meta.gml
+++ patched/gml_GlobalScript_scr19_Meta.gml
@@ -20,3 +20,5 @@
     global.mGameTitleMusic[arg0] = -1;
     global.mGameTitleMusicLoop[arg0] = false;
+    global.mGameOnlineHUDHAlign[arg0] = 0;
+    global.mGameOnlineHUDVAlign[arg0] = 2;
 }
--- original/gml_GlobalScript_scr20_Meta.gml
+++ patched/gml_GlobalScript_scr20_Meta.gml
@@ -19,3 +19,4 @@
     global.mGameTitleMusic[arg0] = -1;
     global.mGameTitleMusicLoop[arg0] = false;
+    global.mGameOnlineFavoredPlayerFunction[arg0] = scrFunction(o20_Game, scr20_GetFavoredPlayer);
 }
--- original/gml_GlobalScript_scr23_AttractMode.gml
+++ patched/gml_GlobalScript_scr23_AttractMode.gml
@@ -1,6 +1,4 @@
 function scr23_AttractMode(arg0 = -1)
 {
-    static _variant = 0;
-    
     if (inRecordOrPlayback())
     {
@@ -8,7 +6,7 @@
         if (arg0 != -1)
         {
-            _variant = arg0 % _variantCount;
+            global.s23_attractModeVariant = arg0 % _variantCount;
         }
-        trace("ATTRACT MODE VARIANT (INIT):", _variant, _variantCount);
+        trace("ATTRACT MODE VARIANT (INIT):", global.s23_attractModeVariant, _variantCount);
         array_delete(stageSequence, 0, 3);
         array_delete(stageSequence, array_length(stageSequence) - 4, 4);
@@ -19,13 +17,13 @@
         {
             trace("Attract mode is clear");
-            if (_variant == 0)
+            if (global.s23_attractModeVariant == 0)
             {
                 global.g23_currStage = 0;
             }
-            if (_variant == 1)
+            if (global.s23_attractModeVariant == 1)
             {
                 global.g23_currStage = 2;
             }
-            if (_variant == 2)
+            if (global.s23_attractModeVariant == 2)
             {
                 global.g23_currStage = 4;
@@ -54,5 +52,5 @@
         {
             global.playbackMode = true;
-            if (_variant == 0)
+            if (global.s23_attractModeVariant == 0)
             {
                 global.inputPlayback[0][0] = 85;
@@ -589,5 +587,5 @@
                 global.inputPlayback[132][3] = 1;
             }
-            else if (_variant == 1)
+            else if (global.s23_attractModeVariant == 1)
             {
                 global.inputPlayback[0][0] = 51;
@@ -1124,5 +1122,5 @@
                 global.inputPlayback[132][3] = 1;
             }
-            else if (_variant == 2)
+            else if (global.s23_attractModeVariant == 2)
             {
                 global.inputPlayback[0][0] = 66;
@@ -1707,6 +1705,6 @@
                 global.inputPlayback[144][3] = 1;
             }
-            _variant = (_variant + 1) % _variantCount;
-            trace("ATTRACT MODE VARIANT (INCREMENTED):", _variant, _variantCount);
+            global.s23_attractModeVariant = (global.s23_attractModeVariant + 1) % _variantCount;
+            trace("ATTRACT MODE VARIANT (INCREMENTED):", global.s23_attractModeVariant, _variantCount);
         }
     }
--- original/gml_GlobalScript_scr23_Meta.gml
+++ patched/gml_GlobalScript_scr23_Meta.gml
@@ -20,3 +20,4 @@
     global.mGameTitleMusic[arg0] = -1;
     global.mGameTitleMusicLoop[arg0] = false;
+    global.mGameOnlineFavoredPlayerFunction[arg0] = scrFunction(o23_Mas, scr23_GetFavoredPlayer);
 }
--- original/gml_GlobalScript_scr25_Meta.gml
+++ patched/gml_GlobalScript_scr25_Meta.gml
@@ -20,3 +20,5 @@
     global.mGameTitleMusic[arg0] = -1;
     global.mGameTitleMusicLoop[arg0] = false;
+    global.mGameOnlineHUDHAlign[arg0] = 2;
+    global.mGameOnlineHUDVAlign[arg0] = 0;
 }
--- original/gml_GlobalScript_scr28_Checkpoints.gml
+++ patched/gml_GlobalScript_scr28_Checkpoints.gml
@@ -19,7 +19,7 @@
                     {
                         totalP1Laps++;
-                        var lapTime = current_time - lapP1TimeStamp;
+                        var lapTime = global.pfo_current_time - lapP1TimeStamp;
                         totalP1Time += lapTime;
-                        lapP1TimeStamp = current_time;
+                        lapP1TimeStamp = global.pfo_current_time;
                     }
                 }
@@ -29,7 +29,7 @@
                     {
                         totalP2Laps++;
-                        var lapTime = current_time - lapP2TimeStamp;
+                        var lapTime = global.pfo_current_time - lapP2TimeStamp;
                         totalP2Time += lapTime;
-                        lapP2TimeStamp = current_time;
+                        lapP2TimeStamp = global.pfo_current_time;
                     }
                 }
@@ -118,7 +118,7 @@
                     {
                         totalP1Races++;
-                        var raceTime = current_time - raceP1TimeStamp;
+                        var raceTime = global.pfo_current_time - raceP1TimeStamp;
                         totalP1RaceTime += raceTime;
-                        raceP1TimeStamp = current_time;
+                        raceP1TimeStamp = global.pfo_current_time;
                     }
                 }
@@ -128,7 +128,7 @@
                     {
                         totalP2Races++;
-                        var raceTime = current_time - raceP2TimeStamp;
+                        var raceTime = global.pfo_current_time - raceP2TimeStamp;
                         totalP2RaceTime += raceTime;
-                        raceP2TimeStamp = current_time;
+                        raceP2TimeStamp = global.pfo_current_time;
                     }
                 }
--- original/gml_GlobalScript_scr29_Meta.gml
+++ patched/gml_GlobalScript_scr29_Meta.gml
@@ -20,3 +20,4 @@
     global.mGameTitleMusic[arg0] = -1;
     global.mGameTitleMusicLoop[arg0] = false;
+    global.mGameOnlineFavoredPlayerFunction[arg0] = scrFunction(o29_Game, scr29_GetFavoredPlayer, 0);
 }
--- original/gml_GlobalScript_scr30_Meta.gml
+++ patched/gml_GlobalScript_scr30_Meta.gml
@@ -20,3 +20,6 @@
     global.mGameTitleMusic[arg0] = -1;
     global.mGameTitleMusicLoop[arg0] = false;
+    global.mGameOnlineHUDHAlign[arg0] = 1;
+    global.mGameOnlineHUDVAlign[arg0] = 0;
+    global.mGameOnlineFavoredPlayerFunction[arg0] = scrFunction(o30_Game, scr30_GetFavoredPlayer);
 }
--- original/gml_GlobalScript_scr31_Meta.gml
+++ patched/gml_GlobalScript_scr31_Meta.gml
@@ -20,3 +20,5 @@
     global.mGameTitleMusic[arg0] = -1;
     global.mGameTitleMusicLoop[arg0] = false;
+    global.mGameOnlineHUDHAlign[arg0] = 2;
+    global.mGameOnlineHUDVAlign[arg0] = 0;
 }
--- original/gml_GlobalScript_scr32_Meta.gml
+++ patched/gml_GlobalScript_scr32_Meta.gml
@@ -20,3 +20,5 @@
     global.mGameTitleMusic[arg0] = -1;
     global.mGameTitleMusicLoop[arg0] = false;
+    global.mGameOnlineHUDHAlign[arg0] = 0;
+    global.mGameOnlineHUDVAlign[arg0] = 2;
 }
--- original/gml_GlobalScript_scr33_Meta.gml
+++ patched/gml_GlobalScript_scr33_Meta.gml
@@ -20,3 +20,4 @@
     global.mGameTitleMusic[arg0] = -1;
     global.mGameTitleMusicLoop[arg0] = false;
+    global.mGameOnlineFavoredPlayerFunction[arg0] = scrFunction(o33_Game, scr33_GetFavoredPlayer);
 }
--- original/gml_GlobalScript_scr35_DrawInventoryUI.gml
+++ patched/gml_GlobalScript_scr35_DrawInventoryUI.gml
@@ -114,5 +114,5 @@
         {
             _dropname = scrString("inventory_give");
-            if ((floor(current_time / 500) % 2) == 0)
+            if ((floor(global.pfo_current_time / 500) % 2) == 0)
             {
                 draw_set_color(global.palette[7]);
@@ -122,5 +122,5 @@
         {
             _dropname = scrString("inventory_sell");
-            if ((floor(current_time / 500) % 2) == 0)
+            if ((floor(global.pfo_current_time / 500) % 2) == 0)
             {
                 draw_set_color(global.palette[3]);
--- original/gml_GlobalScript_scr35_DrawStatusUI.gml
+++ patched/gml_GlobalScript_scr35_DrawStatusUI.gml
@@ -5,5 +5,5 @@
     {
         var _traits = scr35_EquipmentTraits(ds_map_find_value(myInventory, "ACCESSORY"));
-        draw_set_color(global.palette[2 + ((current_time div 160) % 2)]);
+        draw_set_color(global.palette[2 + ((global.pfo_current_time div 160) % 2)]);
         var _l = string_line_breaks(_traits[0], 7, 2);
         if (_l[1] != "")
--- original/gml_GlobalScript_scr35_EnemyAttackSwoop.gml
+++ patched/gml_GlobalScript_scr35_EnemyAttackSwoop.gml
@@ -1,7 +1,9 @@
 function scr35_EnemyAttackSwoop(arg0, arg1, arg2)
 {
-    static attToX = verify(target) ? (target.x + scrIRandomRange(-6, 6)) : x;
-    static attToY = (camera_get_view_y(view_get_camera(0)) + 216) - 64;
-    
+    if (is_undefined(global.s35_enemyAttackSwoopAttToX))
+    {
+        global.s35_enemyAttackSwoopAttToX = verify(target) ? (target.x + scrIRandomRange(-6, 6)) : x;
+        global.s35_enemyAttackSwoopAttToY = (camera_get_view_y(view_get_camera(0)) + 216) - 64;
+    }
     if (animState == 0)
     {
@@ -13,12 +15,12 @@
         {
             animState += 1;
-            attToY = (camera_get_view_y(view_get_camera(0)) + 216) - 64;
+            global.s35_enemyAttackSwoopAttToY = (camera_get_view_y(view_get_camera(0)) + 216) - 64;
             if (verify(target))
             {
-                attToX = target.x + scrIRandomRange(-6, 6);
+                global.s35_enemyAttackSwoopAttToX = target.x + scrIRandomRange(-6, 6);
             }
             else
             {
-                attToX = x;
+                global.s35_enemyAttackSwoopAttToX = x;
             }
             img += 1;
@@ -28,6 +30,6 @@
     else if (animState == 1)
     {
-        x = lerp(x, attToX, 1 - (animCount / arg0));
-        y = lerp(y, attToY, 1 - (animCount / arg0));
+        x = lerp(x, global.s35_enemyAttackSwoopAttToX, 1 - (animCount / arg0));
+        y = lerp(y, global.s35_enemyAttackSwoopAttToY, 1 - (animCount / arg0));
         z = (camera_get_view_y(view_get_camera(0)) + 216) - 64 - (16 * !inForeground) - y;
         if (animCount > 0)
--- original/gml_GlobalScript_scr36_Meta.gml
+++ patched/gml_GlobalScript_scr36_Meta.gml
@@ -20,3 +20,4 @@
     global.mGameTitleMusic[arg0] = -1;
     global.mGameTitleMusicLoop[arg0] = false;
+    global.mGameOnlineFavoredPlayerFunction[arg0] = scrFunction(o36_Game, scr36_GetFavoredPlayer);
 }
--- original/gml_GlobalScript_scr39_Meta.gml
+++ patched/gml_GlobalScript_scr39_Meta.gml
@@ -20,3 +20,5 @@
     global.mGameTitleMusic[arg0] = bgm39_title;
     global.mGameTitleMusicLoop[arg0] = true;
+    global.mGameOnlineHUDHAlign[arg0] = 2;
+    global.mGameOnlineHUDVAlign[arg0] = 0;
 }
--- original/gml_GlobalScript_scr42_Meta.gml
+++ patched/gml_GlobalScript_scr42_Meta.gml
@@ -20,3 +20,5 @@
     global.mGameTitleMusic[arg0] = bgm42_stingTitle;
     global.mGameTitleMusicLoop[arg0] = false;
+    global.mGameOnlineHUDHAlign[arg0] = 2;
+    global.mGameOnlineHUDVAlign[arg0] = 0;
 }
--- original/gml_GlobalScript_scr44_Meta.gml
+++ patched/gml_GlobalScript_scr44_Meta.gml
@@ -20,3 +20,5 @@
     global.mGameTitleMusic[arg0] = -1;
     global.mGameTitleMusicLoop[arg0] = false;
+    global.mGameOnlineHUDHAlign[arg0] = 0;
+    global.mGameOnlineHUDVAlign[arg0] = 2;
 }
--- original/gml_GlobalScript_scr45_Meta.gml
+++ patched/gml_GlobalScript_scr45_Meta.gml
@@ -21,3 +21,5 @@
     global.mGameTitleMusic[arg0] = -1;
     global.mGameTitleMusicLoop[arg0] = false;
+    global.mGameOnlineHUDHAlign[arg0] = 1;
+    global.mGameOnlineHUDVAlign[arg0] = 0;
 }
--- original/gml_GlobalScript_scr47_Meta.gml
+++ patched/gml_GlobalScript_scr47_Meta.gml
@@ -20,3 +20,5 @@
     global.mGameTitleMusic[arg0] = -1;
     global.mGameTitleMusicLoop[arg0] = false;
+    global.mGameOnlineHUDHAlign[arg0] = 1;
+    global.mGameOnlineHUDVAlign[arg0] = 2;
 }
--- original/gml_GlobalScript_scr48_Meta.gml
+++ patched/gml_GlobalScript_scr48_Meta.gml
@@ -20,3 +20,5 @@
     global.mGameTitleMusic[arg0] = -1;
     global.mGameTitleMusicLoop[arg0] = false;
+    global.mGameOnlineHUDHAlign[arg0] = 2;
+    global.mGameOnlineHUDVAlign[arg0] = 0;
 }
--- original/gml_GlobalScript_scr49_Meta.gml
+++ patched/gml_GlobalScript_scr49_Meta.gml
@@ -20,3 +20,5 @@
     global.mGameTitleMusic[arg0] = -1;
     global.mGameTitleMusicLoop[arg0] = false;
+    global.mGameOnlineHUDHAlign[arg0] = 2;
+    global.mGameOnlineHUDVAlign[arg0] = 0;
 }
--- original/gml_GlobalScript_scr50_MessageFast.gml
+++ patched/gml_GlobalScript_scr50_MessageFast.gml
@@ -1,5 +1,5 @@
 function scr50_MessageFast(arg0)
 {
-    if (controller == 3)
+    if (controller == 3 || (numPlayers == 2 && scr50_IsOnlineRemotePlayer(controller)))
     {
         exit;
--- original/gml_GlobalScript_scr50_MessageFastLocal.gml
+++ patched/gml_GlobalScript_scr50_MessageFastLocal.gml
@@ -1,5 +1,5 @@
 function scr50_MessageFastLocal(arg0)
 {
-    if (controller == 3)
+    if (controller == 3 || (numPlayers == 2 && scr50_IsOnlineRemotePlayer(controller)))
     {
         exit;
--- original/gml_GlobalScript_scr50_MessageSlow.gml
+++ patched/gml_GlobalScript_scr50_MessageSlow.gml
@@ -1,5 +1,5 @@
 function scr50_MessageSlow(arg0)
 {
-    if (controller == 3)
+    if (controller == 3 || (numPlayers == 2 && scr50_IsOnlineRemotePlayer(controller)))
     {
         exit;
--- original/gml_GlobalScript_scr50_Meta.gml
+++ patched/gml_GlobalScript_scr50_Meta.gml
@@ -20,3 +20,6 @@
     global.mGameTitleMusic[arg0] = -1;
     global.mGameTitleMusicLoop[arg0] = false;
+    global.mGameOnlineHUDHAlign[arg0] = 2;
+    global.mGameOnlineHUDVAlign[arg0] = 0;
+    global.mGameOnlineFavoredPlayerFunction[arg0] = scrFunction(o50_Game, scr50_GetFavoredPlayer);
 }
--- original/gml_GlobalScript_scr50_Sfx.gml
+++ patched/gml_GlobalScript_scr50_Sfx.gml
@@ -1,5 +1,5 @@
 function scr50_Sfx(arg0, arg1)
 {
-    if (controller == 3)
+    if (controller == 3 || (state != STATE_COMBAT && numPlayers == 2 && scr50_IsOnlineRemotePlayer(controller)))
     {
         exit;
--- original/gml_GlobalScript_scrAch.gml
+++ patched/gml_GlobalScript_scrAch.gml
@@ -21,5 +21,5 @@
     }
     trace("Attempting to set Steam achievement...", _ach);
-    if (global.steamReady)
+    if (global.steamReady && !is_int64(pfo_file_status(global.ACCOUNT_FILE)))
     {
         var _achSteamID = scrGetSteamAchID(_ach);
--- original/gml_GlobalScript_scrCheckForAch.gml
+++ patched/gml_GlobalScript_scrCheckForAch.gml
@@ -260,5 +260,5 @@
     }
     scrCloseCurrFile();
-    if (global.steamReady)
+    if (global.steamReady && !is_int64(pfo_file_status(global.ACCOUNT_FILE)))
     {
         var _achSteamID = "";
--- original/gml_GlobalScript_scrCloseAccount.gml
+++ patched/gml_GlobalScript_scrCloseAccount.gml
@@ -11,5 +11,5 @@
         var _buffer = buffer_create(string_byte_length(_filestring) + 1, buffer_fixed, 1);
         buffer_write(_buffer, buffer_string, _filestring);
-        buffer_save(_buffer, global.ACCOUNT_FILE);
+        pfo_buffer_save(_buffer, global.ACCOUNT_FILE);
         buffer_delete(_buffer);
         global.needToSaveAccount = false;
--- original/gml_GlobalScript_scrCloseCurrFile.gml
+++ patched/gml_GlobalScript_scrCloseCurrFile.gml
@@ -16,5 +16,5 @@
         var _buffer = buffer_create(string_byte_length(_filestring) + 1, buffer_fixed, 1);
         buffer_write(_buffer, buffer_string, _filestring);
-        buffer_save(_buffer, _filename);
+        pfo_buffer_save(_buffer, _filename);
         buffer_delete(_buffer);
         global.needToSaveProfile = false;
--- original/gml_GlobalScript_scrCurrentTime.gml
+++ patched/gml_GlobalScript_scrCurrentTime.gml
@@ -1,7 +1,4 @@
 function scrCurrentTime()
 {
-    with (oController)
-    {
-        return totalTime / 1000;
-    }
+    return global.pfo_current_time;
 }
--- original/gml_GlobalScript_scrDeactivateOutsideView.gml
+++ patched/gml_GlobalScript_scrDeactivateOutsideView.gml
@@ -1,4 +1,5 @@
 function scrDeactivateOutsideView(arg0, arg1)
 {
+    var _spectatorPauseMenuActive = instance_exists(global.onlineSpectatorPauseMenu);
     var _xv = camera_get_view_x(view_get_camera(0));
     var _yv = camera_get_view_y(view_get_camera(0));
@@ -8,3 +9,7 @@
     thresh = 200;
     scrDeactivateRegion(_xv - arg0 - thresh, _yv - arg1 - thresh, 384 + ((arg0 + thresh) * 2), 216 + ((arg1 + thresh) * 2), false, true);
+    if (_spectatorPauseMenuActive)
+    {
+        instance_activate_object(global.onlineSpectatorPauseMenu);
+    }
 }
--- original/gml_GlobalScript_scrDeactivateRegion.gml
+++ patched/gml_GlobalScript_scrDeactivateRegion.gml
@@ -1,6 +1,11 @@
 function scrDeactivateRegion(arg0, arg1, arg2, arg3, arg4, arg5)
 {
+    var _spectatorPauseMenuActive = instance_exists(global.onlineSpectatorPauseMenu);
     var ret = instance_deactivate_region(arg0 + 1, arg1 + 1, arg2 - 1, arg3 - 1, arg4, arg5);
     scrActivateCoreObjects();
+    if (_spectatorPauseMenuActive)
+    {
+        instance_activate_object(global.onlineSpectatorPauseMenu);
+    }
     return ret;
 }
--- original/gml_GlobalScript_scrDefineMeta.gml
+++ patched/gml_GlobalScript_scrDefineMeta.gml
@@ -13,4 +13,5 @@
         global.mGameGenre[i] = 0;
         global.mGameRealtime[i] = true;
+        global.mGamePlayersSupported[i] = 0;
         for (var j = 0; j < global.NUM_PILLARS; j++)
         {
@@ -29,4 +30,7 @@
         global.mGameTitleMusic[i] = -1;
         global.mGameTitleMusicLoop[i] = false;
+        global.mGameOnlineHUDHAlign[i] = 2;
+        global.mGameOnlineHUDVAlign[i] = 2;
+        global.mGameOnlineFavoredPlayerFunction[i] = -1;
     }
     nGame = 1;
@@ -209,3 +213,14 @@
     scr51_Meta(nGame);
     nGame++;
+    for (var i = 1; i <= global.NUM_GAMES; i++)
+    {
+        if (global.mGamePlayersSupported[i] == 0)
+        {
+            global.mGamePlayersSupported[i] = global.mGameMultiplayer[i] ? 2 : 1;
+        }
+        if (global.MAX_PLAYERS_SUPPORTED < global.mGamePlayersSupported[i])
+        {
+            global.MAX_PLAYERS_SUPPORTED = global.mGamePlayersSupported[i];
+        }
+    }
 }
--- original/gml_GlobalScript_scrDrawJoyIcon.gml
+++ patched/gml_GlobalScript_scrDrawJoyIcon.gml
@@ -1,4 +1,8 @@
 function scrDrawJoyIcon(arg0, arg1, arg2, arg3)
 {
+    if (pfo_is_online())
+    {
+        arg3 = 0;
+    }
     draw_sprite(global.JOY_TYPE_SPRITE[global.joyType[arg3]], scrGetJoyIndex(arg2), arg0, arg1);
 }
--- original/gml_GlobalScript_scrDrawProfile.gml
+++ patched/gml_GlobalScript_scrDrawProfile.gml
@@ -1,5 +1,5 @@
-function scrDrawProfile(arg0, arg1, arg2)
+function scrDrawProfile(arg0, arg1, arg2, arg3)
 {
-    scrDrawMenuBorder(arg0, arg1, 256, 32);
+    scrDrawMenuBorder(arg0, arg1, 256 + arg3, 32);
     scrSetFont(global.fontTall);
     var _textTop = arg1 + 8;
@@ -18,5 +18,5 @@
         draw_text(arg0 + 184, _textTop + 8, ":" + scrTimeFormat(timePlayed[arg2], -3));
         draw_set_halign(fa_left);
-        var _winsAddX = 216;
+        var _winsAddX = 216 + arg3;
         draw_sprite(sWinIcons, global.GOLD_WIN, arg0 + _winsAddX, _textTop);
         var winString;
--- original/gml_GlobalScript_scrDrawTextInput.gml
+++ patched/gml_GlobalScript_scrDrawTextInput.gml
@@ -1,4 +1,8 @@
 function scrDrawTextInput(arg0, arg1, arg2, arg3, arg4, arg5)
 {
+    if (pfo_is_online())
+    {
+        arg3 = 0;
+    }
     var xx = arg0;
     var yy = arg1;
@@ -8,4 +12,6 @@
     var center = arg5;
     var makeEven = false;
+    var _xview = camera_get_view_x(view_get_camera(0));
+    xx -= _xview;
     if (type == 0)
     {
@@ -155,5 +161,5 @@
             if (makeEven && center == 3)
             {
-                draw_text(xx, yy, " ");
+                draw_text(xx + _xview, yy, " ");
                 xx += 8;
                 makeEven = false;
@@ -195,5 +201,5 @@
             if (code != -1)
             {
-                draw_sprite_ext(sVirtualInputs, code, xx, yy, 1, 1, 0, draw_get_color(), 1);
+                draw_sprite_ext(sVirtualInputs, code, xx + _xview, yy, 1, 1, 0, draw_get_color(), 1);
                 xx += 8;
             }
@@ -206,5 +212,5 @@
             if (makeEven && center == 4)
             {
-                draw_text(xx, yy, " ");
+                draw_text(xx + _xview, yy, " ");
                 xx += 8;
                 makeEven = false;
@@ -215,5 +221,5 @@
             if (makeEven && center == 3)
             {
-                draw_text(xx, yy, " ");
+                draw_text(xx + _xview, yy, " ");
                 xx += 8;
                 makeEven = false;
@@ -257,5 +263,5 @@
                 if (code != -1)
                 {
-                    scrDrawJoyIcon(xx, yy, code, arg3);
+                    scrDrawJoyIcon(xx + _xview, yy, code, arg3);
                     xx += global.JOY_ICON_WIDTH[scrGetJoyIndex(code)];
                 }
@@ -305,5 +311,5 @@
                 if (code != -1)
                 {
-                    scrDrawKeyIcon(xx, yy, code);
+                    scrDrawKeyIcon(xx + _xview, yy, code);
                     xx += global.KEY_ICON_WIDTH[code];
                 }
@@ -317,5 +323,5 @@
             if (makeEven && center == 4)
             {
-                draw_text(xx, yy, " ");
+                draw_text(xx + _xview, yy, " ");
                 xx += 8;
                 makeEven = false;
@@ -324,5 +330,5 @@
         else
         {
-            draw_text(xx, yy, char);
+            draw_text(xx + _xview, yy, char);
             xx += 8;
         }
--- original/gml_GlobalScript_scrFFrame.gml
+++ patched/gml_GlobalScript_scrFFrame.gml
@@ -6,5 +6,5 @@
     }
     var fframe = instance_create_depth(x, y, 0, oFFrame);
-    fframe.fSpeed = game_get_speed(gamespeed_fps);
-    game_set_speed(arg0, gamespeed_fps);
+    fframe.fSpeed = pfo_game_get_speed(0);
+    pfo_game_set_speed(arg0, 0);
 }
--- original/gml_GlobalScript_scrGetInput.gml
+++ patched/gml_GlobalScript_scrGetInput.gml
@@ -1,8 +1,20 @@
 function scrGetInput(arg0)
 {
-    var _slot = global.joySlot[arg0];
-    if (global.playbackMode && is_array(global.inputPlayback) && instance_exists(oGame))
+    var _getInputType = (argument_count > 1) ? argument[1] : UnknownEnum.Value_0;
+    if ((_getInputType & UnknownEnum.Value_1) != 0 && pfo_is_online())
     {
+        arg0 = 0;
+    }
+    if ((_getInputType & UnknownEnum.Value_2) == 0 && global.playbackMode && is_array(global.inputPlayback) && instance_exists(oGame))
+    {
+        if ((_getInputType & UnknownEnum.Value_1) == 0 && pfo_is_online())
+        {
+            pressStart = (pfo_remove_extra_input(pfo_player_get_input(arg0)) & UnknownEnum.Value_8192) != 0;
+        }
+        else
+        {
+            var _slot = global.joySlot[arg0];
         pressStart = keyboard_check_pressed(global.keyMap[arg0][global.INP_START]) || gamepad_button_check_pressed(_slot, global.joyMap[arg0][global.INP_START]) || keyboard_check_pressed(vk_escape);
+        }
         if (pressStart)
         {
@@ -13,4 +25,11 @@
         exit;
     }
+    if ((_getInputType & UnknownEnum.Value_1) == 0 && pfo_is_online())
+    {
+        scrSetInputFromPFOFlags(pfo_remove_extra_input(pfo_player_get_input(arg0)));
+    }
+    else
+    {
+        var _slot = global.joySlot[arg0];
     holdUp = keyboard_check(global.keyMap[arg0][global.INP_UP]);
     holdDown = keyboard_check(global.keyMap[arg0][global.INP_DOWN]);
@@ -145,4 +164,5 @@
     }
     pressStart = keyboard_check_pressed(global.keyMap[arg0][global.INP_START]) || gamepad_button_check_pressed(_slot, global.joyMap[arg0][global.INP_START]) || keyboard_check_pressed(vk_escape);
+        forcePause = global.steamOverlayActivated;
     if (holdLeft && holdRight)
     {
@@ -184,3 +204,12 @@
         global.inputFocus = 2;
     }
+    }
+}
+
+enum UnknownEnum
+{
+    Value_0,
+    Value_1,
+    Value_2,
+    Value_8192 = 8192
 }
--- original/gml_GlobalScript_scrGetInputBoth.gml
+++ patched/gml_GlobalScript_scrGetInputBoth.gml
@@ -3,5 +3,5 @@
     for (var p = 1; p >= 0; p--)
     {
-        scrGetInput(p);
+        scrGetInput(p, (argument_count > 0) ? argument[0] : UnknownEnum.Value_0);
         bHoldUp[p] = holdUp;
         bHoldDown[p] = holdDown;
@@ -19,4 +19,10 @@
         bFire2released[p] = fire2released;
         bPressStart[p] = pressStart;
+        bForcePause[p] = forcePause;
     }
+}
+
+enum UnknownEnum
+{
+    Value_0
 }
--- original/gml_GlobalScript_scrGetInputBothStart.gml
+++ patched/gml_GlobalScript_scrGetInputBothStart.gml
@@ -1,4 +1,6 @@
 function scrGetInputBothStart()
 {
+    scrGetInputBoth();
+    exit;
     for (var p = 1; p >= 0; p--)
     {
--- original/gml_GlobalScript_scrGetInputIconWidth.gml
+++ patched/gml_GlobalScript_scrGetInputIconWidth.gml
@@ -1,4 +1,8 @@
 function scrGetInputIconWidth(arg0, arg1)
 {
+    if (pfo_is_online())
+    {
+        arg0 = 0;
+    }
     if (global.inputFocus == 1)
     {
--- original/gml_GlobalScript_scrGetInputMerged.gml
+++ patched/gml_GlobalScript_scrGetInputMerged.gml
@@ -1,5 +1,5 @@
 function scrGetInputMerged()
 {
-    scrGetInputBoth();
+    scrGetInputBoth((argument_count > 0) ? argument[0] : UnknownEnum.Value_0);
     holdUp = bHoldUp[0] || bHoldUp[1];
     holdDown = bHoldDown[0] || bHoldDown[1];
@@ -15,3 +15,9 @@
     fire2pressed = bFire2pressed[0] || bFire2pressed[1];
     pressStart = bPressStart[0] || bPressStart[1];
+    forcePause = bForcePause[0] || bForcePause[1];
+}
+
+enum UnknownEnum
+{
+    Value_0
 }
--- original/gml_GlobalScript_scrGetInputMergedFire1.gml
+++ patched/gml_GlobalScript_scrGetInputMergedFire1.gml
@@ -1,4 +1,6 @@
 function scrGetInputMergedFire1()
 {
+    scrGetInputMerged();
+    exit;
     for (var p = 1; p >= 0; p--)
     {
--- original/gml_GlobalScript_scrGetOkay.gml
+++ patched/gml_GlobalScript_scrGetOkay.gml
@@ -3,9 +3,21 @@
     if (!instance_exists(oOkay))
     {
-        instance_create_depth(0, 0, 0, oOkay);
+        instance_create_depth(0, 0, depth - 1, oOkay);
         with (oOkay)
         {
-            if (arg0 != "")
+            ours = undefined;
+            theirs = undefined;
+            if (is_array(arg0))
             {
+                str = arg0[0];
+                ours = arg0[1];
+                theirs = arg0[2];
+            }
+            else if (other.object_index == oLibrary)
+            {
+                str = arg0;
+            }
+            else if (arg0 != "")
+            {
                 str = scrStringManual(arg0, 0);
             }
@@ -14,6 +26,20 @@
                 str = "";
             }
+            inputMode = (argument_count > 1) ? argument[1] : UnknownEnum.Value_3;
+            if (other.object_index == oPauseMenu)
+            {
+                menuType = other.menuType;
         }
+            else
+            {
+                menuType = 0;
     }
+        }
+    }
     return oOkay.okay;
+}
+
+enum UnknownEnum
+{
+    Value_3 = 3
 }
--- original/gml_GlobalScript_scrInit.gml
+++ patched/gml_GlobalScript_scrInit.gml
@@ -53,4 +53,5 @@
     scrInitTerminal();
     global.NUM_LIBRARY_GAMES = 50;
+    global.MAX_PLAYERS_SUPPORTED = 0;
     global.currGame = 0;
     global.currGameID = 0;
@@ -76,4 +77,9 @@
     }
     scrInitInput();
+    pfo_start();
+    global.s07_bossXCutsceneRoom = undefined;
+    global.s23_attractModeVariant = 0;
+    global.s35_enemyAttackSwoopAttToX = undefined;
+    global.s35_enemyAttackSwoopAttToY = undefined;
     global.attractMode = false;
     global.attractModeLibrary = false;
@@ -82,11 +88,4 @@
     global.AM_LIB_TIME_PER_GAME = 1200;
     global.attractModePlaylist = ds_list_create();
-    for (var i = 1; i <= global.NUM_LIBRARY_GAMES; i++)
-    {
-        if (global.mGameAttractModeTimer[i] && global.mGameRoom[i] != -1)
-        {
-            ds_list_add(global.attractModePlaylist, i);
-        }
-    }
     global.attractModeIndex = 0;
     global.playbackMode = false;
@@ -133,5 +132,5 @@
     scrInitAch();
     scrInitRandomness();
-    scrShuffle(global.attractModePlaylist);
+    scrInitAttractModePlaylist();
     global.BACKUP_MINIMUM_TIME = 72000;
     global.NUM_BACKUPS = 20;
--- original/gml_GlobalScript_scrInitAch.gml
+++ patched/gml_GlobalScript_scrInitAch.gml
@@ -1,6 +1,9 @@
 function scrInitAch()
 {
+    if (!instance_exists(oAchManager))
+    {
     instance_create_depth(-32, -32, 0, oAchManager);
     scrDefineAch();
+    }
     scrOpenAccount();
     for (var i = 0; i < global.ACH_TOTAL; i++)
--- original/gml_GlobalScript_scrInitDisplay.gml
+++ patched/gml_GlobalScript_scrInitDisplay.gml
@@ -13,6 +13,10 @@
     global.DEFAULT_SCALE = max(1, global.scaleMax - 2);
     scrSetDisplayDefaults(true);
-    global.scale = min(global.scale, global.scaleMax + 1);
+    global.scale = min(global.scale, (global.scaleFill <= global.scaleMax) ? global.scaleMax : (global.scaleMax + 1));
     global.canDoShaders = shaders_are_supported();
+    if (global.canDoShaders)
+    {
+        application_surface_draw_enable(false);
+    }
     instance_create_depth(-32, -32, 0, oScreenHandler);
     window_set_size(384 * global.scale, 216 * global.scale);
--- original/gml_GlobalScript_scrInputAny.gml
+++ patched/gml_GlobalScript_scrInputAny.gml
@@ -1,23 +1,22 @@
 function scrInputAny()
 {
-    var _slot0 = global.joySlot[0];
     if (!global.playbackMode)
     {
-        if (holdUp || holdDown || holdLeft || holdRight || fire1 || fire2 || pressStart)
-        {
-            return true;
+        return holdUp || holdDown || holdLeft || holdRight || fire1 || fire2 || pressStart;
         }
         else
         {
-            return false;
+        with (oScreenHandler)
+        {
+            scrInputClear();
+            scrGetInput(0, UnknownEnum.Value_2 | ((argument_count > 0) ? argument[0] : UnknownEnum.Value_0));
+            return pressUp || pressDown || pressLeft || pressRight || fire1pressed || fire2pressed || pressStart;
         }
     }
-    else if (keyboard_check_pressed(global.keyMap[0][global.INP_UP]) || keyboard_check_pressed(global.keyMap[0][global.INP_DOWN]) || keyboard_check_pressed(global.keyMap[0][global.INP_LEFT]) || keyboard_check_pressed(global.keyMap[0][global.INP_RIGHT]) || keyboard_check_pressed(global.keyMap[0][global.INP_FIRE1]) || keyboard_check_pressed(global.keyMap[0][global.INP_FIRE2]) || keyboard_check_pressed(global.keyMap[0][global.INP_START]) || keyboard_check_pressed(vk_escape) || gamepad_button_check_pressed(_slot0, gp_padu) || gamepad_button_check_pressed(_slot0, gp_padd) || gamepad_button_check_pressed(_slot0, gp_padl) || gamepad_button_check_pressed(_slot0, gp_padr) || gamepad_button_check_pressed(_slot0, global.joyMap[0][global.INP_FIRE1]) || gamepad_button_check_pressed(_slot0, global.joyMap[0][global.INP_FIRE2]) || gamepad_button_check_pressed(_slot0, global.joyMap[0][global.INP_START]))
-    {
-        return true;
     }
-    else
+
+enum UnknownEnum
     {
-        return false;
-    }
+    Value_0,
+    Value_2 = 2
 }
--- original/gml_GlobalScript_scrInputAnyBoth.gml
+++ patched/gml_GlobalScript_scrInputAnyBoth.gml
@@ -1,24 +1,22 @@
 function scrInputAnyBoth()
 {
-    var _slot0 = global.joySlot[0];
-    var _slot1 = global.joySlot[1];
     if (!global.playbackMode)
     {
-        if (holdUp || holdDown || holdLeft || holdRight || fire1 || fire2 || pressStart)
-        {
-            return true;
+        return holdUp || holdDown || holdLeft || holdRight || fire1 || fire2 || pressStart;
         }
         else
         {
-            return false;
+        with (oScreenHandler)
+        {
+            scrInputClear();
+            scrGetInputBoth(UnknownEnum.Value_2 | ((argument_count > 0) ? argument[0] : UnknownEnum.Value_0));
+            return bPressUp[0] || bPressDown[0] || bPressLeft[0] || bPressRight[0] || bFire1pressed[0] || bFire2pressed[0] || bPressStart[0] || bPressUp[1] || bPressDown[1] || bPressLeft[1] || bPressRight[1] || bFire1pressed[1] || bFire2pressed[1] || bPressStart[1];
         }
     }
-    else if (keyboard_check_pressed(global.keyMap[0][global.INP_UP]) || keyboard_check_pressed(global.keyMap[0][global.INP_DOWN]) || keyboard_check_pressed(global.keyMap[0][global.INP_LEFT]) || keyboard_check_pressed(global.keyMap[0][global.INP_RIGHT]) || keyboard_check_pressed(global.keyMap[0][global.INP_FIRE1]) || keyboard_check_pressed(global.keyMap[0][global.INP_FIRE2]) || keyboard_check_pressed(global.keyMap[0][global.INP_START]) || keyboard_check_pressed(vk_escape) || gamepad_button_check_pressed(_slot0, gp_padu) || gamepad_button_check_pressed(_slot0, gp_padd) || gamepad_button_check_pressed(_slot0, gp_padl) || gamepad_button_check_pressed(_slot0, gp_padr) || gamepad_button_check_pressed(_slot0, global.joyMap[0][global.INP_FIRE1]) || gamepad_button_check_pressed(_slot0, global.joyMap[0][global.INP_FIRE2]) || gamepad_button_check_pressed(_slot0, global.joyMap[0][global.INP_START]) || keyboard_check_pressed(global.keyMap[1][global.INP_UP]) || keyboard_check_pressed(global.keyMap[1][global.INP_DOWN]) || keyboard_check_pressed(global.keyMap[1][global.INP_LEFT]) || keyboard_check_pressed(global.keyMap[1][global.INP_RIGHT]) || keyboard_check_pressed(global.keyMap[1][global.INP_FIRE1]) || keyboard_check_pressed(global.keyMap[1][global.INP_FIRE2]) || keyboard_check_pressed(global.keyMap[1][global.INP_START]) || gamepad_button_check_pressed(_slot1, gp_padu) || gamepad_button_check_pressed(_slot1, gp_padd) || gamepad_button_check_pressed(_slot1, gp_padl) || gamepad_button_check_pressed(_slot1, gp_padr) || gamepad_button_check_pressed(_slot1, global.joyMap[0][global.INP_FIRE1]) || gamepad_button_check_pressed(_slot1, global.joyMap[0][global.INP_FIRE2]) || gamepad_button_check_pressed(_slot1, global.joyMap[0][global.INP_START]))
-    {
-        return true;
     }
-    else
+
+enum UnknownEnum
     {
-        return false;
-    }
+    Value_0,
+    Value_2 = 2
 }
--- original/gml_GlobalScript_scrInputClear.gml
+++ patched/gml_GlobalScript_scrInputClear.gml
@@ -16,4 +16,5 @@
     fire2released = false;
     pressStart = false;
+    forcePause = false;
     if (argument_count == 0 || argument[0] == true)
     {
@@ -25,5 +26,5 @@
         }
     }
-    if (global.paused)
+    if (object_index == oPauseMenu)
     {
         keyboard_clear(vk_enter);
--- original/gml_GlobalScript_scrInputClearBoth.gml
+++ patched/gml_GlobalScript_scrInputClearBoth.gml
@@ -18,4 +18,5 @@
         bFire2released[p] = false;
         bPressStart[p] = false;
+        bForcePause[p] = false;
     }
 }
--- original/gml_GlobalScript_scrInputClearButtons.gml
+++ patched/gml_GlobalScript_scrInputClearButtons.gml
@@ -16,5 +16,5 @@
     pressRight = false;
     pressStart = false;
-    if (global.paused)
+    if (object_index == oPauseMenu)
     {
         keyboard_clear(vk_enter);
--- original/gml_GlobalScript_scrMenuCreate.gml
+++ patched/gml_GlobalScript_scrMenuCreate.gml
@@ -14,4 +14,5 @@
     TYPE_INFO_CONTENT = 11;
     TYPE_DUAL_DETECT = 12;
+    TYPE_DUAL_ONLINE_PLAYER = 13;
     menuHeader = arg0;
     menuSel = arg1;
@@ -23,3 +24,5 @@
     itemIndex = false;
     itemSelectable = false;
+    firstOnlinePlayerMenuIndex = -1;
+    lastOnlinePlayerMenuIndex = -1;
 }
--- original/gml_GlobalScript_scrMenuItem.gml
+++ patched/gml_GlobalScript_scrMenuItem.gml
@@ -24,5 +24,5 @@
         itemValues[menuIndex] = _values;
     }
-    if (argument[0] == TYPE_DUAL || argument[0] == TYPE_DUAL_CENTER)
+    if (argument[0] == TYPE_DUAL || argument[0] == TYPE_DUAL_CENTER || argument[0] == TYPE_DUAL_ONLINE_PLAYER)
     {
         itemName[menuIndex] = argument[1];
@@ -42,4 +42,12 @@
         }
         itemSelectable[menuIndex] = true;
+        if (argument[0] == TYPE_DUAL_ONLINE_PLAYER)
+        {
+            if (firstOnlinePlayerMenuIndex == -1)
+            {
+                firstOnlinePlayerMenuIndex = menuIndex;
+            }
+            lastOnlinePlayerMenuIndex = menuIndex;
+        }
     }
     if (argument[0] == TYPE_DUAL_KEY || argument[0] == TYPE_DUAL_JOY || argument[0] == TYPE_DUAL_DETECT)
--- original/gml_GlobalScript_scrMenuNavigation.gml
+++ patched/gml_GlobalScript_scrMenuNavigation.gml
@@ -10,5 +10,6 @@
     }
     var _startSel = menuSel;
-    if (keyboard_check_pressed(vk_up))
+    var _usingLocalInput = usingLocalInput();
+    if (_usingLocalInput && keyboard_check_pressed(vk_up))
     {
         do
@@ -22,5 +23,5 @@
         until (itemSelectable[menuSel]);
     }
-    else if (keyboard_check_pressed(vk_down))
+    else if (_usingLocalInput && keyboard_check_pressed(vk_down))
     {
         do
@@ -81,5 +82,5 @@
         var _left = false;
         var _right = false;
-        if (keyboard_check_pressed(vk_left) && _numItems > 1)
+        if (_usingLocalInput && keyboard_check_pressed(vk_left) && _numItems > 1)
         {
             arrowLeftActive = arrowActiveMax;
@@ -91,5 +92,5 @@
             _left = true;
         }
-        else if (keyboard_check_pressed(vk_enter))
+        else if (_usingLocalInput && keyboard_check_pressed(vk_enter))
         {
             itemIndex[menuSel]++;
@@ -107,5 +108,5 @@
             return itemIndex[menuSel];
         }
-        else if (keyboard_check_pressed(vk_right))
+        else if (_usingLocalInput && keyboard_check_pressed(vk_right))
         {
             arrowRightActive = arrowActiveMax;
@@ -156,5 +157,5 @@
     if (itemType[menuSel] == TYPE_DUAL_KEY || itemType[menuSel] == TYPE_DUAL_JOY || itemType[menuSel] == TYPE_DUAL_DETECT)
     {
-        if (keyboard_check_pressed(vk_enter))
+        if (_usingLocalInput && keyboard_check_pressed(vk_enter))
         {
             itemIndex[menuSel]++;
@@ -195,5 +196,5 @@
     else if (itemType[menuSel] == TYPE_PLAYER)
     {
-        if (keyboard_check_pressed(vk_left) || keyboard_check_pressed(vk_right) || keyboard_check_pressed(vk_enter))
+        if (_usingLocalInput && (keyboard_check_pressed(vk_left) || keyboard_check_pressed(vk_right) || keyboard_check_pressed(vk_enter)))
         {
             if (keyboard_check_pressed(vk_left))
@@ -224,5 +225,5 @@
     else if (itemType[menuSel] == TYPE_SINGLE)
     {
-        if (keyboard_check_pressed(vk_enter))
+        if (_usingLocalInput && keyboard_check_pressed(vk_enter))
         {
             return itemIndex[menuSel];
@@ -237,5 +238,5 @@
         if (seekingConfirmation)
         {
-            if (keyboard_check_pressed(vk_enter))
+            if (_usingLocalInput && keyboard_check_pressed(vk_enter))
             {
                 seekingConfirmation = false;
@@ -248,5 +249,5 @@
             }
         }
-        else if (keyboard_check_pressed(vk_enter))
+        else if (_usingLocalInput && keyboard_check_pressed(vk_enter))
         {
             scrSfxLibrary(soundWarn[currentSoundSet]);
@@ -261,4 +262,92 @@
         }
     }
+    else if (itemType[menuSel] == TYPE_DUAL_ONLINE_PLAYER)
+    {
+        var _numItems = array_length(itemValues[menuSel]);
+        var _direction = 0;
+        if (pressRight || fire2pressed)
+        {
+            _direction = 1;
+        }
+        else if (pressLeft)
+        {
+            _direction = -1;
+        }
+        for (var _sel = firstOnlinePlayerMenuIndex; _sel < menuSel; _sel++)
+        {
+            if (itemIndex[_sel] >= (_numItems - 1))
+            {
+                _direction = 0;
+                break;
+            }
+        }
+        if (_direction != 0)
+        {
+            var _newIndex = itemIndex[menuSel];
+            for (var i = 1; i < _numItems; i++)
+            {
+                var _index = (itemIndex[menuSel] + _numItems + (i * _direction)) % _numItems;
+                if (_index < (_numItems - 1))
+                {
+                    var _found = false;
+                    for (var _sel = firstOnlinePlayerMenuIndex; _sel < menuSel; _sel++)
+                    {
+                        if (itemIndex[_sel] == _index)
+                        {
+                            _found = true;
+                            break;
+                        }
+                    }
+                    if (!_found && pfo_client_is_connected(_index))
+                    {
+                        _newIndex = _index;
+                        break;
+                    }
+                }
+                else if (menuSel != firstOnlinePlayerMenuIndex)
+                {
+                    var _found = false;
+                    var _sel = menuSel + 1;
+                    while (_sel <= lastOnlinePlayerMenuIndex)
+                    {
+                        if (itemIndex[_sel] < (_numItems - 1))
+                        {
+                            _found = true;
+                            break;
+                        }
+                        _sel++;
+                    }
+                    if (!_found)
+                    {
+                        _newIndex = _index;
+                        break;
+                    }
+                }
+            }
+            if (_newIndex != itemIndex[menuSel])
+            {
+                if (!localInputDisabled() && !pfo_send_command_in_progress())
+                {
+                    var _playerIndex = menuSel - firstOnlinePlayerMenuIndex;
+                    var _value = (_newIndex < (_numItems - 1)) ? _newIndex : -1;
+                    sendCommand(UnknownEnum.Value_4, (int64(_playerIndex) & UnknownEnum.Value_15) | ((int64(_value) & UnknownEnum.Value_15) << UnknownEnum.Value_4));
+                }
+                if (pressRight)
+                {
+                    arrowRightActive = arrowActiveMax;
+                }
+                else if (pressLeft)
+                {
+                    arrowLeftActive = arrowActiveMax;
+                }
+            }
+        }
+    }
     return -1;
+}
+
+enum UnknownEnum
+{
+    Value_4 = 4,
+    Value_15 = 15
 }
--- original/gml_GlobalScript_scrOpenAccount.gml
+++ patched/gml_GlobalScript_scrOpenAccount.gml
@@ -2,9 +2,9 @@
 {
     var _error = false;
-    if (!file_exists(global.ACCOUNT_FILE))
+    if (!pfo_file_exists(global.ACCOUNT_FILE))
     {
         return true;
     }
-    var _buffer = buffer_load(global.ACCOUNT_FILE);
+    var _buffer = pfo_buffer_load(global.ACCOUNT_FILE);
     if (_buffer == -1)
     {
@@ -32,5 +32,5 @@
     if (_error)
     {
-        file_delete(global.ACCOUNT_FILE);
+        pfo_file_delete(global.ACCOUNT_FILE);
     }
     return true;
--- original/gml_GlobalScript_scrOpenFileManual.gml
+++ patched/gml_GlobalScript_scrOpenFileManual.gml
@@ -9,9 +9,9 @@
     }
     global.currProfileSaveNum = arg0;
-    if (!file_exists(_filename))
+    if (!pfo_file_exists(_filename))
     {
         return true;
     }
-    var _buffer = buffer_load(_filename);
+    var _buffer = pfo_buffer_load(_filename);
     var _filestring = buffer_read(_buffer, buffer_string);
     _filestring = base64_decode(_filestring);
--- original/gml_GlobalScript_scrPause.gml
+++ patched/gml_GlobalScript_scrPause.gml
@@ -7,5 +7,5 @@
     global.paused = true;
     global.prePauseFont = global.currFont;
-    game_set_speed(60, gamespeed_fps);
+    pfo_game_set_speed(60, 0);
     if (audio_exists(global.SFX))
     {
@@ -22,12 +22,11 @@
         }
     }
-    var pauseMenu = instance_create_depth(room_width + 16, 0, 0, oPauseMenu);
-    with (pauseMenu)
+    var pauseMenu = instance_create_depth(room_width + 16, 0, -1000, oPauseMenu, 
     {
-        player = arg0;
-        joySlot = global.joySlot[arg0];
-    }
+        menuType: 0
+    });
     with (pauseMenu)
     {
+        player = arg0;
         instNum = instance_count;
         for (var i = instNum; i > 0; i--)
--- original/gml_GlobalScript_scrRandomize.gml
+++ patched/gml_GlobalScript_scrRandomize.gml
@@ -7,5 +7,5 @@
     else
     {
-        randomize();
+        pfo_randomize();
         rng_seed(random(99999999));
     }
--- original/gml_GlobalScript_scrSaveConfig.gml
+++ patched/gml_GlobalScript_scrSaveConfig.gml
@@ -31,5 +31,5 @@
     scrWriteConfig("joyTypeP1", global.joyType[0]);
     scrWriteConfig("joyTypeP2", global.joyType[1]);
-    scrWriteConfig("defaultLanguage", global.LANG_SAVE_ID[global.defaultLanguage]);
+    scrWriteConfig("defaultLanguage", global.LANG_SAVE_ID[is_undefined(global.onlineBackupDefaultLanguage) ? global.defaultLanguage : global.onlineBackupDefaultLanguage]);
     scrCloseConfig();
 }
--- original/gml_GlobalScript_scrSaveGame.gml
+++ patched/gml_GlobalScript_scrSaveGame.gml
@@ -37,4 +37,5 @@
     if (global.backupTimer >= global.BACKUP_MINIMUM_TIME && global.currFile >= 1 && global.currFile <= global.NUM_PROFILES_ACCESSIBLE)
     {
+        global.backupTimer = 0;
         global.backupSaveNum[global.currFile]++;
         if (global.backupSaveNum[global.currFile] > global.NUM_BACKUPS)
@@ -46,8 +47,7 @@
     }
     scrCloseCurrFile();
-    if (_makeBackup)
-    {
-        global.backupTimer = 0;
         var _fileFrom = string_replace(global.SAVE_FILE, "*", string(global.currFile));
+    if (_makeBackup && !is_int64(pfo_file_status(_fileFrom)))
+    {
         var _fileTo = string_copy(_fileFrom, 1, string_length(_fileFrom) - 4);
         _fileTo += ("_" + string(global.backupSaveNum[global.currFile]) + ".bak");
--- original/gml_GlobalScript_scrUnpause.gml
+++ patched/gml_GlobalScript_scrUnpause.gml
@@ -27,4 +27,6 @@
     with (oPauseMenu)
     {
+        if (menuType == 0)
+        {
         for (var i = 0; i < instNum; i++)
         {
@@ -37,4 +39,9 @@
         }
         instance_destroy();
+        }
+    }
+    if (!global.disableSettingOnlinePlayers)
+    {
+        scrSetOnlinePlayers(global.onlinePlayers);
     }
 }
--- original/gml_Object_o01_Game_Alarm_0.gml
+++ patched/gml_Object_o01_Game_Alarm_0.gml
@@ -10,12 +10,9 @@
 else
 {
-    if (numPlayers == 2)
+    if (numPlayers == 2 && !firstSpawn)
     {
-        currPlayer += 1;
+        currPlayer = !currPlayer;
     }
-    if (currPlayer > 1)
-    {
-        currPlayer = 0;
-    }
+    firstSpawn = false;
     state = 0;
     arrowBlink = 0;
--- original/gml_Object_o01_Game_Create_0.gml
+++ patched/gml_Object_o01_Game_Create_0.gml
@@ -11,12 +11,6 @@
 blackTransitionLength = 45;
 numPlayers = global.numPlayers;
-if (numPlayers == 1)
-{
     currPlayer = 0;
-}
-else
-{
-    currPlayer = 1;
-}
+firstSpawn = true;
 playerType = 0;
 numLives = 99;
--- original/gml_Object_o06_SwitchBarrier_Step_0.gml
+++ patched/gml_Object_o06_SwitchBarrier_Step_0.gml
@@ -31,5 +31,5 @@
 if (onTier == 3)
 {
-    image_index = 3 + (round(current_time / 100) % 2);
+    image_index = 3 + (round(global.pfo_current_time / 100) % 2);
 }
 else
--- original/gml_Object_o06__Game_Draw_0.gml
+++ patched/gml_Object_o06__Game_Draw_0.gml
@@ -198,9 +198,9 @@
 if (clubflash == 1)
 {
-    draw_sprite(s06_UIClub, (current_time / 1000) % 2, viewx + 16, viewy);
+    draw_sprite(s06_UIClub, (global.pfo_current_time / 1000) % 2, viewx + 16, viewy);
 }
 else if (clubflash == 2)
 {
-    draw_sprite(s06_UIClub, 3 + ((current_time / 1000) % 2), viewx + 16, viewy);
+    draw_sprite(s06_UIClub, 3 + ((global.pfo_current_time / 1000) % 2), viewx + 16, viewy);
 }
 else
@@ -239,5 +239,5 @@
     else if (o06_zPlayer.brakeCount == o06_zPlayer.brakeCountMax)
     {
-        draw_sprite(s06_pBrake, (current_time / 100) % 2, viewx + 24, viewy + 88 + 32 + pY);
+        draw_sprite(s06_pBrake, (global.pfo_current_time / 100) % 2, viewx + 24, viewy + 88 + 32 + pY);
     }
     else
--- original/gml_Object_o06_eMosquito_Draw_0.gml
+++ patched/gml_Object_o06_eMosquito_Draw_0.gml
@@ -7,5 +7,5 @@
 if (latchOn == -4)
 {
-    _yoff = lengthdir_x(2, current_time / 10);
+    _yoff = lengthdir_x(2, global.pfo_current_time / 10);
     draw_sprite(s06_Shadow, 0, x, y);
 }
--- original/gml_Object_o06_pPowerUp_Draw_0.gml
+++ patched/gml_Object_o06_pPowerUp_Draw_0.gml
@@ -1,2 +1,2 @@
 draw_sprite(s06_Shadow, 0, x, y);
-draw_sprite(sprite_index, 0, x, (y - 4) + lengthdir_x(2, current_time / 8));
+draw_sprite(sprite_index, 0, x, (y - 4) + lengthdir_x(2, global.pfo_current_time / 8));
--- original/gml_Object_o07_AcidSurfDown_Step_0.gml
+++ patched/gml_Object_o07_AcidSurfDown_Step_0.gml
@@ -1 +1 @@
-image_index = round(current_time / 100);
+image_index = round(global.pfo_current_time / 100);
--- original/gml_Object_o07_AcidSurfUp_Step_0.gml
+++ patched/gml_Object_o07_AcidSurfUp_Step_0.gml
@@ -1 +1 @@
-image_index = round(current_time / 100);
+image_index = round(global.pfo_current_time / 100);
--- original/gml_Object_o07_Player_Draw_0.gml
+++ patched/gml_Object_o07_Player_Draw_0.gml
@@ -84,5 +84,5 @@
     if (o07__Game.modSlot[2] == 2)
     {
-        if ((floor(current_time * 0.2) % 2) == 1)
+        if ((floor(global.pfo_current_time * 0.2) % 2) == 1)
         {
             scrSpriteColored(s07_pTorsoH, imgTorso, _drawX, _drawY, xscale, yscale, global.palette[3]);
@@ -109,9 +109,9 @@
                 if (floor(leechNum) == _f || ceil(leechNum) == _f || floor(leechNum) == (_f + 1) || ceil(leechNum) == (_f + 1))
                 {
-                    draw_sprite(s07_LeechSparkSiphon, (floor(_dir / 45) * 4) + floor(((current_time * 0.02) + _f) % 4), _x, _y);
+                    draw_sprite(s07_LeechSparkSiphon, (floor(_dir / 45) * 4) + floor(((global.pfo_current_time * 0.02) + _f) % 4), _x, _y);
                 }
                 else if ((_f % 2) == 1)
                 {
-                    draw_sprite(s07_LeechSpark, (floor(_dir / 45) * 4) + floor(((current_time * 0.02) + _f) % 4), _x, _y);
+                    draw_sprite(s07_LeechSpark, (floor(_dir / 45) * 4) + floor(((global.pfo_current_time * 0.02) + _f) % 4), _x, _y);
                 }
                 if (point_distance(_x, _y, leechPosX, leechPosY) <= (_dist * 2))
--- original/gml_Object_o07__Game_Create_0.gml
+++ patched/gml_Object_o07__Game_Create_0.gml
@@ -34,5 +34,5 @@
 savePosY = 0;
 savePosFlip = false;
-previous_second = current_second;
+previous_second = global.pfo_current_time div 1000;
 hpMax = 75;
 hp = hpMax;
--- original/gml_Object_o07__Game_Step_0.gml
+++ patched/gml_Object_o07__Game_Step_0.gml
@@ -3,8 +3,9 @@
     exit;
 }
-if (current_second != previous_second && introPlaying == 0)
+var _current_second = global.pfo_current_time div 1000;
+if (_current_second != previous_second && introPlaying == 0)
 {
     global.g07_totalTime++;
-    previous_second = current_second;
+    previous_second = _current_second;
 }
 enemyWallCheckCount++;
--- original/gml_Object_o07_eBossX_MeatWall_Draw_0.gml
+++ patched/gml_Object_o07_eBossX_MeatWall_Draw_0.gml
@@ -1,3 +1,4 @@
 var _offsetX = 0;
+var _current_second = global.pfo_current_time div 1000;
 if (leftWall)
 {
@@ -10,5 +11,5 @@
             _offsetX = round(lengthdir_x(4 * wriggleAmt, (wriggleTime / 5) + (i * (30 + lengthdir_x(15, wriggleTime / 13)))) / 4) * 8;
         }
-        draw_sprite_ext(s07_eSpikesRed, 0, x + 8 + _offsetX + 16 + (4 * ((current_second + i) % 2)), y + (16 * i), 1, 1, 0, c_white, 1);
+        draw_sprite_ext(s07_eSpikesRed, 0, x + 8 + _offsetX + 16 + (4 * ((_current_second + i) % 2)), y + (16 * i), 1, 1, 0, c_white, 1);
         draw_sprite_part(bg07_GravGuns, 0, 96, 304, 64, 16, (x - 32) + _offsetX, y + (16 * i));
     }
@@ -26,5 +27,5 @@
             _offsetX = round(lengthdir_x(4 * wriggleAmt, (wriggleTime / 5) + (i * (30 + lengthdir_x(15, wriggleTime / 11))) + 90) / 4) * 4;
         }
-        draw_sprite_ext(s07_eSpikesRed, 0, (x + _offsetX) - 8 - (4 * ((current_second + i) % 2)), y + (16 * i), 1, 1, 0, c_white, 1);
+        draw_sprite_ext(s07_eSpikesRed, 0, (x + _offsetX) - 8 - (4 * ((_current_second + i) % 2)), y + (16 * i), 1, 1, 0, c_white, 1);
         draw_sprite_part(bg07_GravGuns, 0, 80, 304, 64, 16, x + _offsetX, y + (16 * i));
     }
--- original/gml_Object_o07_eBossX_Phase3_Head_Step_0.gml
+++ patched/gml_Object_o07_eBossX_Phase3_Head_Step_0.gml
@@ -51,5 +51,5 @@
     }
     num = _k;
-    x = lerp(other.x, myBody.x + (32 * myBody.image_xscale), 1 - (_k / _neckLength)) + lengthdir_x(4, (current_time * 0.5) - (_k * 45));
+    x = lerp(other.x, myBody.x + (32 * myBody.image_xscale), 1 - (_k / _neckLength)) + lengthdir_x(4, (global.pfo_current_time * 0.5) - (_k * 45));
     y = lerp(other.y - 8, myBody.y + (24 * myBody.image_yscale), 1 - (_k / _neckLength));
     with (o07_eBossX_Phase3_Ball)
--- original/gml_Object_o07_eBossX_TentacleArm_Draw_0.gml
+++ patched/gml_Object_o07_eBossX_TentacleArm_Draw_0.gml
@@ -6,12 +6,12 @@
 if (electric && scrIRandom(3) >= 1)
 {
-    draw_sprite(s07_eElecWeak, current_time div 20, x - 16, y - 12);
+    draw_sprite(s07_eElecWeak, global.pfo_current_time div 20, x - 16, y - 12);
 }
 if (electric && scrIRandom(3) >= 1)
 {
-    draw_sprite(s07_eElecWeak, current_time div 20, x - 16, y - 4);
+    draw_sprite(s07_eElecWeak, global.pfo_current_time div 20, x - 16, y - 4);
 }
 if (electric && scrIRandom(3) >= 1)
 {
-    draw_sprite(s07_eElec, (current_time div 100) + ((x / 16) % 4), x - 16, y - 8);
+    draw_sprite(s07_eElec, (global.pfo_current_time div 100) + ((x / 16) % 4), x - 16, y - 8);
 }
--- original/gml_Object_o07_eHeatRoom_Draw_0.gml
+++ patched/gml_Object_o07_eHeatRoom_Draw_0.gml
@@ -16,5 +16,5 @@
     if (o07__Game.heatAlpha > 0)
     {
-        draw_set_color(merge_color(c_red, c_yellow, 0.6 + (round(lengthdir_x(1, current_time / 5)) / 6)));
+        draw_set_color(merge_color(c_red, c_yellow, 0.6 + (round(lengthdir_x(1, global.pfo_current_time / 5)) / 6)));
         draw_set_alpha((0.19 * round(o07__Game.heatAlpha * 2)) / 2);
         gpu_set_blendmode(bm_add);
--- original/gml_Object_o07_zDoor_Draw_0.gml
+++ patched/gml_Object_o07_zDoor_Draw_0.gml
@@ -17,5 +17,5 @@
 if (place_meeting(x, y, o07_eHeatRoom))
 {
-    draw_sprite_ext(sprite_index, doorFrame, x, y, 1, 1, 0, merge_color(c_red, c_yellow, 0.3 + (round(lengthdir_x(1, current_time / 5)) / 6)), 1);
+    draw_sprite_ext(sprite_index, doorFrame, x, y, 1, 1, 0, merge_color(c_red, c_yellow, 0.3 + (round(lengthdir_x(1, global.pfo_current_time / 5)) / 6)), 1);
     gpu_set_blendmode(bm_add);
 }
--- original/gml_Object_o10_Player_Draw_0.gml
+++ patched/gml_Object_o10_Player_Draw_0.gml
@@ -37,5 +37,5 @@
             var _xx = lerp(_item.x, x, clamp((dropCount - 25) / 5, 0, 1)) - 8;
             var _yy = lerp(_item.y, y, clamp((dropCount - 25) / 5, 0, 1)) - 8;
-            if (i == 0 && ((current_time div 120) % 2) == 0)
+            if (i == 0 && ((global.pfo_current_time div 120) % 2) == 0)
             {
                 scrSpriteColored(s10_Items, _item.item_type, _xx, _yy, 1, 1, 16777215);
@@ -111,5 +111,5 @@
 if (missileDelay < missileDelayMax)
 {
-    if (missileDelay == 0 && ((current_time div 10) % 2) == 0)
+    if (missileDelay == 0 && ((global.pfo_current_time div 10) % 2) == 0)
     {
         sprite_index = s10_PlayerFlash;
--- original/gml_Object_o10_eBossShark_Step_0.gml
+++ patched/gml_Object_o10_eBossShark_Step_0.gml
@@ -216,5 +216,5 @@
         var target = o10_Player;
         xVel = approach(xVel, sign(target.x - x) * (abs(target.x - x) > 96 || place_meeting(x, y + sign(target.y - y), obj_floor) * -(abs(target.x - x) < 64)), 0.1);
-        yVel = approach(yVel, (sign(target.y - y) * (abs(target.y - y) > 16)) + lengthdir_x(0.5, current_time * 0.2), 0.1);
+        yVel = approach(yVel, (sign(target.y - y) * (abs(target.y - y) > 16)) + lengthdir_x(0.5, global.pfo_current_time * 0.2), 0.1);
         if (moveCount-- <= 0)
         {
--- original/gml_Object_o10_eHermitCrab_Step_0.gml
+++ patched/gml_Object_o10_eHermitCrab_Step_0.gml
@@ -83,5 +83,5 @@
     if (moveCount < 20)
     {
-        x = xstart + lengthdir_x(1, (current_time div 10) * 45);
+        x = xstart + lengthdir_x(1, (global.pfo_current_time div 10) * 45);
     }
     else
--- original/gml_Object_o10_eNautilus_Step_0.gml
+++ patched/gml_Object_o10_eNautilus_Step_0.gml
@@ -113,5 +113,5 @@
     }
     image_angle = 0;
-    hspeed = approach(hspeed, lengthdir_x(1, current_time * 0.1), 0.5);
+    hspeed = approach(hspeed, lengthdir_x(1, global.pfo_current_time * 0.1), 0.5);
     if (vspeed == 0)
     {
@@ -120,5 +120,5 @@
     else
     {
-        vspeed = approach(vspeed, sign(vspeed) + (sign(vspeed) * lengthdir_x(0.75, current_time * 0.2)), 0.1);
+        vspeed = approach(vspeed, sign(vspeed) + (sign(vspeed) * lengthdir_x(0.75, global.pfo_current_time * 0.2)), 0.1);
     }
     if (abs(o10_Player.y - y) > 64)
--- original/gml_Object_o10_eOcto_Draw_0.gml
+++ patched/gml_Object_o10_eOcto_Draw_0.gml
@@ -17,6 +17,6 @@
 if (sprite_index == s10_Octo || image_index < 3)
 {
-    draw_sprite_part_ext(s10_OctoTentacle, 0, 16, 0, 48, 16, x + 12, y + ((16 * image_yscale) + (8 * sign((((200 + current_time) div 400) % 2) - 0.5) * image_yscale)), 1, -sign((((200 + current_time) div 400) % 2) - 0.5) * image_yscale, global.palette[16], 1);
-    draw_sprite_part_ext(s10_OctoTentacle, 0, 16, 0, 48, 16, x - 12, y + ((16 * image_yscale) + (8 * -sign((((200 + current_time) div 400) % 2) - 0.5) * image_yscale)), -1, sign((((200 + current_time) div 400) % 2) - 0.5) * image_yscale, global.palette[16], 1);
+    draw_sprite_part_ext(s10_OctoTentacle, 0, 16, 0, 48, 16, x + 12, y + ((16 * image_yscale) + (8 * sign((((200 + global.pfo_current_time) div 400) % 2) - 0.5) * image_yscale)), 1, -sign((((200 + global.pfo_current_time) div 400) % 2) - 0.5) * image_yscale, global.palette[16], 1);
+    draw_sprite_part_ext(s10_OctoTentacle, 0, 16, 0, 48, 16, x - 12, y + ((16 * image_yscale) + (8 * -sign((((200 + global.pfo_current_time) div 400) % 2) - 0.5) * image_yscale)), -1, sign((((200 + global.pfo_current_time) div 400) % 2) - 0.5) * image_yscale, global.palette[16], 1);
 }
 if (blink > 0)
@@ -36,6 +36,6 @@
 if (sprite_index == s10_Octo || image_index < 3)
 {
-    draw_sprite_part_ext(s10_OctoTentacle, 0, 16, 0, 48, 16, round(x) + 10, y + ((16 * image_yscale) + (6 * -sign(((current_time div 400) % 2) - 0.5) * image_yscale)), 1, sign(((current_time div 400) % 2) - 0.5) * image_yscale, c_white, 1);
-    draw_sprite_part_ext(s10_OctoTentacle, 0, 16, 0, 48, 16, round(x) - 10, y + ((16 * image_yscale) + (6 * sign(((current_time div 400) % 2) - 0.5) * image_yscale)), -1, -sign(((current_time div 400) % 2) - 0.5) * image_yscale, c_white, 1);
+    draw_sprite_part_ext(s10_OctoTentacle, 0, 16, 0, 48, 16, round(x) + 10, y + ((16 * image_yscale) + (6 * -sign(((global.pfo_current_time div 400) % 2) - 0.5) * image_yscale)), 1, sign(((global.pfo_current_time div 400) % 2) - 0.5) * image_yscale, c_white, 1);
+    draw_sprite_part_ext(s10_OctoTentacle, 0, 16, 0, 48, 16, round(x) - 10, y + ((16 * image_yscale) + (6 * sign(((global.pfo_current_time div 400) % 2) - 0.5) * image_yscale)), -1, -sign(((global.pfo_current_time div 400) % 2) - 0.5) * image_yscale, c_white, 1);
 }
 if (shotCount < 20 && state == 1)
--- original/gml_Object_o14_Game_Create_0.gml
+++ patched/gml_Object_o14_Game_Create_0.gml
@@ -185,16 +185,5 @@
 eventType = 0;
 readyTimer = 100;
-sponsee[0] = -4;
-sponsee[1] = -4;
-sponsee[2] = -4;
-sponseeCost[0] = 0;
-sponseeCost[1] = 0;
-sponseeCost[2] = 0;
 numSponsees = 0;
-stable[0] = -1;
-stable[1] = -1;
-trainDelta[0] = 0;
-trainDelta[1] = 0;
-trainDelta[2] = 0;
 numPlayers = global.numPlayers;
 numHumanPlayers = numPlayers;
@@ -210,5 +199,9 @@
     ai[1] = false;
     ai[2] = true;
+    if (pfo_get_client_count() == numPlayers)
+    {
+        global.onlineSimultaneousTurns = true;
 }
+}
 else if (numPlayers == 3)
 {
@@ -239,4 +232,15 @@
     debt[i] = 0;
     result[i] = 0;
+    sponsee[i][0] = -4;
+    sponsee[i][1] = -4;
+    sponsee[i][2] = -4;
+    sponseeCost[i][0] = 0;
+    sponseeCost[i][1] = 0;
+    sponseeCost[i][2] = 0;
+    stable[i][0] = -4;
+    stable[i][1] = -4;
+    trainDelta[i][0] = 0;
+    trainDelta[i][1] = 0;
+    turnDone[i] = false;
 }
 portraitList = ds_list_create();
--- original/gml_Object_o14_Game_Draw_0.gml
+++ patched/gml_Object_o14_Game_Draw_0.gml
@@ -78,5 +78,5 @@
     draw_rectangle(viewx, viewy, viewx + 384, viewy + 216, 0);
 }
-else if (state == STATE_TURN_SETUP)
+else if (state == STATE_TURN_SETUP || scr14_OnlineHideInfo())
 {
     draw_set_color(c_black);
@@ -84,5 +84,5 @@
     if (currPlayer <= 2)
     {
-        if (!ai[currPlayer] && numHumanPlayers > 1)
+        if (!ai[currPlayer] && numHumanPlayers > 1 && !(numPlayers == 2 && global.onlineSimultaneousTurns && currPlayer == pfo_client_get_player_index()) && !(state == STATE_TURN_SETUP && numPlayers == 2 && !global.onlineSimultaneousTurns && pfo_is_online() && !scr14_OnlineHideInfo()))
         {
             if (portrait[currPlayer] == NUM_PORTRAITS)
@@ -106,5 +106,12 @@
                 scrSetFont(global.fontDefault);
                 draw_set_color(c_white);
+                if (numPlayers == 2 && (pfo_is_online() || global.onlineSimultaneousTurns))
+                {
+                    draw_text_centered(viewx + 192, viewy + 136, "WAITING FOR PLAYER TO FINISH TURN", 8);
+                }
+                else
+                {
                 scrStringDrawCenterAltExt(viewx + 192, viewy + 136, "look_away", 0, 0, 2);
+                }
                 var pname = scrString("p" + string(currPlayer + 1));
                 if (numPlayers == 3)
@@ -283,10 +290,10 @@
         }
     }
-    if (stable[0] == -4 && stable[1] == -4)
+    if (stable[currPlayer][0] == -4 && stable[currPlayer][1] == -4)
     {
         draw_set_color(global.palette[2]);
         draw_rectangle(672, 176, 703, 207, false);
     }
-    if ((stable[0] != -4 && stable[1] != -4) || numSponsees == 0)
+    if ((stable[currPlayer][0] != -4 && stable[currPlayer][1] != -4) || numSponsees == 0)
     {
         draw_set_color(global.palette[2]);
@@ -512,10 +519,10 @@
     if (sel != 3)
     {
-        if (sponsee[sel] != -4)
+        if (sponsee[currPlayer][sel] != -4)
         {
             draw_set_color(c_white);
-            draw_text(1288, 271 + global.jpvoff, string(sponsee[sel].name));
+            draw_text(1288, 271 + global.jpvoff, string(sponsee[currPlayer][sel].name));
             draw_set_color(global.palette[9]);
-            draw_text(1384, 271 + global.jpvoff, "$" + string(sponseeCost[sel]));
+            draw_text(1384, 271 + global.jpvoff, "$" + string(sponseeCost[currPlayer][sel]));
         }
     }
@@ -530,25 +537,25 @@
         for (var i = 0; i < 3; i++)
         {
-            if (sponsee[i] != -4)
+            if (sponsee[currPlayer][i] != -4)
             {
                 var qSprite = s14_qRun;
-                if (sponsee[i].body == BODY_BLUE)
+                if (sponsee[currPlayer][i].body == BODY_BLUE)
                 {
                     qSprite = s14_qRunBlueBody;
                 }
-                if (sponsee[i].body == BODY_YELLOW)
+                if (sponsee[currPlayer][i].body == BODY_YELLOW)
                 {
                     qSprite = s14_qRunYellowBody;
                 }
-                if (sponsee[i].body == BODY_WHITE)
+                if (sponsee[currPlayer][i].body == BODY_WHITE)
                 {
                     qSprite = s14_qRunWhiteBody;
                 }
                 draw_sprite(qSprite, 0, 1304 + (64 * i), 336);
-                if (sponsee[i].nose == NOSE_PURPLE)
+                if (sponsee[currPlayer][i].nose == NOSE_PURPLE)
                 {
                     draw_sprite(s14_qRunPurpleNose, 0, 1304 + (64 * i), 336);
                 }
-                if (sponsee[i].nose == NOSE_YELLOW)
+                if (sponsee[currPlayer][i].nose == NOSE_YELLOW)
                 {
                     draw_sprite(s14_qRunYellowNose, 0, 1304 + (64 * i), 336);
@@ -587,5 +594,5 @@
     draw_text_bg_centered(viewx + 56, viewy + 142, scrStringExt("trainer", 0, 10, 2), 0, 8, 8, true);
     draw_text_ce(1864, 271 + global.jpvoff, "$" + string(cash[currPlayer]), 1);
-    if (sel == 0 && !stable[1 - subsel].trained)
+    if (sel == 0 && !stable[currPlayer][1 - subsel].trained)
     {
         draw_set_color(global.palette[9]);
@@ -599,51 +606,51 @@
     for (var i = 1; i >= 0; i--)
     {
-        if (stable[i] != -4)
+        if (stable[currPlayer][i] != -4)
         {
             var qSprite = s14_qRun;
-            if (stable[i].body == BODY_BLUE)
+            if (stable[currPlayer][i].body == BODY_BLUE)
             {
                 qSprite = s14_qRunBlueBody;
             }
-            if (stable[i].body == BODY_YELLOW)
+            if (stable[currPlayer][i].body == BODY_YELLOW)
             {
                 qSprite = s14_qRunYellowBody;
             }
-            if (stable[i].body == BODY_WHITE)
+            if (stable[currPlayer][i].body == BODY_WHITE)
             {
                 qSprite = s14_qRunWhiteBody;
             }
-            var animFrame = floor(timer * (stable[i].qSpeed + trainDelta[i]) * 0.002) % 4;
-            if (!stable[i].trained)
+            var animFrame = floor(timer * (stable[currPlayer][i].qSpeed + trainDelta[currPlayer][i]) * 0.002) % 4;
+            if (!stable[currPlayer][i].trained)
             {
                 animFrame = 0;
             }
             draw_sprite(qSprite, animFrame, 1688, 328 - (40 * i));
-            if (stable[i].nose == NOSE_PURPLE)
+            if (stable[currPlayer][i].nose == NOSE_PURPLE)
             {
                 draw_sprite(s14_qRunPurpleNose, animFrame, 1688, 328 - (40 * i));
             }
-            if (stable[i].nose == NOSE_YELLOW)
+            if (stable[currPlayer][i].nose == NOSE_YELLOW)
             {
                 draw_sprite(s14_qRunYellowNose, animFrame, 1688, 328 - (40 * i));
             }
             draw_set_color(c_white);
-            draw_text(1704, 320 - (40 * i), string(stable[i].name));
+            draw_text(1704, 320 - (40 * i), string(stable[currPlayer][i].name));
             draw_set_color(global.palette[2]);
-            draw_text(1704, 328 - (40 * i), scrString("speed") + ": " + string(stable[i].qSpeed));
+            draw_text(1704, 328 - (40 * i), scrString("speed") + ": " + string(stable[currPlayer][i].qSpeed));
             var cs;
-            if (stable[i].clumsy > 8)
+            if (stable[currPlayer][i].clumsy > 8)
             {
                 cs = scrString("very_clumsy");
             }
-            else if (stable[i].clumsy > 6)
+            else if (stable[currPlayer][i].clumsy > 6)
             {
                 cs = scrString("clumsy");
             }
-            else if (stable[i].clumsy > 4)
+            else if (stable[currPlayer][i].clumsy > 4)
             {
                 cs = "";
             }
-            else if (stable[i].clumsy > 2)
+            else if (stable[currPlayer][i].clumsy > 2)
             {
                 cs = scrString("steady");
--- original/gml_Object_o14_Game_Other_11.gml
+++ patched/gml_Object_o14_Game_Other_11.gml
@@ -9,8 +9,15 @@
 for (var i = 0; i < 3; i++)
 {
+    bet[i] = 10;
+    qBet[i] = 0;
+    if (cash[i] < 10)
+    {
+        cash[i] = 10;
+    }
     if (debt[i] > 0)
     {
         debt[i] += ceil(debt[i] * (INTEREST_RATE / 100));
     }
+    turnDone[i] = false;
 }
 for (var i = 0; i < 3; i++)
@@ -68,3 +75,21 @@
 }
 newsEventToReport = eventType;
+for (var p = 0; p < 3; p++)
+{
+    scr14_GetStable(p);
+    for (var i = 0; i < 2; i++)
+    {
+        if (stable[p][i] != -4)
+        {
+            if (!stable[p][i].trained)
+            {
+                stable[p][i].qSpeed -= TRAIN_AMOUNT;
+            }
+            else
+            {
+                stable[p][i].trained = false;
+            }
+        }
+    }
+}
 scr14_ChangeState(STATE_TURN_SETUP);
--- original/gml_Object_o14_Game_Other_12.gml
+++ patched/gml_Object_o14_Game_Other_12.gml
@@ -2,8 +2,31 @@
 {
     substate = 1;
+    var onlineFirstHumanTurn = false;
+    var onlineOtherHumanTurn = false;
+    if (numPlayers == 2 && global.onlineSimultaneousTurns && currPlayer < numPlayers)
+    {
+        var playerIndex = !!pfo_client_get_player_index();
+        if (turnDone[0] && turnDone[1])
+        {
+            currPlayer = 2;
+        }
+        else if (!turnDone[playerIndex])
+        {
+            currPlayer = playerIndex;
+            onlineFirstHumanTurn = true;
+        }
+        else
+        {
+            currPlayer = !playerIndex;
+            onlineOtherHumanTurn = true;
+        }
+    }
+    else
+    {
     currPlayer++;
+    }
     if (currPlayer >= 3)
     {
-        if (numHumanPlayers <= 1)
+        if (numHumanPlayers <= 1 || (numPlayers == 2 && (global.onlineSimultaneousTurns || pfo_is_online())))
         {
             scr14_ChangeState(STATE_RACE_NEWS);
@@ -19,36 +42,83 @@
         visible = false;
     }
-    numSponsees = scr14_GetSponsee();
-    scr14_GetStable();
+    if (!onlineOtherHumanTurn)
+    {
+        var sponseeCandidates = ds_list_create();
+        for (var listPos = 0; listPos < NUM_QUIBBLES; listPos++)
+        {
+            var inCurrentRace = false;
+            if (ds_list_find_value(quibbles, listPos) == currQ[0] || ds_list_find_value(quibbles, listPos) == currQ[1] || ds_list_find_value(quibbles, listPos) == currQ[2])
+            {
+                inCurrentRace = true;
+            }
+            var alreadySponsored = false;
+            if (ds_list_find_value(quibbles, listPos).sponsor != -1)
+            {
+                alreadySponsored = true;
+            }
+            var inFutureRace = false;
+            for (var j = deckIndex; j < (roundsToWin * 3); j++)
+            {
+                if (ds_list_find_value(quibbleDeck, j) == ds_list_find_value(quibbles, listPos))
+                {
+                    inFutureRace = true;
+                }
+            }
+            if (!inCurrentRace && !alreadySponsored && inFutureRace)
+            {
+                ds_list_add(sponseeCandidates, ds_list_find_value(quibbles, listPos));
+            }
+        }
+        scrShuffle(sponseeCandidates);
+        if (onlineFirstHumanTurn)
+        {
+            var numPlayersNeedingSponsees = 2;
     for (var i = 0; i < 2; i++)
     {
-        if (stable[i] != -4)
+                if (stable[i][0] != -4 && stable[i][1] != -4)
         {
-            if (!stable[i].trained)
+                    numPlayersNeedingSponsees--;
+                }
+            }
+            var numSponseesPerPlayer = 3;
+            if (numPlayersNeedingSponsees == 2)
             {
-                stable[i].qSpeed -= TRAIN_AMOUNT;
+                numSponseesPerPlayer = min(3, floor(ds_list_size(sponseeCandidates) / 2));
             }
-            else
+            for (var i = 0; i < 2; i++)
             {
-                stable[i].trained = false;
+                var num = scr14_GetSponsee(i, sponseeCandidates, (stable[i][0] != -4 && stable[i][1] != -4) ? 0 : numSponseesPerPlayer);
+                if (i == currPlayer)
+                {
+                    numSponsees = num;
             }
         }
     }
+        else
+        {
+            numSponsees = scr14_GetSponsee(currPlayer, sponseeCandidates, 3);
+        }
+        ds_list_destroy(sponseeCandidates);
+    }
     thugActionTaken = false;
     loanActionTaken = false;
     infoActionTaken = false;
     sponsorActionTaken = false;
+    if (onlineFirstHumanTurn)
+    {
+        infoText = scrIRandomRange(1, 8);
+    }
+    else
+    {
     infoText = "";
+    }
     infoTextShort = "";
     infoQuibble = -1;
-    bet[currPlayer] = 10;
-    qBet[currPlayer] = 0;
-    if (cash[currPlayer] < 10)
-    {
-        cash[currPlayer] = 10;
-    }
     set_view(1536, 0);
+    if (!onlineOtherHumanTurn)
+    {
     scr14_SimulateRace();
 }
+}
 if (ai[currPlayer])
 {
@@ -56,5 +126,5 @@
     exit;
 }
-if (fire2pressed || numHumanPlayers == 1)
+if ((numPlayers == 2 && global.onlineSimultaneousTurns) ? (currPlayer == pfo_client_get_player_index()) : (fire2pressed || numHumanPlayers == 1 || (numPlayers == 2 && pfo_is_online())))
 {
     musicTrig = false;
--- original/gml_Object_o14_Game_Other_13.gml
+++ patched/gml_Object_o14_Game_Other_13.gml
@@ -2,5 +2,5 @@
 {
     substate = 1;
-    scrSfx(sfx_transition02, 10);
+    scr14_Sfx(sfx_transition02, 10);
     with (o14_Hand)
     {
@@ -11,5 +11,5 @@
         sel = 2;
         subsel = 0;
-        scrTransition(6, 32, 0, -1, 1);
+        scrTransition(scr14_OnlineHideInfo() ? -4 : 6, 32, 0, -1, 1);
     }
     else if (prevstate == STATE_TURN_INFO)
@@ -32,5 +32,5 @@
         sel = 1;
         subsel = 2;
-        if (stable[0] != -4 && stable[1] != -4)
+        if (stable[currPlayer][0] != -4 && stable[currPlayer][1] != -4)
         {
             sel = 2;
@@ -43,18 +43,13 @@
     }
     set_view(384, 0);
-    if (bet[currPlayer] > scr14_GetCash(currPlayer))
-    {
-        bet[currPlayer] = scr14_GetCash(currPlayer);
     }
-    if (bet[currPlayer] < 10)
-    {
-        bet[currPlayer] = 10;
-    }
-}
 if (scrTransitionDone())
 {
     if (!musicTrig)
     {
+        if (!scr14_OnlineHideInfo())
+        {
         scrBGM(bgm14_gameplay);
+        }
         musicTrig = true;
     }
@@ -64,4 +59,6 @@
     }
 }
+if (!pfo_send_command_in_progress())
+{
 var pPos = sel + (subsel * 10);
 if (pressLeft)
@@ -99,5 +96,5 @@
 repeat (2)
 {
-    if (stable[0] == -4 && stable[1] == -4)
+        if (stable[currPlayer][0] == -4 && stable[currPlayer][1] == -4)
     {
         if (sel == 2 && subsel == 2)
@@ -137,5 +134,5 @@
         }
     }
-    if ((stable[0] != -4 && stable[1] != -4) || numSponsees == 0)
+        if ((stable[currPlayer][0] != -4 && stable[currPlayer][1] != -4) || numSponsees == 0)
     {
         if (sel == 1 && subsel == 2)
@@ -179,4 +176,5 @@
 {
 }
+}
 if (fire2)
 {
@@ -197,57 +195,27 @@
     holdThreshold = 16;
 }
+if (!pfo_send_command_in_progress() || (sel == 1 && subsel == 0) || (sel == 2 && subsel == 0))
+{
 if (fire2pressed)
 {
     if (sel == 0)
     {
-        if (qBet[currPlayer] != subsel)
-        {
-            scrSfx(sfx_navi04, 30);
-            qBet[currPlayer] = subsel;
+            pfo_send_command(UnknownEnum.Value_16, int64(subsel), global.onlineSimultaneousTurns ? currPlayer : -1);
         }
-    }
     else if (sel == 1 && subsel == 0)
     {
-        if (bet[currPlayer] > 10)
-        {
-            scrSfx(sfx_select06, 25);
-            bet[currPlayer] -= 10;
-            if (bet[currPlayer] < 10)
-            {
-                bet[currPlayer] = 10;
+            pfo_send_command(UnknownEnum.Value_17, UnknownEnum.Value_0, global.onlineSimultaneousTurns ? currPlayer : -1);
             }
-        }
-    }
     else if (sel == 2 && subsel == 0)
     {
-        if (bet[currPlayer] < scr14_GetCash(currPlayer) && bet[currPlayer] < maxBet)
-        {
-            scrSfx(sfx_select06, 25);
-            bet[currPlayer] += 10;
-            if (bet[currPlayer] > scr14_GetCash(currPlayer))
-            {
-                bet[currPlayer] = scr14_GetCash(currPlayer);
+            pfo_send_command(UnknownEnum.Value_18, UnknownEnum.Value_0, global.onlineSimultaneousTurns ? currPlayer : -1);
             }
-            if (bet[currPlayer] > maxBet)
-            {
-                bet[currPlayer] = maxBet;
-            }
-        }
-    }
     else if (sel == 3 && subsel == 0)
     {
-        if (bet[currPlayer] < scr14_GetCash(currPlayer) && bet[currPlayer] < maxBet)
-        {
-            scrSfx(sfx_select06, 25);
-            bet[currPlayer] = maxBet;
-            if (bet[currPlayer] > scr14_GetCash(currPlayer))
-            {
-                bet[currPlayer] = scr14_GetCash(currPlayer);
+            pfo_send_command(UnknownEnum.Value_19, UnknownEnum.Value_0, global.onlineSimultaneousTurns ? currPlayer : -1);
             }
-        }
-    }
     else if (sel == 1 && subsel == 1)
     {
-        scrSfx(soundSelect, 50);
+            scr14_Sfx(soundSelect, 50);
         scr14_ChangeState(STATE_TURN_INFO);
         exit;
@@ -255,5 +223,5 @@
     else if (sel == 2 && subsel == 1)
     {
-        scrSfx(soundSelect, 50);
+            scr14_Sfx(soundSelect, 50);
         scr14_ChangeState(STATE_TURN_THUG);
         exit;
@@ -261,5 +229,5 @@
     else if (sel == 3 && subsel == 1)
     {
-        scrSfx(soundSelect, 50);
+            scr14_Sfx(soundSelect, 50);
         scr14_ChangeState(STATE_TURN_LOAN_SHARK);
         exit;
@@ -267,5 +235,5 @@
     else if (sel == 1 && subsel == 2)
     {
-        scrSfx(soundSelect, 50);
+            scr14_Sfx(soundSelect, 50);
         scr14_ChangeState(STATE_TURN_SPONSOR);
         exit;
@@ -273,5 +241,5 @@
     else if (sel == 2 && subsel == 2)
     {
-        scrSfx(soundSelect, 50);
+            scr14_Sfx(soundSelect, 50);
         scr14_ChangeState(STATE_TURN_STABLE);
         exit;
@@ -279,9 +247,8 @@
     else if (sel == 3 && subsel == 2)
     {
-        muteBGM();
-        scr14_ChangeState(STATE_TURN_SETUP);
-        exit;
+            pfo_send_command(UnknownEnum.Value_20, UnknownEnum.Value_0, global.onlineSimultaneousTurns ? currPlayer : -1);
     }
 }
+}
 if (!instance_exists(o14_Hand))
 {
@@ -299,3 +266,13 @@
     o14_Hand.x = 608 + (48 * (sel - 1));
     o14_Hand.y = 120 + (32 * subsel);
+}
+
+enum UnknownEnum
+{
+    Value_0,
+    Value_16 = 16,
+    Value_17,
+    Value_18,
+    Value_19,
+    Value_20
 }
--- original/gml_Object_o14_Game_Other_14.gml
+++ patched/gml_Object_o14_Game_Other_14.gml
@@ -26,8 +26,10 @@
     if (tSpeak-- <= 0)
     {
-        scrSfx(sfx_speak02a, 70);
+        scr14_Sfx(sfx_speak02a, 70);
         tSpeak = -1;
     }
 }
+if (!pfo_send_command_in_progress())
+{
 var pPos = sel;
 if (!loanActionTaken)
@@ -77,5 +79,5 @@
 if (fire1pressed)
 {
-    scrSfx(sfx_select08, 40);
+        scr14_Sfx(sfx_select08, 40);
     scr14_ChangeState(STATE_TURN_MAIN);
     exit;
@@ -85,49 +87,14 @@
     if (sel == 0 && !loanActionTaken)
     {
-        if (debt[currPlayer] > 0 && debt[currPlayer] <= scr14_GetCash(currPlayer))
-        {
-            if (debt[currPlayer] == 0)
-            {
+            pfo_send_command(UnknownEnum.Value_21, UnknownEnum.Value_0, global.onlineSimultaneousTurns ? currPlayer : -1);
             }
-            else if (scr14_GetCash(currPlayer) < debt[currPlayer])
-            {
-            }
-            else
-            {
-                scrSfx(sfx_speak02b, 71);
-                scr14_ChangeCash(currPlayer, -debt[currPlayer]);
-                debt[currPlayer] = 0;
-                sel = 1;
-                scr14_MessageSet(scrString("loan_shark_paid_off"));
-            }
-        }
-        else
-        {
-            scrSfx(sfx_speak02b, 71);
-            scr14_ChangeCash(currPlayer, min(1000, maxBet));
-            debt[currPlayer] += min(1000, maxBet);
-            loanActionTaken = true;
-            sel = 1;
-            if (debt[currPlayer] >= 1000)
-            {
-                scr14_MessageSet(scrString("loan_shark_get_loan_1"));
-            }
-            else if (debt[currPlayer] >= 500)
-            {
-                scr14_MessageSet(scrString("loan_shark_get_loan_2"));
-            }
-            else
-            {
-                scr14_MessageSet(scrString("loan_shark_get_loan_3"));
-            }
-        }
-    }
     else if (sel == 1)
     {
-        scrSfx(sfx_select08, 40);
+            scr14_Sfx(sfx_select08, 40);
         scr14_ChangeState(STATE_TURN_MAIN);
         exit;
     }
 }
+}
 if (!instance_exists(o14_Hand))
 {
@@ -145,3 +112,9 @@
     o14_Hand.x = 320;
     o14_Hand.y = 352;
+}
+
+enum UnknownEnum
+{
+    Value_0,
+    Value_21 = 21
 }
--- original/gml_Object_o14_Game_Other_15.gml
+++ patched/gml_Object_o14_Game_Other_15.gml
@@ -24,5 +24,5 @@
         if (tSpeak-- <= 0)
         {
-            scrSfx(sfx_speak01a, 70);
+            scr14_Sfx(sfx_speak01a, 70);
             tSpeak = -1;
         }
@@ -128,5 +128,5 @@
     if (fire1pressed)
     {
-        scrSfx(sfx_select08, 40);
+        scr14_Sfx(sfx_select08, 40);
         scr14_ChangeState(STATE_TURN_MAIN);
         exit;
@@ -138,10 +138,10 @@
             if (thugActionID == THUG_POISON && poolIndex >= (ds_list_size(generalPool) - 2))
             {
-                scrSfx(soundInactive, 55);
+                scr14_Sfx(soundInactive, 55);
                 scr14_MessageSet(scrString("thug_poison_unavail_2"));
             }
             else if (scr14_GetCash(currPlayer) >= thugActionCost)
             {
-                scrSfx(sfx_navi01, 30);
+                scr14_Sfx(sfx_navi01, 30);
                 if (thugActionID == THUG_ROIDS)
                 {
@@ -174,5 +174,5 @@
             else
             {
-                scrSfx(soundInactive, 55);
+                scr14_Sfx(soundInactive, 55);
                 scr14_MessageSet(scrString("thug_low_cash"));
             }
@@ -180,5 +180,5 @@
         else
         {
-            scrSfx(sfx_select08, 40);
+            scr14_Sfx(sfx_select08, 40);
             scr14_ChangeState(STATE_TURN_MAIN);
             exit;
@@ -204,4 +204,6 @@
 else if (substate == 2)
 {
+    if (!pfo_send_command_in_progress())
+    {
     var pPos = subsel;
     if (pressUp)
@@ -223,5 +225,5 @@
     if (pPos != subsel)
     {
-        scrSfx(sfx_navi01, 25);
+            scr14_Sfx(sfx_navi01, 25);
     }
     if (fire2pressed)
@@ -229,5 +231,5 @@
         if (subsel == 3)
         {
-            scrSfx(sfx_deselect01, 30);
+                scr14_Sfx(sfx_deselect01, 30);
             sel = 3;
             subsel = 1;
@@ -237,14 +239,8 @@
         else
         {
-            scrSfx(sfx_speak01b, 71);
-            thugActionTaken = true;
-            scr14_ChangeCash(currPlayer, -thugActionCost);
-            scr14_AddThugAction(currPlayer, thugActionID, subsel);
-            sel = 3;
-            subsel = 1;
-            substate = 1;
-            scr14_MessageSet(scrString("thug_finalize_purchase"));
+                pfo_send_command(UnknownEnum.Value_22, (int64(thugActionID) << UnknownEnum.Value_2) | (int64(subsel) & UnknownEnum.Value_3), global.onlineSimultaneousTurns ? currPlayer : -1);
         }
     }
+    }
     if (!instance_exists(o14_Hand))
     {
@@ -272,3 +268,10 @@
         visible = true;
     }
+}
+
+enum UnknownEnum
+{
+    Value_2 = 2,
+    Value_3,
+    Value_22 = 22
 }
--- original/gml_Object_o14_Game_Other_16.gml
+++ patched/gml_Object_o14_Game_Other_16.gml
@@ -6,8 +6,8 @@
     sel = 0;
     subsel = 1;
-    if (infoText == "")
+    if (infoText == "" || is_real(infoText))
     {
         scr14_MessageSet(scrString("info_got_a_tip"));
-        var r = scrIRandomRange(1, 8);
+        var r = is_real(infoText) ? infoText : scrIRandomRange(1, 8);
         infoText = scrString("tip") + ":" + global.CARRIAGE_RETURN + global.CARRIAGE_RETURN;
         infoText += scrString("tip_" + string(r));
@@ -24,5 +24,5 @@
         if (tSpeak-- <= 0)
         {
-            scrSfx(sfx_speak00a, 70);
+            scr14_Sfx(sfx_speak00a, 70);
             tSpeak = -1;
         }
@@ -68,5 +68,5 @@
     if (fire1pressed)
     {
-        scrSfx(sfx_select08, 40);
+        scr14_Sfx(sfx_select08, 40);
         scr14_ChangeState(STATE_TURN_MAIN);
         exit;
@@ -78,5 +78,5 @@
             if (scr14_GetCash(currPlayer) >= INFO_COST)
             {
-                scrSfx(sfx_navi01, 30);
+                scr14_Sfx(sfx_navi01, 30);
                 scrMessageSet(scrString("info_buy_2"));
                 scrMessageInsertBreaks(256);
@@ -85,5 +85,5 @@
             else
             {
-                scrSfx(soundInactive, 55);
+                scr14_Sfx(soundInactive, 55);
                 scr14_MessageSet(scrString("info_low_cash"));
             }
@@ -91,5 +91,5 @@
         if (subsel == 1)
         {
-            scrSfx(sfx_select08, 40);
+            scr14_Sfx(sfx_select08, 40);
             scr14_ChangeState(STATE_TURN_MAIN);
             exit;
@@ -115,4 +115,6 @@
 else if (substate == 2)
 {
+    if (!pfo_send_command_in_progress())
+    {
     var pPos = subsel;
     if (pressUp)
@@ -134,5 +136,5 @@
     if (pPos != subsel)
     {
-        scrSfx(sfx_navi01, 25);
+            scr14_Sfx(sfx_navi01, 25);
     }
     if (fire2pressed)
@@ -140,5 +142,5 @@
         if (subsel == 3)
         {
-            scrSfx(sfx_deselect01, 30);
+                scr14_Sfx(sfx_deselect01, 30);
             sel = 3;
             subsel = 1;
@@ -146,92 +148,10 @@
             scr14_MessageSet(scrString("info_cancel"));
         }
-        else if (scr14_GetCash(currPlayer) >= INFO_COST)
-        {
-            scrSfx(sfx_speak00b, 71);
-            infoActionTaken = true;
-            infoQuibble = subsel;
-            var clumsyStr;
-            if (currQ[infoQuibble].clumsy > 8)
-            {
-                clumsyStr = scrString("very_clumsy");
-            }
-            else if (currQ[infoQuibble].clumsy > 6)
-            {
-                clumsyStr = scrString("clumsy");
-            }
-            else if (currQ[infoQuibble].clumsy > 4)
-            {
-                clumsyStr = "";
-            }
-            else if (currQ[infoQuibble].clumsy > 2)
-            {
-                clumsyStr = scrString("steady");
-            }
             else
             {
-                clumsyStr = scrString("very_steady");
+                pfo_send_command(UnknownEnum.Value_23, int64(subsel), global.onlineSimultaneousTurns ? currPlayer : -1);
             }
-            var speedStr;
-            if (infoQuibble == scr14_GetRanking(RANK_FASTEST))
-            {
-                speedStr = scrString("fastest");
             }
-            else if (infoQuibble == scr14_GetRanking(RANK_SECOND_FASTEST))
-            {
-                speedStr = scrString("second_fastest");
             }
-            else if (infoQuibble == scr14_GetRanking(RANK_SLOWEST))
-            {
-                speedStr = scrString("slowest");
-            }
-            infoText = currQ[infoQuibble].name + ":" + global.CARRIAGE_RETURN + global.CARRIAGE_RETURN + speedStr + " " + scrString("quibble_in_race") + ".";
-            if (clumsyStr != "")
-            {
-                infoText += (global.CARRIAGE_RETURN + global.CARRIAGE_RETURN + clumsyStr + ".");
-            }
-            if (eventType)
-            {
-                if (eventType == EVENT_METEOR)
-                {
-                    infoText += (global.CARRIAGE_RETURN + global.CARRIAGE_RETURN + scrString("tip_meteor"));
-                }
-                if (eventType == EVENT_POLLUTION)
-                {
-                    infoText += (global.CARRIAGE_RETURN + global.CARRIAGE_RETURN + scrString("tip_pollution"));
-                }
-                if (eventType == EVENT_GARBAGE)
-                {
-                    infoText += (global.CARRIAGE_RETURN + global.CARRIAGE_RETURN + scrString("tip_garbage"));
-                }
-            }
-            if (speedStr == scrString("second_fastest"))
-            {
-                speedStr = scrStringExt("second_fastest_abbrev", 0, 8, 0);
-            }
-            if (clumsyStr == scrString("very_clumsy"))
-            {
-                clumsyStr = scrStringExt("very_clumsy_abbrev", 0, 8, 0);
-            }
-            if (clumsyStr == scrString("very_steady"))
-            {
-                clumsyStr = scrStringExt("very_steady_abbrev", 0, 8, 0);
-            }
-            infoTextShort = speedStr;
-            if (clumsyStr != "")
-            {
-                infoTextShort += ("," + clumsyStr);
-            }
-            sel = 0;
-            subsel = 1;
-            substate = 1;
-            scr14_ChangeCash(currPlayer, -INFO_COST);
-            scr14_MessageSet(scrString("info_buy_3"));
-        }
-        else
-        {
-            scrSfx(soundInactive, 55);
-            scr14_MessageSet(scrString("info_low_cash"));
-        }
-    }
     if (!instance_exists(o14_Hand))
     {
@@ -245,3 +165,8 @@
         o14_Hand.y += 16;
     }
+}
+
+enum UnknownEnum
+{
+    Value_23 = 23
 }
--- original/gml_Object_o14_Game_Other_17.gml
+++ patched/gml_Object_o14_Game_Other_17.gml
@@ -2,5 +2,5 @@
 for (var i = 0; i < 2; i++)
 {
-    if (stable[i] != -4)
+    if (stable[currPlayer][i] != -4)
     {
         numStable++;
@@ -37,8 +37,10 @@
         if (tSpeak-- <= 0)
         {
-            scrSfx(sfx_speak03a, 70);
+            scr14_Sfx(sfx_speak03a, 70);
             tSpeak = -1;
         }
     }
+    if (!pfo_send_command_in_progress())
+    {
     var pPos = sel;
     do
@@ -61,5 +63,5 @@
         }
     }
-    until (sel == 3 || sponsee[sel] != -4);
+        until (sel == 3 || sponsee[currPlayer][sel] != -4);
     if (numStable == 3)
     {
@@ -77,5 +79,5 @@
         if (sel != 3)
         {
-            var msg = scrStringVal("breeder_has_won", sponsee[sel].name, sponsee[sel].wins, sponsee[sel].races);
+                var msg = scrStringVal("breeder_has_won", sponsee[currPlayer][sel].name, sponsee[currPlayer][sel].wins, sponsee[currPlayer][sel].races);
             scr14_MessageSet(msg);
         }
@@ -91,5 +93,5 @@
     if (fire1pressed)
     {
-        scrSfx(sfx_select08, 40);
+            scr14_Sfx(sfx_select08, 40);
         scr14_ChangeState(STATE_TURN_MAIN);
         exit;
@@ -99,28 +101,14 @@
         if (sel != 3)
         {
-            if (scr14_GetCash(currPlayer) >= sponseeCost[sel])
-            {
-                scrSfx(sfx_speak03b, 71);
-                scr14_MessageSet(scrString("breeder_bye"));
-                scr14_ChangeCash(currPlayer, -sponseeCost[sel]);
-                sponsee[sel].sponsor = currPlayer;
-                sponsee[sel] = -4;
-                scr14_GetStable();
-                sponsorActionTaken = true;
-                sel = 3;
+                pfo_send_command(UnknownEnum.Value_24, int64(sel), global.onlineSimultaneousTurns ? currPlayer : -1);
             }
             else
             {
-                scrSfx(soundInactive, 55);
-                scr14_MessageSet(scrString("breeder_low_cash"));
-            }
-        }
-        else
-        {
-            scrSfx(sfx_select08, 40);
+                scr14_Sfx(sfx_select08, 40);
             scr14_ChangeState(STATE_TURN_MAIN);
             exit;
         }
     }
+    }
     if (!instance_exists(o14_Hand))
     {
@@ -139,3 +127,8 @@
         o14_Hand.y = 352;
     }
+}
+
+enum UnknownEnum
+{
+    Value_24 = 24
 }
--- original/gml_Object_o14_Game_Other_18.gml
+++ patched/gml_Object_o14_Game_Other_18.gml
@@ -12,5 +12,5 @@
     if (tSpeak-- <= 0)
     {
-        scrSfx(sfx_speak04a, 70);
+        scr14_Sfx(sfx_speak04a, 70);
         tSpeak = -1;
     }
@@ -19,4 +19,6 @@
 var oldSel = sel;
 var oldSubsel = subsel;
+if (!pfo_send_command_in_progress())
+{
 if (pressLeft)
 {
@@ -58,5 +60,5 @@
         }
     }
-    until (subsel == 1 || stable[1 - subsel] != -4);
+        until (subsel == 1 || stable[currPlayer][1 - subsel] != -4);
 }
 if (sel == 1)
@@ -68,5 +70,5 @@
     }
 }
-if (stable[0] == -4 && stable[1] == -4)
+    if (stable[currPlayer][0] == -4 && stable[currPlayer][1] == -4)
 {
     sel = 1;
@@ -76,9 +78,10 @@
 {
 }
+}
 var selQ = 1 - subsel;
 trainCost = TRAIN_COST;
-if ((stable[selQ].qSpeed + TRAIN_AMOUNT) > SPEED_UPPER)
+if ((stable[currPlayer][selQ].qSpeed + TRAIN_AMOUNT) > SPEED_UPPER)
 {
-    trainCost += (((stable[selQ].qSpeed + TRAIN_AMOUNT) - SPEED_UPPER) * TRAIN_COST);
+    trainCost += (((stable[currPlayer][selQ].qSpeed + TRAIN_AMOUNT) - SPEED_UPPER) * TRAIN_COST);
 }
 if (sel != oldSel || subsel != oldSubsel)
@@ -86,15 +89,15 @@
     if (sel != 1)
     {
-        if (stableRetired[selQ])
+        if (stableRetired[currPlayer][selQ])
         {
             scr14_MessageSet(scrString("trainer_no_upcoming_races"));
         }
-        else if (!stable[selQ].trained)
+        else if (!stable[currPlayer][selQ].trained)
         {
-            scr14_MessageSet(scrStringExt("trainer_select_1", stable[selQ].name, 0, 0));
+            scr14_MessageSet(scrStringExt("trainer_select_1", stable[currPlayer][selQ].name, 0, 0));
         }
         else
         {
-            scr14_MessageSet(scrStringExt("trainer_select_2", stable[selQ].name, 0, 0));
+            scr14_MessageSet(scrStringExt("trainer_select_2", stable[currPlayer][selQ].name, 0, 0));
         }
     }
@@ -104,7 +107,7 @@
         for (var i = 0; i < 2; i++)
         {
-            if (stable[i] != -4)
+            if (stable[currPlayer][i] != -4)
             {
-                if (!stable[i].trained)
+                if (!stable[currPlayer][i].trained)
                 {
                     scr14_MessageSet(scrString("trainer_warning"));
@@ -114,7 +117,9 @@
     }
 }
+if (!pfo_send_command_in_progress())
+{
 if (fire1pressed)
 {
-    scrSfx(sfx_select08, 40);
+        scr14_Sfx(sfx_select08, 40);
     scr14_ChangeState(STATE_TURN_MAIN);
     exit;
@@ -124,42 +129,14 @@
     if (sel == 0)
     {
-        if (stableRetired[selQ])
-        {
-            scr14_MessageSet(scrString("trainer_retired"));
+            pfo_send_command(UnknownEnum.Value_25, int64(selQ), global.onlineSimultaneousTurns ? currPlayer : -1);
         }
-        else if (scr14_GetCash(currPlayer) >= trainCost && !stable[selQ].trained)
-        {
-            scrSfx(sfx_speak04b, 71);
-            var r = scrIRandomRange(1, 2);
-            if (r == 1)
-            {
-                scr14_MessageSet(scrString("trainer_train_1"));
-            }
             else
             {
-                scr14_MessageSet(scrString("trainer_train_2"));
-            }
-            scr14_ChangeCash(currPlayer, -trainCost);
-            trainDelta[selQ] = TRAIN_AMOUNT;
-            stable[selQ].trained = true;
-        }
-        else if (stable[selQ].trained)
-        {
-            scrSfx(soundInactive, 55);
-            scr14_MessageSet(scrStringExt("trainer_already_trained", stable[selQ].name, 0, 0));
-        }
-        else
-        {
-            scrSfx(soundInactive, 55);
-            scr14_MessageSet(scrString("trainer_low_cash"));
-        }
-    }
-    else
-    {
-        scrSfx(sfx_select08, 40);
+            scr14_Sfx(sfx_select08, 40);
         scr14_ChangeState(STATE_TURN_MAIN);
         exit;
     }
 }
+}
 if (!instance_exists(o14_Hand))
 {
@@ -177,3 +154,8 @@
     o14_Hand.x = 1856;
     o14_Hand.y = 352;
+}
+
+enum UnknownEnum
+{
+    Value_25 = 25
 }
--- original/gml_Object_o14_Game_Other_19.gml
+++ patched/gml_Object_o14_Game_Other_19.gml
@@ -17,30 +17,31 @@
     }
     var i = rand(0, 2);
-    if (cash[n] >= 100 && (cash[n] - 100) > maxBet && (stable[0] == -4 || stable[1] == -4))
+    if (cash[n] >= 100 && (cash[n] - 100) > maxBet && (stable[n][0] == -4 || stable[n][1] == -4))
     {
-        if (sponsee[i] != -4)
+        if (sponsee[n][i] != -4)
         {
             cash[n] -= 100;
-            sponsee[i].sponsor = n;
+            sponsee[n][i].sponsor = n;
+            stable[n][(stable[n][0] == -4) ? 0 : 1] = sponsee[n][i];
             if (scrIRandomRange(1, 3) == 1)
             {
                 aiTrain[n][i] = false;
             }
-            scr14_GetStable();
+            scr14_GetStable(n);
         }
     }
     for (i = 0; i < 2; i++)
     {
-        if (stable[i] != -4 && aiTrain[n][i])
+        if (stable[n][i] != -4 && aiTrain[n][i])
         {
             trainCost = TRAIN_COST;
-            if ((stable[i].qSpeed + TRAIN_AMOUNT) > SPEED_UPPER)
+            if ((stable[n][i].qSpeed + TRAIN_AMOUNT) > SPEED_UPPER)
             {
-                trainCost += (((stable[i].qSpeed + TRAIN_AMOUNT) - SPEED_UPPER) * TRAIN_COST);
+                trainCost += (((stable[n][i].qSpeed + TRAIN_AMOUNT) - SPEED_UPPER) * TRAIN_COST);
             }
-            if (cash[n] >= trainCost && (cash[n] - trainCost) > 300 && stable[i].qSpeed < 75)
+            if (cash[n] >= trainCost && (cash[n] - trainCost) > 300 && stable[n][i].qSpeed < 75)
             {
                 cash[n] -= trainCost;
-                stable[i].qSpeed += TRAIN_AMOUNT;
+                stable[n][i].qSpeed += TRAIN_AMOUNT;
             }
         }
--- original/gml_Object_o14_Game_Step_0.gml
+++ patched/gml_Object_o14_Game_Step_0.gml
@@ -14,6 +14,17 @@
     else if (state == STATE_TURN_AI || state == STATE_RACE_NEWS || state == STATE_RACE || state == STATE_RACE_RESULTS || state == STATE_GAME_END || state == STATE_GAME_RESULTS || state == STATE_RACE_LOOK_AT_SCREEN)
     {
+        if (numPlayers == 2 && global.onlineSimultaneousTurns)
+        {
+            scrGetInputMerged();
+        }
+        else
+        {
         scrGetInput(0);
     }
+    }
+    else if (numPlayers == 2 && global.onlineSimultaneousTurns)
+    {
+        scrGetInput(0, UnknownEnum.Value_1);
+    }
     else if (numPlayers == 1 || numPlayers == 3 || (numPlayers == 2 && currPlayer == 0))
     {
@@ -85,4 +96,313 @@
         break;
 }
+
+function scr14_CurrentPlayerIsLocal()
+{
+    return numPlayers != 2 || !global.onlineSimultaneousTurns || currPlayer == pfo_client_get_player_index();
+}
+
+var savedCurrPlayer = currPlayer;
+var repeatCount = 1;
+var commandPlayer = -1;
+if (global.onlineSimultaneousTurns)
+{
+    repeatCount = 2;
+    currPlayer = 0;
+    commandPlayer = 0;
+}
+repeat (repeatCount)
+{
+    var param = [UnknownEnum.Value_0];
+    if (pfo_receive_command(UnknownEnum.Value_16, param, commandPlayer))
+    {
+        var _subsel = real(param[0]);
+        if (qBet[currPlayer] != _subsel)
+        {
+            scr14_Sfx(sfx_navi04, 30);
+            qBet[currPlayer] = _subsel;
+        }
+    }
+    else if (pfo_receive_command(UnknownEnum.Value_17, 0, commandPlayer))
+    {
+        if (bet[currPlayer] > 10)
+        {
+            scr14_Sfx(sfx_select06, 25);
+            bet[currPlayer] -= 10;
+            if (bet[currPlayer] < 10)
+            {
+                bet[currPlayer] = 10;
+            }
+        }
+    }
+    else if (pfo_receive_command(UnknownEnum.Value_18, 0, commandPlayer))
+    {
+        if (bet[currPlayer] < scr14_GetCash(currPlayer) && bet[currPlayer] < maxBet)
+        {
+            scr14_Sfx(sfx_select06, 25);
+            bet[currPlayer] += 10;
+            if (bet[currPlayer] > scr14_GetCash(currPlayer))
+            {
+                bet[currPlayer] = scr14_GetCash(currPlayer);
+            }
+            if (bet[currPlayer] > maxBet)
+            {
+                bet[currPlayer] = maxBet;
+            }
+        }
+    }
+    else if (pfo_receive_command(UnknownEnum.Value_19, 0, commandPlayer))
+    {
+        if (bet[currPlayer] < scr14_GetCash(currPlayer) && bet[currPlayer] < maxBet)
+        {
+            scr14_Sfx(sfx_select06, 25);
+            bet[currPlayer] = maxBet;
+            if (bet[currPlayer] > scr14_GetCash(currPlayer))
+            {
+                bet[currPlayer] = scr14_GetCash(currPlayer);
+            }
+        }
+    }
+    else if (pfo_receive_command(UnknownEnum.Value_20, 0, commandPlayer))
+    {
+        turnDone[currPlayer] = true;
+        if (numPlayers != 2 || !global.onlineSimultaneousTurns || turnDone[!!pfo_client_get_player_index()])
+        {
+            if (state == STATE_TURN_SETUP)
+            {
+                substate = 0;
+            }
+            else
+            {
+                muteBGM();
+                scr14_ChangeState(STATE_TURN_SETUP);
+            }
+        }
+    }
+    else if (pfo_receive_command(UnknownEnum.Value_21, 0, commandPlayer))
+    {
+        if (debt[currPlayer] > 0 && debt[currPlayer] <= scr14_GetCash(currPlayer))
+        {
+            if (debt[currPlayer] == 0)
+            {
+            }
+            else if (scr14_GetCash(currPlayer) < debt[currPlayer])
+            {
+            }
+            else
+            {
+                scr14_Sfx(sfx_speak02b, 71);
+                scr14_ChangeCash(currPlayer, -debt[currPlayer]);
+                debt[currPlayer] = 0;
+                if (scr14_CurrentPlayerIsLocal())
+                {
+                    sel = 1;
+                    scr14_MessageSet(scrString("loan_shark_paid_off"));
+                }
+            }
+        }
+        else
+        {
+            scr14_Sfx(sfx_speak02b, 71);
+            scr14_ChangeCash(currPlayer, min(1000, maxBet));
+            debt[currPlayer] += min(1000, maxBet);
+            if (scr14_CurrentPlayerIsLocal())
+            {
+                loanActionTaken = true;
+                sel = 1;
+                if (debt[currPlayer] >= 1000)
+                {
+                    scr14_MessageSet(scrString("loan_shark_get_loan_1"));
+                }
+                else if (debt[currPlayer] >= 500)
+                {
+                    scr14_MessageSet(scrString("loan_shark_get_loan_2"));
+                }
+                else
+                {
+                    scr14_MessageSet(scrString("loan_shark_get_loan_3"));
+                }
+            }
+        }
+    }
+    else if (pfo_receive_command(UnknownEnum.Value_22, param, commandPlayer))
+    {
+        var _thugActionID = real(param[0] >> UnknownEnum.Value_2);
+        var _subsel = real(param[0] & UnknownEnum.Value_3);
+        var _thugActionCost = COST[_thugActionID];
+        scr14_Sfx(sfx_speak01b, 71);
+        if (scr14_CurrentPlayerIsLocal())
+        {
+            thugActionTaken = true;
+        }
+        scr14_ChangeCash(currPlayer, -_thugActionCost);
+        scr14_AddThugAction(currPlayer, _thugActionID, _subsel);
+        if (scr14_CurrentPlayerIsLocal())
+        {
+            sel = 3;
+            subsel = 1;
+            substate = 1;
+            scr14_MessageSet(scrString("thug_finalize_purchase"));
+        }
+    }
+    else if (pfo_receive_command(UnknownEnum.Value_23, param, commandPlayer))
+    {
+        var _subsel = real(param[0]);
+        if (scr14_GetCash(currPlayer) >= INFO_COST)
+        {
+            scr14_Sfx(sfx_speak00b, 71);
+            if (scr14_CurrentPlayerIsLocal())
+            {
+                infoActionTaken = true;
+                infoQuibble = _subsel;
+                var clumsyStr;
+                if (currQ[infoQuibble].clumsy > 8)
+                {
+                    clumsyStr = scrString("very_clumsy");
+                }
+                else if (currQ[infoQuibble].clumsy > 6)
+                {
+                    clumsyStr = scrString("clumsy");
+                }
+                else if (currQ[infoQuibble].clumsy > 4)
+                {
+                    clumsyStr = "";
+                }
+                else if (currQ[infoQuibble].clumsy > 2)
+                {
+                    clumsyStr = scrString("steady");
+                }
+                else
+                {
+                    clumsyStr = scrString("very_steady");
+                }
+                var speedStr;
+                if (infoQuibble == scr14_GetRanking(RANK_FASTEST))
+                {
+                    speedStr = scrString("fastest");
+                }
+                else if (infoQuibble == scr14_GetRanking(RANK_SECOND_FASTEST))
+                {
+                    speedStr = scrString("second_fastest");
+                }
+                else if (infoQuibble == scr14_GetRanking(RANK_SLOWEST))
+                {
+                    speedStr = scrString("slowest");
+                }
+                infoText = currQ[infoQuibble].name + ":" + global.CARRIAGE_RETURN + global.CARRIAGE_RETURN + speedStr + " " + scrString("quibble_in_race") + ".";
+                if (clumsyStr != "")
+                {
+                    infoText += (global.CARRIAGE_RETURN + global.CARRIAGE_RETURN + clumsyStr + ".");
+                }
+                if (eventType)
+                {
+                    if (eventType == EVENT_METEOR)
+                    {
+                        infoText += (global.CARRIAGE_RETURN + global.CARRIAGE_RETURN + scrString("tip_meteor"));
+                    }
+                    if (eventType == EVENT_POLLUTION)
+                    {
+                        infoText += (global.CARRIAGE_RETURN + global.CARRIAGE_RETURN + scrString("tip_pollution"));
+                    }
+                    if (eventType == EVENT_GARBAGE)
+                    {
+                        infoText += (global.CARRIAGE_RETURN + global.CARRIAGE_RETURN + scrString("tip_garbage"));
+                    }
+                }
+                if (speedStr == scrString("second_fastest"))
+                {
+                    speedStr = scrStringExt("second_fastest_abbrev", 0, 8, 0);
+                }
+                if (clumsyStr == scrString("very_clumsy"))
+                {
+                    clumsyStr = scrStringExt("very_clumsy_abbrev", 0, 8, 0);
+                }
+                if (clumsyStr == scrString("very_steady"))
+                {
+                    clumsyStr = scrStringExt("very_steady_abbrev", 0, 8, 0);
+                }
+                infoTextShort = speedStr;
+                if (clumsyStr != "")
+                {
+                    infoTextShort += ("," + clumsyStr);
+                }
+                sel = 0;
+                subsel = 1;
+                substate = 1;
+            }
+            scr14_ChangeCash(currPlayer, -INFO_COST);
+            scr14_MessageSet(scrString("info_buy_3"));
+        }
+        else
+        {
+            scr14_Sfx(soundInactive, 55);
+            scr14_MessageSet(scrString("info_low_cash"));
+        }
+    }
+    else if (pfo_receive_command(UnknownEnum.Value_24, param, commandPlayer))
+    {
+        var _sel = real(param[0]);
+        if (scr14_GetCash(currPlayer) >= sponseeCost[currPlayer][_sel])
+        {
+            scr14_Sfx(sfx_speak03b, 71);
+            scr14_MessageSet(scrString("breeder_bye"));
+            scr14_ChangeCash(currPlayer, -sponseeCost[currPlayer][_sel]);
+            sponsee[currPlayer][_sel].sponsor = currPlayer;
+            stable[currPlayer][(stable[currPlayer][0] == -4) ? 0 : 1] = sponsee[currPlayer][_sel];
+            scr14_GetStable(currPlayer);
+            if (scr14_CurrentPlayerIsLocal())
+            {
+                sponsorActionTaken = true;
+                sel = 3;
+            }
+        }
+        else
+        {
+            scr14_Sfx(soundInactive, 55);
+            scr14_MessageSet(scrString("breeder_low_cash"));
+        }
+    }
+    else if (pfo_receive_command(UnknownEnum.Value_25, param, commandPlayer))
+    {
+        var _selQ = real(param[0]);
+        var _trainCost = TRAIN_COST;
+        if ((stable[currPlayer][_selQ].qSpeed + TRAIN_AMOUNT) > SPEED_UPPER)
+        {
+            _trainCost += (((stable[currPlayer][_selQ].qSpeed + TRAIN_AMOUNT) - SPEED_UPPER) * TRAIN_COST);
+        }
+        if (stableRetired[currPlayer][_selQ])
+        {
+            scr14_MessageSet(scrString("trainer_retired"));
+        }
+        else if (scr14_GetCash(currPlayer) >= _trainCost && !stable[currPlayer][_selQ].trained)
+        {
+            scr14_Sfx(sfx_speak04b, 71);
+            var r = scrIRandomRange(1, 2);
+            if (r == 1)
+            {
+                scr14_MessageSet(scrString("trainer_train_1"));
+            }
+            else
+            {
+                scr14_MessageSet(scrString("trainer_train_2"));
+            }
+            scr14_ChangeCash(currPlayer, -_trainCost);
+            trainDelta[currPlayer][_selQ] = TRAIN_AMOUNT;
+            stable[currPlayer][_selQ].trained = true;
+        }
+        else if (stable[currPlayer][_selQ].trained)
+        {
+            scr14_Sfx(soundInactive, 55);
+            scr14_MessageSet(scrStringExt("trainer_already_trained", stable[currPlayer][_selQ].name, 0, 0));
+        }
+        else
+        {
+            scr14_Sfx(soundInactive, 55);
+            scr14_MessageSet(scrString("trainer_low_cash"));
+        }
+    }
+    currPlayer++;
+    commandPlayer++;
+}
+currPlayer = savedCurrPlayer;
 for (var i = 0; i < 3; i++)
 {
@@ -125,11 +445,43 @@
 for (var i = 0; i < 3; i++)
 {
+    if (bet[i] > scr14_GetCash(i))
+    {
+        bet[i] = scr14_GetCash(i);
+    }
+    if (bet[i] < 10)
+    {
+        bet[i] = 10;
+    }
+}
+for (var p = 0; p < 3; p++)
+{
+    for (var i = 0; i < 2; i++)
+    {
     if ((timer % 48) == 0)
     {
-        if (trainDelta[i] > 0 && stable[i] != -4)
+            if (trainDelta[p][i] > 0 && stable[p][i] != -4)
         {
-            trainDelta[i]--;
-            stable[i].qSpeed++;
+                trainDelta[p][i]--;
+                stable[p][i].qSpeed++;
         }
     }
+    }
+}
+
+enum UnknownEnum
+{
+    Value_0,
+    Value_1,
+    Value_2,
+    Value_3,
+    Value_16 = 16,
+    Value_17,
+    Value_18,
+    Value_19,
+    Value_20,
+    Value_21,
+    Value_22,
+    Value_23,
+    Value_24,
+    Value_25
 }
--- original/gml_Object_o14_Hand_Draw_0.gml
+++ patched/gml_Object_o14_Hand_Draw_0.gml
@@ -1,2 +1,9 @@
+with (o14_Game)
+{
+    if (scr14_OnlineHideInfo())
+    {
+        exit;
+    }
+}
 if (faceLeft)
 {
--- original/gml_Object_o22_Ball_Draw_0.gml
+++ patched/gml_Object_o22_Ball_Draw_0.gml
@@ -8,5 +8,5 @@
     if (abs(spin) > 0)
     {
-        draw_sprite_ext(sprite_index, round(image_index), x + lengthdir_x(4, current_time * 0.5), y - z, 1, 1, 0, c_white, 1);
+        draw_sprite_ext(sprite_index, round(image_index), x + lengthdir_x(4, global.pfo_current_time * 0.5), y - z, 1, 1, 0, c_white, 1);
     }
     else
--- original/gml_Object_o22_Game_Draw_0.gml
+++ patched/gml_Object_o22_Game_Draw_0.gml
@@ -254,5 +254,5 @@
         else if (_charge > 1 && (_charge / _chargemax) >= i)
         {
-            draw_set_color(global.palette[(((current_time div 80) % 2) == 0) ? 2 : 10]);
+            draw_set_color(global.palette[(((global.pfo_current_time div 80) % 2) == 0) ? 2 : 10]);
             draw_rectangle(camera_get_view_x(view_get_camera(0)) + 19 + (32 * (i - 1)), camera_get_view_y(view_get_camera(0)) + 11, camera_get_view_x(view_get_camera(0)) + 19 + (32 * (i - 1)) + 25, camera_get_view_y(view_get_camera(0)) + 20, 0);
         }
@@ -292,5 +292,5 @@
         else if (_charge > 1 && (_charge / _chargemax) >= i)
         {
-            draw_set_color(global.palette[(((current_time div 80) % 2) == 0) ? 2 : 10]);
+            draw_set_color(global.palette[(((global.pfo_current_time div 80) % 2) == 0) ? 2 : 10]);
             draw_rectangle((camera_get_view_x(view_get_camera(0)) + 384) - 20 - (32 * (i - 1)), camera_get_view_y(view_get_camera(0)) + 11, (camera_get_view_x(view_get_camera(0)) + 384) - 19 - (32 * (i - 1)) - 26, camera_get_view_y(view_get_camera(0)) + 20, 0);
         }
--- original/gml_Object_o23_Mas_Draw_0.gml
+++ patched/gml_Object_o23_Mas_Draw_0.gml
@@ -306,5 +306,5 @@
     var _y = _yview + 48;
     var _img = 0;
-    var _i = current_time div 120;
+    var _i = global.pfo_current_time div 120;
     if (endingType == 1 && (_i % 18) <= 6)
     {
@@ -341,8 +341,8 @@
     draw_rectangle_color(_x, _y, _x + 96, _y + 48, c_black, c_black, c_black, c_black, 0);
     var _sprite = spriteGolfer[_victor];
-    _img = floor(1.5 + lengthdir_x(1.5, current_time / 3));
-    _x = _xview + 112 + lerp(1, -1, (((current_time / 3) + 180) % 720) < 360);
-    _y = (_yview + 112) - abs(lengthdir_x(8 * (_victor == 6), current_time / 6));
-    var _xface = lerp(-1, 1, (((current_time / 3) + 180) % 720) < 360);
+    _img = floor(1.5 + lengthdir_x(1.5, global.pfo_current_time / 3));
+    _x = _xview + 112 + lerp(1, -1, (((global.pfo_current_time / 3) + 180) % 720) < 360);
+    _y = (_yview + 112) - abs(lengthdir_x(8 * (_victor == 6), global.pfo_current_time / 6));
+    var _xface = lerp(-1, 1, (((global.pfo_current_time / 3) + 180) % 720) < 360);
     draw_sprite_ext(_sprite, _img, _x, _y, _xface, 1, 0, c_white, 1);
     var _c = 16777215;
--- original/gml_Object_o28_Mas_Step_0.gml
+++ patched/gml_Object_o28_Mas_Step_0.gml
@@ -299,6 +299,6 @@
                 racer[rCount].human = true;
                 racer[rCount].num = 0;
-                lapP1TimeStamp = current_time;
-                raceP1TimeStamp = current_time;
+                lapP1TimeStamp = global.pfo_current_time;
+                raceP1TimeStamp = global.pfo_current_time;
             }
             else if (multip && uId == playerSel[1])
@@ -306,6 +306,6 @@
                 racer[rCount].human = true;
                 racer[rCount].num = 1;
-                lapP2TimeStamp = current_time;
-                raceP2TimeStamp = current_time;
+                lapP2TimeStamp = global.pfo_current_time;
+                raceP2TimeStamp = global.pfo_current_time;
             }
             else
--- original/gml_Object_o29_Game_Create_0.gml
+++ patched/gml_Object_o29_Game_Create_0.gml
@@ -26,4 +26,5 @@
 numPlayers = global.numPlayers;
 currPlayer = -1;
+firstSpawn = false;
 lvl = 1;
 myLives = NUM_START_LIVES - 1;
--- original/gml_Object_o29_Game_Other_13.gml
+++ patched/gml_Object_o29_Game_Other_13.gml
@@ -69,8 +69,9 @@
             oxygen = 128;
             futureState = STATE_LEVEL_PLAY;
-            if (numPlayers == 2)
+            if (!firstSpawn)
             {
-                currPlayer = !currPlayer;
+                currPlayer = (currPlayer + 1) % numPlayers;
             }
+            firstSpawn = false;
             playerIndicator++;
             playerIndicatorTimer = 240;
@@ -154,8 +155,9 @@
                 oxygen = OXYGEN_MAX;
                 futureState = STATE_LEVEL_PLAY;
-                if (numPlayers == 2)
+                if (!firstSpawn)
                 {
-                    currPlayer = !currPlayer;
+                    currPlayer = (currPlayer + 1) % numPlayers;
                 }
+                firstSpawn = false;
                 hasSpawned = true;
                 exit;
--- original/gml_Object_o29_Game_Other_16.gml
+++ patched/gml_Object_o29_Game_Other_16.gml
@@ -318,12 +318,6 @@
     if (scrTransitionDone())
     {
-        if (numPlayers == 1)
-        {
             currPlayer = 0;
-        }
-        else
-        {
-            currPlayer = 1;
-        }
+        firstSpawn = true;
         futureState = STATE_LEVEL_SELECT;
     }
--- original/gml_Object_o29_Game_Other_21.gml
+++ patched/gml_Object_o29_Game_Other_21.gml
@@ -54,12 +54,6 @@
         }
         scrSfx(soundSelect, 60);
-        if (numPlayers == 1)
-        {
             currPlayer = 0;
-        }
-        else
-        {
-            currPlayer = 1;
-        }
+        firstSpawn = true;
         myLives = bestScores[global.numPlayers][lvl] - 1;
         if (lvl != 10)
--- original/gml_Object_o30_Game_Step_0.gml
+++ patched/gml_Object_o30_Game_Step_0.gml
@@ -6,5 +6,12 @@
     stateTimer = 0;
 }
+if (state == STATE_2P_STATS)
+{
+    scrGetInputMerged();
+}
+else
+{
 scrGetInput(controller - 1);
+}
 event_user(state);
 timer++;
--- original/gml_Object_o32_Mas_Draw_0.gml
+++ patched/gml_Object_o32_Mas_Draw_0.gml
@@ -74,5 +74,5 @@
 {
     draw_rectangle_color(_xv + 96, (_yv + 216) - 24, _xv + 384, (_yv + 216) - 8, c_black, c_black, c_black, c_black, 0);
-    var _xText = (_xv + 384) - ((((current_time * 0.1) % 672) div 8) * 8);
+    var _xText = (_xv + 384) - ((((global.pfo_current_time * 0.1) % 672) div 8) * 8);
     var _yText = (_yv + 216) - 24;
     scrSetFont(global.fontTall);
@@ -227,5 +227,5 @@
     var _xRadarPoint = round(_xRadarCenter + lengthdir_x(_RadarDistance, _CheckpointDirection));
     var _yRadarPoint = round(_yRadarCenter + lengthdir_y(_RadarDistance, _CheckpointDirection));
-    draw_sprite(s32_RadarPoint, (current_time div 100) % 2, _xRadarPoint, _yRadarPoint);
+    draw_sprite(s32_RadarPoint, (global.pfo_current_time div 100) % 2, _xRadarPoint, _yRadarPoint);
     var _drawPointer = false;
     if (tPointer > 0 && (tPointer > 30 || global.halfTime) && onionsDelivered[dayNumber] != 5)
@@ -253,5 +253,5 @@
         _xRadarPoint = round(_xRadarCenter + lengthdir_x(_RadarDistance, _CheckpointDirection));
         _yRadarPoint = round(_yRadarCenter + lengthdir_y(_RadarDistance, _CheckpointDirection));
-        draw_sprite(s32_RadarPointHome, (current_time div 100) % 2, _xRadarPoint, _yRadarPoint);
+        draw_sprite(s32_RadarPointHome, (global.pfo_current_time div 100) % 2, _xRadarPoint, _yRadarPoint);
     }
     if (returnToBase)
--- original/gml_Object_o32_eGangsterCar_Draw_0.gml
+++ patched/gml_Object_o32_eGangsterCar_Draw_0.gml
@@ -16,5 +16,5 @@
     else
     {
-        _aimdir = round(current_time / 10 / 22.5) * 22.5;
+        _aimdir = round(global.pfo_current_time / 10 / 22.5) * 22.5;
         target = -4;
     }
--- original/gml_Object_o32_eGangsterCar_Step_0.gml
+++ patched/gml_Object_o32_eGangsterCar_Step_0.gml
@@ -244,5 +244,5 @@
     else
     {
-        _aimdir = round(current_time / 10 / 22.5) * 22.5;
+        _aimdir = round(global.pfo_current_time / 10 / 22.5) * 22.5;
     }
     if (shotCountBurst++ > 60)
--- original/gml_Object_o32_eUFOInvaderSmall_Step_0.gml
+++ patched/gml_Object_o32_eUFOInvaderSmall_Step_0.gml
@@ -21,5 +21,5 @@
     stateTime--;
     zHeight = approach(zHeight, 48 + lengthdir_x(4, (stateTime / 100) * 1080), 1);
-    direction += lengthdir_x(2, current_time div 100);
+    direction += lengthdir_x(2, global.pfo_current_time div 100);
     if (stateTime <= 0)
     {
--- original/gml_Object_o35_Game_Step_0.gml
+++ patched/gml_Object_o35_Game_Step_0.gml
@@ -86,5 +86,5 @@
 if (isFlying > 0 && gameState != UnknownEnum.Value_1 && gameState != UnknownEnum.Value_6 && !instance_exists(o35__Enemy) && descending == 0)
 {
-    o35_Background.y = lerp(o35_Background.y, 4 + min(isFlying, 4) + lengthdir_x(2, current_time / 10), 0.1);
+    o35_Background.y = lerp(o35_Background.y, 4 + min(isFlying, 4) + lengthdir_x(2, global.pfo_current_time / 10), 0.1);
 }
 else if (gameState != UnknownEnum.Value_m99)
--- original/gml_Object_o35_Player_Draw_0.gml
+++ patched/gml_Object_o35_Player_Draw_0.gml
@@ -83,10 +83,10 @@
 {
     _outline = true;
-    _outlinecol = global.palette[lerp(3, 24, (current_time div 3) % 2)];
+    _outlinecol = global.palette[lerp(3, 24, (global.pfo_current_time div 3) % 2)];
 }
 if (o35_Game.isPoisoned > 0 && mainBody)
 {
     _outline = true;
-    _outlinecol = global.palette[lerp(12, 24, (current_time div 3) % 2)];
+    _outlinecol = global.palette[lerp(12, 24, (global.pfo_current_time div 3) % 2)];
 }
 if (o35_Game.isHasted > 0 && mainBody)
--- original/gml_Object_o35__Enemy_Draw_0.gml
+++ patched/gml_Object_o35__Enemy_Draw_0.gml
@@ -29,5 +29,5 @@
 {
     _outline = true;
-    _outlinecol = global.palette[28 + ((current_time div 200) % 2)];
+    _outlinecol = global.palette[28 + ((global.pfo_current_time div 200) % 2)];
     _colorshift = global.palette[4];
 }
@@ -40,5 +40,5 @@
     _cycle[3] = 18;
     _outline = true;
-    _outlinecol = merge_color(c_black, global.palette[_cycle[(current_time div 200) % 4]], 0.75);
+    _outlinecol = merge_color(c_black, global.palette[_cycle[(global.pfo_current_time div 200) % 4]], 0.75);
     _colorshift = global.palette[2];
 }
@@ -75,5 +75,5 @@
     }
 }
-if (isFlashing > 0 && (floor(current_time / 50) % 2) == 0)
+if (isFlashing > 0 && (floor(global.pfo_current_time / 50) % 2) == 0)
 {
     gpu_set_fog(true, flashingColor, 0, 1);
@@ -82,10 +82,10 @@
 {
     _colorshift = 16777215;
-    _outlinecol = lerp(128, 0, (current_time div 100) % 2);
+    _outlinecol = lerp(128, 0, (global.pfo_current_time div 100) % 2);
 }
 if (scr35_CurrAttackHasTrait("unstoppable") && animState <= 1)
 {
     _colorshift = global.palette[3];
-    _outlinecol = lerp(16711680, 0, ((current_time div 100) + 1) % 2);
+    _outlinecol = lerp(16711680, 0, ((global.pfo_current_time div 100) + 1) % 2);
 }
 if (_colorshift != -1 && global.canDoShaders)
--- original/gml_Object_o35_eAbyssLordHand_Step_0.gml
+++ patched/gml_Object_o35_eAbyssLordHand_Step_0.gml
@@ -54,5 +54,5 @@
     {
         x = clamp(lerp(x, clamp(_lord.x, 144, 240) + lengthdir_x(64, orbitTimeActual), 0.1), 96, 288);
-        y = lerp(y, ((_lord.y + lengthdir_y(32 + (32 * (_lord.fightMode == 4)), orbitTimeActual)) - 16) + lengthdir_y(8, current_time * 0.6), 0.1);
+        y = lerp(y, ((_lord.y + lengthdir_y(32 + (32 * (_lord.fightMode == 4)), orbitTimeActual)) - 16) + lengthdir_y(8, global.pfo_current_time * 0.6), 0.1);
         z = 152 - y;
         moveCount++;
--- original/gml_Object_o35_iPuzzleWall_Draw_0.gml
+++ patched/gml_Object_o35_iPuzzleWall_Draw_0.gml
@@ -46,5 +46,5 @@
                     else
                     {
-                        if (((current_time div 60) % 100) == (xx + yy))
+                        if (((global.pfo_current_time div 60) % 100) == (xx + yy))
                         {
                             gpu_set_fog(true, global.palette[0], 0, 1);
--- original/gml_Object_o35_iSpellWall_Draw_0.gml
+++ patched/gml_Object_o35_iSpellWall_Draw_0.gml
@@ -18,5 +18,5 @@
 var _y = round(y + 108);
 var _prevRNG = scrGetRandomState();
-randomize();
+pfo_randomize();
 rng_seed(random(99999999));
 if (o35_Game.gameState != UnknownEnum.Value_1 && o35_Game.gameState != UnknownEnum.Value_9)
--- original/gml_Object_o35_pWhirlwind_Step_0.gml
+++ patched/gml_Object_o35_pWhirlwind_Step_0.gml
@@ -3,5 +3,5 @@
 x = xstart + lengthdir_y(64 - (aliveTime * 0.4), aliveTime * 5);
 image_index = ceil(lerp(58, 54, clamp(aliveTime / 45, 0, 1)));
-image_xscale = lerp(-1, 1, (current_time div 250) % 2);
+image_xscale = lerp(-1, 1, (global.pfo_current_time div 250) % 2);
 with (o35__Enemy)
 {
--- original/gml_Object_o45_MapNode_Draw_0.gml
+++ patched/gml_Object_o45_MapNode_Draw_0.gml
@@ -15,5 +15,5 @@
     else
     {
-        draw_sprite(sprite_index, 4 + ((current_time div 100) % 4), x, y);
+        draw_sprite(sprite_index, 4 + ((global.pfo_current_time div 100) % 4), x, y);
     }
 }
--- original/gml_Object_o45__pAsimov_Draw_0.gml
+++ patched/gml_Object_o45__pAsimov_Draw_0.gml
@@ -25,5 +25,5 @@
         if (chargeCount == chargeCountMax)
         {
-            if (((current_time div 100) % 2) == 0)
+            if (((global.pfo_current_time div 100) % 2) == 0)
             {
                 scrSpriteColored(sprite_index, image_index, x, y, xscale, image_yscale, global.palette[18]);
--- original/gml_Object_o45_aCat_Step_0.gml
+++ patched/gml_Object_o45_aCat_Step_0.gml
@@ -8,5 +8,5 @@
         hspd = sign(hspd) * 3;
     }
-    y = ystart - abs(lengthdir_x(4, current_time * 0.8));
+    y = ystart - abs(lengthdir_x(4, global.pfo_current_time * 0.8));
     if (y > yprevious)
     {
--- original/gml_Object_o45_eHawk_Step_0.gml
+++ patched/gml_Object_o45_eHawk_Step_0.gml
@@ -145,5 +145,5 @@
     frame = -1;
     var _t = o45_guin_AsimovTop;
-    var _d = 48 + lengthdir_x(2, current_time div 10);
+    var _d = 48 + lengthdir_x(2, global.pfo_current_time div 10);
     if (instance_exists(_t))
     {
--- original/gml_Object_o45_eJetpacker_Step_0.gml
+++ patched/gml_Object_o45_eJetpacker_Step_0.gml
@@ -66,5 +66,5 @@
     if (thrownCount > 60)
     {
-        y = lerp(y, ystart + lengthdir_x(16, current_time * 0.2), 0.2);
+        y = lerp(y, ystart + lengthdir_x(16, global.pfo_current_time * 0.2), 0.2);
     }
     image_index = approach(image_index, 1 - (sign(x - o45__pAsimov.x) * (abs(x - o45__pAsimov.x) > 24)), 0.1);
@@ -74,5 +74,5 @@
     if (thrownCount > 60)
     {
-        y = lerp(y, ystart + lengthdir_x(16, current_time * 0.2), 0.2);
+        y = lerp(y, ystart + lengthdir_x(16, global.pfo_current_time * 0.2), 0.2);
     }
     if (moveCount > 0)
--- original/gml_Object_o45_eShotFront_Draw_0.gml
+++ patched/gml_Object_o45_eShotFront_Draw_0.gml
@@ -1,3 +1,3 @@
-if (((current_time div 60) % 2) == 0)
+if (((global.pfo_current_time div 60) % 2) == 0)
 {
     draw_sprite(s45_Shadow, 0, x, y + z);
--- original/gml_Object_o45_hux_roadDebris_Draw_0.gml
+++ patched/gml_Object_o45_hux_roadDebris_Draw_0.gml
@@ -1,3 +1,3 @@
-if (((current_time div 20) % 2) == 0)
+if (((global.pfo_current_time div 20) % 2) == 0)
 {
     draw_sprite(s45_Shadow, 0, x, y);
--- original/gml_Object_o45_hux_roadDebris_Step_0.gml
+++ patched/gml_Object_o45_hux_roadDebris_Step_0.gml
@@ -45,5 +45,5 @@
             instance_destroy();
         }
-        else if (((current_time div 2) % 10) == 0)
+        else if (((global.pfo_current_time div 2) % 10) == 0)
         {
             image_angle -= 90;
--- original/gml_Object_o45_rescueEntity_Draw_0.gml
+++ patched/gml_Object_o45_rescueEntity_Draw_0.gml
@@ -10,5 +10,5 @@
 var _mox = camera_get_view_x(view_get_camera(0)) + (4.5 * TILE_SIZE);
 var _moy = camera_get_view_y(view_get_camera(0));
-if (asleep > 0 && ((current_time div 50) % 20) < 10 && hp > 0)
+if (asleep > 0 && ((global.pfo_current_time div 50) % 20) < 10 && hp > 0)
 {
     draw_sprite_ext(sprite_index, RESCUE.SPRITE_RESCUE_ZZZ + min(asleep, 3), _mox + (lerp(xprevious, x, 0.5) * TILE_SIZE) + (TILE_SIZE * 0.5) + xoffset, ((_moy + (lerp(yprevious, y, 0.5) * TILE_SIZE)) - 4) + yoffset, image_xscale, 1, 0, c_white, 1);
@@ -20,5 +20,5 @@
 if (type == UnknownEnum.Value_1 || type == UnknownEnum.Value_9)
 {
-    yoffset = lengthdir_y(2, current_time * 0.1);
+    yoffset = lengthdir_y(2, global.pfo_current_time * 0.1);
     xoffset = 0;
 }
--- original/gml_Object_o45_rescueEntity_Step_0.gml
+++ patched/gml_Object_o45_rescueEntity_Step_0.gml
@@ -69,5 +69,5 @@
 {
     xoffset = lerp((xstart - x) * 16, 0, clamp(interp, 0, 1));
-    yoffset = lerp((ystart - y) * 16, lengthdir_y(2 * (asleep == 0), current_time * 0.2) - (2 * (asleep == 0)), clamp(interp, 0, 1)) + lengthdir_y(32, 180 * clamp(interp, 0, 1));
+    yoffset = lerp((ystart - y) * 16, lengthdir_y(2 * (asleep == 0), global.pfo_current_time * 0.2) - (2 * (asleep == 0)), clamp(interp, 0, 1)) + lengthdir_y(32, 180 * clamp(interp, 0, 1));
     interp = min(interp + 0.03, 1);
 }
--- original/gml_Object_o50_Game_Create_0.gml
+++ patched/gml_Object_o50_Game_Create_0.gml
@@ -263,4 +263,5 @@
 flicker8 = false;
 hudShiftX = 0;
+miniHudShiftX = 0;
 scenarioY = 0;
 newBuildType = -1;
@@ -279,5 +280,12 @@
 else if (numPlayers == 2)
 {
+    if (pfo_is_online())
+    {
+        fogOfWar = true;
+    }
+    else
+    {
     fogOfWar = false;
+    }
 }
 else if (numPlayers == 0)
--- original/gml_Object_o50_Game_Draw_0.gml
+++ patched/gml_Object_o50_Game_Draw_0.gml
@@ -1,2 +1,5 @@
+var hasOnlinePlayers = scr50_HasOnlinePlayers();
+var isAnyOnlineLocalPlayer = scr50_IsOnlineLocalPlayer(1) || scr50_IsOnlineLocalPlayer(2);
+var onlineLocalPlayer = scr50_IsOnlineLocalPlayer(2) ? 2 : 1;
 var viewx = camera_get_view_x(view_get_camera(0));
 var viewy = camera_get_view_y(view_get_camera(0));
@@ -389,5 +392,5 @@
                 }
             }
-            if (loc.revealed || (loc.owner == player && numPlayers == 2 && !combatFlash && state != STATE_END_COMBAT && state != STATE_END_TURN) || (loc.owner == 1 && numPlayers == 1) || !fogOfWar)
+            if ((numPlayers == 2 && !hasOnlinePlayers && loc.revealed[player] && !combatFlash && state != STATE_END_COMBAT && state != STATE_END_TURN) || (numPlayers == 2 && hasOnlinePlayers && loc.revealed[onlineLocalPlayer]) || (numPlayers == 1 && loc.revealed[1]) || (numPlayers == 2 && !hasOnlinePlayers && loc.owner == player && !combatFlash && state != STATE_END_COMBAT && state != STATE_END_TURN) || (numPlayers == 2 && hasOnlinePlayers && loc.owner == onlineLocalPlayer) || (numPlayers == 1 && loc.owner == 1) || !fogOfWar || (numPlayers == 2 && hasOnlinePlayers && !isAnyOnlineLocalPlayer))
             {
                 var xShift = 0;
@@ -417,5 +420,5 @@
                     if (loc.resources[k])
                     {
-                        if (loc.owner == player)
+                        if ((numPlayers == 2 && isAnyOnlineLocalPlayer) ? (loc.owner == onlineLocalPlayer) : (loc.owner == player))
                         {
                             if (!flicker20)
@@ -516,5 +519,5 @@
         }
     }
-    if ((fogOfWar == false && !custom) || controller != 3)
+    if ((numPlayers == 2 && hasOnlinePlayers) ? (controller == onlineLocalPlayer || !isAnyOnlineLocalPlayer) : ((fogOfWar == false && !custom) || controller != 3))
     {
         var OFF_COLOR = 24;
@@ -566,7 +569,11 @@
                         var xPos = scrIRandomRange(GRID_LEFT + (GRID_WIDTH * i), (GRID_LEFT + (GRID_WIDTH * i) + GRID_WIDTH) - 8);
                         var yPos = scrIRandomRange(GRID_TOP + (GRID_HEIGHT * j), (GRID_TOP + (GRID_HEIGHT * j) + GRID_HEIGHT) - 8);
-                        scrFX(s50_StarFX, 0.3, scrQuantize(xPos, 8), scrQuantize(yPos, 8));
+                        var starFX = scrFX(s50_StarFX, 0.3, scrQuantize(xPos, 8), scrQuantize(yPos, 8));
+                        if (is_int64(map[i][j].sparkleTimer) && (numPlayers == 2 && scr50_IsOnlineRemotePlayer(controller)))
+                        {
+                            starFX.visible = false;
                     }
                 }
+                }
                 map[i][j].sparkleTimer--;
             }
@@ -581,5 +588,5 @@
         }
     }
-    if ((numPlayers == 2 && fogOfWar && !combatFlash && state != STATE_END_COMBAT && state != STATE_END_TURN) || (numPlayers == 1 && fogOfWar && player == 1) || (numPlayers == 1 && !fogOfWar && (player == 1 || !custom)) || (numPlayers == 2 && !fogOfWar) || aiVersus)
+    if ((numPlayers == 2 && !hasOnlinePlayers && fogOfWar && !combatFlash && state != STATE_END_COMBAT && state != STATE_END_TURN) || (numPlayers == 2 && hasOnlinePlayers && player == onlineLocalPlayer) || (numPlayers == 1 && fogOfWar && player == 1) || (numPlayers == 1 && !fogOfWar && (player == 1 || !custom)) || (numPlayers == 2 && !hasOnlinePlayers && !fogOfWar) || (numPlayers == 2 && hasOnlinePlayers && !isAnyOnlineLocalPlayer) || aiVersus)
     {
         if ((state == STATE_GOD_SELECT && subState == 4) || (state == STATE_ACTIONS && subState == (200 + (10 * OFFERING) + 3)))
@@ -932,35 +939,50 @@
     else
     {
-        draw_sprite(s50_MiniHUD, actionHighlight, HUD_LEFT + hudShiftX, 0);
+        var _hudShiftX = (numPlayers == 2 && hasOnlinePlayers) ? miniHudShiftX : hudShiftX;
+        _hudShiftX = hudShiftX;
+        draw_sprite(s50_MiniHUD, actionHighlight, HUD_LEFT + _hudShiftX, 0);
+        if (numPlayers == 2 && hasOnlinePlayers)
+        {
+            if (player == 1)
+            {
+                draw_set_color(global.palette[9]);
+            }
+            if (player == 2)
+            {
+                draw_set_color(global.palette[4]);
+            }
+            draw_text(HUD_LEFT + _hudShiftX + 8, 0, "P" + string(player));
+            draw_set_color(c_white);
+        }
         if (godAvail[god] >= GOD_COOLDOWN)
         {
             if (!SCRAMBLED_ACTIONS)
             {
-                draw_sprite(s50_Gods, GOD_PORTRAITS[god], HUD_LEFT + hudShiftX + 16, 8);
+                draw_sprite(s50_Gods, GOD_PORTRAITS[god], HUD_LEFT + _hudShiftX + 16, 8);
             }
             else
             {
-                draw_sprite(s50_GodsScrambled, GOD_PORTRAITS[god], HUD_LEFT + hudShiftX + 16, 8);
+                draw_sprite(s50_GodsScrambled, GOD_PORTRAITS[god], HUD_LEFT + _hudShiftX + 16, 8);
             }
             if (godPowerFlicker)
             {
                 godPowerFlicker--;
-                draw_sprite(powerSprite, godPower[player][god] - flicker8, HUD_LEFT + hudShiftX + 64, 8);
+                draw_sprite(powerSprite, godPower[player][god] - flicker8, HUD_LEFT + _hudShiftX + 64, 8);
             }
             else
             {
-                draw_sprite(powerSprite, godPower[player][god], HUD_LEFT + hudShiftX + 64, 8);
+                draw_sprite(powerSprite, godPower[player][god], HUD_LEFT + _hudShiftX + 64, 8);
             }
         }
         else
         {
-            draw_sprite(s50_GodsExhausted, GOD_PORTRAITS[god], HUD_LEFT + hudShiftX + 16, 8);
-            draw_sprite(powerSprite, 0, HUD_LEFT + hudShiftX + 64, 8);
+            draw_sprite(s50_GodsExhausted, GOD_PORTRAITS[god], HUD_LEFT + _hudShiftX + 16, 8);
+            draw_sprite(powerSprite, 0, HUD_LEFT + _hudShiftX + 64, 8);
         }
-        draw_text((HUD_TEXT_CENTER + hudShiftX) - 32, 56, string_even(GOD_NAMES[god], 4));
+        draw_text((HUD_TEXT_CENTER + _hudShiftX) - 32, 56, string_even(GOD_NAMES[god], 4));
     }
     draw_set_color(c_white);
     draw_text(GRID_LEFT, 200, scrMessageWrite(2));
-    if ((fogOfWar == false && !custom) || controller != 3)
+    if ((numPlayers == 2 && hasOnlinePlayers) ? (controller == onlineLocalPlayer || !isAnyOnlineLocalPlayer) : ((fogOfWar == false && !custom) || controller != 3))
     {
         if (transfer)
@@ -1074,4 +1096,7 @@
     if (!global.attractMode)
     {
+        var drewPlayer1 = false;
+        if (!(numPlayers == 2 && scr50_IsOnlineRemotePlayer(1)))
+        {
         if (defendPlayer == 1 && surpriseAttack)
         {
@@ -1135,5 +1160,7 @@
             }
         }
-        if (numPlayers == 2)
+            drewPlayer1 = true;
+        }
+        if (numPlayers == 2 && !scr50_IsOnlineRemotePlayer(2))
         {
             if (defendPlayer == 2 && surpriseAttack)
@@ -1198,6 +1225,9 @@
                 }
             }
+            if (drewPlayer1)
+            {
             draw_set_color(c_white);
             draw_line(viewx + 192, viewy + 192, viewx + 192, viewy + 216);
+            }
         }
     }
--- original/gml_Object_o50_Game_KeyPress_49.gml
+++ patched/gml_Object_o50_Game_KeyPress_49.gml
@@ -7,5 +7,8 @@
         for (var j = 0; j < GRID_Y_NUM; j++)
         {
-            map[i][j].revealed = false;
+            for (var p = 1; p <= 2; p++)
+            {
+                map[i][j].revealed[p] = false;
+            }
         }
     }
--- original/gml_Object_o50_Game_Other_11.gml
+++ patched/gml_Object_o50_Game_Other_11.gml
@@ -834,5 +834,5 @@
     {
         scrSfx(soundMissionStart, 10);
-        if (numPlayers == 1 || !fogOfWar)
+        if (numPlayers == 1 || !fogOfWar || scr50_HasOnlinePlayers())
         {
             futureState = STATE_START_TURN;
--- original/gml_Object_o50_Game_Other_12.gml
+++ patched/gml_Object_o50_Game_Other_12.gml
@@ -30,8 +30,11 @@
         for (var j = 0; j < GRID_Y_NUM; j++)
         {
-            if (map[i][j].revealed)
+            for (var p = 1; p <= 2; p++)
             {
-                map[i][j].revealed--;
+                if (map[i][j].revealed[p])
+                {
+                    map[i][j].revealed[p]--;
             }
+            }
             if (map[i][j].frozen)
             {
@@ -57,4 +60,5 @@
     until (godAvail[god] >= GOD_COOLDOWN);
     hudShiftX = -HUD_WIDTH;
+    miniHudShiftX = -HUD_WIDTH;
 }
 if (subState == 1)
--- original/gml_Object_o50_Game_Other_13.gml
+++ patched/gml_Object_o50_Game_Other_13.gml
@@ -3,4 +3,8 @@
     subState = 1;
     scr50_MessageFastLocal("which_god");
+    if (numPlayers == 2 && scr50_IsOnlineRemotePlayer(controller))
+    {
+        scrMessageSet("ENEMY PLAYER TURN");
+    }
     with (o50_Move)
     {
@@ -93,5 +97,5 @@
         scrSfx(soundGodSelect, 30);
         scr50_MessageFast("");
-        if (controller == 3)
+        if (controller == 3 || (numPlayers == 2 && scr50_IsOnlineRemotePlayer(controller)))
         {
             scrMessageSet(scrStringVal("enemy_prays", GOD_NAMES[god]));
@@ -117,4 +121,8 @@
 {
     stateTimer++;
+    if (miniHudShiftX < 0 && (stateTimer % 2) == 0)
+    {
+        miniHudShiftX += 8;
+    }
     if (stateTimer >= 80)
     {
@@ -143,5 +151,5 @@
     if (stateTimer >= 120)
     {
-        if (controller != 3)
+        if (controller != 3 && !(numPlayers == 2 && scr50_IsOnlineRemotePlayer(controller)))
         {
             scrBGM(bgm50_bless);
@@ -246,5 +254,9 @@
             var xPos = scrIRandomRange(HUD_LEFT, (HUD_LEFT + HUD_WIDTH) - 8);
             var yPos = scrIRandomRange(ACTION_TOP + (ACTION_HEIGHT * upgradeIndex), (ACTION_TOP + (ACTION_HEIGHT * upgradeIndex) + ACTION_HEIGHT) - 8);
-            scrFX(s50_StarFXBlack, 0.3, scrQuantize(xPos, 8), scrQuantize(yPos, 8));
+            var starFX = scrFX(s50_StarFXBlack, 0.3, scrQuantize(xPos, 8), scrQuantize(yPos, 8));
+            if (numPlayers == 2 && scr50_IsOnlineRemotePlayer(controller))
+            {
+                starFX.visible = false;
+            }
         }
     }
--- original/gml_Object_o50_Game_Other_14.gml
+++ patched/gml_Object_o50_Game_Other_14.gml
@@ -373,13 +373,17 @@
             controller = 1;
         }
-        if (numPlayers == 1 && player == 1)
+        else if (numPlayers == 2 && scr50_HasOnlinePlayers())
         {
+            controller = (player == 1) ? 2 : 1;
+        }
+        if ((numPlayers == 1 && player == 1) || (numPlayers == 2 && scr50_IsOnlineLocalPlayer(player)))
+        {
             scrMessageSet(scrString("you_attacked"));
         }
-        else if (numPlayers == 1 && player == 2)
+        else if ((numPlayers == 1 && player == 2) || (numPlayers == 2 && scr50_IsOnlineRemotePlayer(player)))
         {
             scrMessageSet(scrString("they_attacked"));
         }
-        else if (numPlayers == 2 && !fogOfWar)
+        else if (numPlayers == 2 && (!fogOfWar || scr50_HasOnlinePlayers()))
         {
             scrMessageSet(scrStringExt("player_attacked", player, 35, 0));
@@ -392,12 +396,12 @@
     else
     {
-        if (controller == 3 && toLoc.owner != player)
+        if ((controller == 3 || (numPlayers == 2 && scr50_IsOnlineRemotePlayer(controller))) && toLoc.owner != player)
         {
             scrSfx(soundMoveUnits, 70);
             scrMessageSet(scrString("enemy_expand"));
         }
-        else if (controller != 3)
+        else
         {
-            scrSfx(soundMoveUnits, 70);
+            scr50_Sfx(soundMoveUnits, 70);
         }
         for (var i = 0; i < NUM_UNITS; i++)
--- original/gml_Object_o50_Game_Other_15.gml
+++ patched/gml_Object_o50_Game_Other_15.gml
@@ -79,5 +79,8 @@
     {
         mirChoice = false;
+        if (!(numPlayers == 2 && scr50_IsOnlineRemotePlayer(controller)))
+        {
         scrBGM(bgm50_bless);
+        }
         stateTimer = 0;
         subState++;
@@ -175,7 +178,11 @@
             var xPos = scrIRandomRange(HUD_LEFT, (HUD_LEFT + HUD_WIDTH) - 8);
             var yPos = scrIRandomRange(ACTION_TOP + (ACTION_HEIGHT * upgradeIndex), (ACTION_TOP + (ACTION_HEIGHT * upgradeIndex) + ACTION_HEIGHT) - 8);
-            scrFX(s50_StarFXBlack, 0.3, scrQuantize(xPos, 8), scrQuantize(yPos, 8));
+            var starFX = scrFX(s50_StarFXBlack, 0.3, scrQuantize(xPos, 8), scrQuantize(yPos, 8));
+            if (numPlayers == 2 && scr50_IsOnlineRemotePlayer(controller))
+            {
+                starFX.visible = false;
         }
     }
+    }
     if (stateTimer >= 144)
     {
@@ -204,5 +211,5 @@
         {
             var loc = map[i][j];
-            if (loc.owner != 0 && loc.owner != player && !loc.revealed)
+            if (loc.owner != 0 && loc.owner != player && !loc.revealed[player])
             {
                 numSelectable++;
@@ -244,5 +251,5 @@
     {
         scr50_Sfx(soundMiracle, 50);
-        map[selX][selY].revealed = 7;
+        map[selX][selY].revealed[player] = 7;
         scr50_MessageSlow(scrString("revealed"));
         subState = 299;
@@ -250,5 +257,5 @@
         if (controller != 3)
         {
-            map[selX][selY].sparkleTimer = 48;
+            map[selX][selY].sparkleTimer = UnknownEnum.Value_48;
         }
     }
@@ -317,5 +324,5 @@
             scrSfx(soundMiracle, 50);
             map[selX][selY].frozen = 3;
-            if (numPlayers == 1 && player == 2)
+            if ((numPlayers == 1 && player == 2) || (numPlayers == 2 && scr50_IsOnlineRemotePlayer(player)))
             {
                 scrMessageSet(scrStringVal("snowed_in_by_comp", unitsFrozen));
@@ -398,5 +405,5 @@
         {
             scrSfx(soundMiracle, 50);
-            if (numPlayers == 1 && player == 2)
+            if ((numPlayers == 1 && player == 2) || (numPlayers == 2 && scr50_IsOnlineRemotePlayer(player)))
             {
                 scrMessageSet(scrStringVal("killed_by_comp", unitsStruck));
@@ -404,5 +411,5 @@
             else
             {
-                scr50_MessageSlow(scrStringVal("enemy_units_killed", unitsStruck));
+                scrMessageSet(scrStringVal("enemy_units_killed", unitsStruck));
             }
             map[selX][selY].sparkleTimer = 48;
@@ -854,3 +861,8 @@
     resources[player][refundType] += refundAmount;
     scr50_MessageFastLocal("what_miracle");
+}
+
+enum UnknownEnum
+{
+    Value_48 = 48
 }
--- original/gml_Object_o50_Game_Other_16.gml
+++ patched/gml_Object_o50_Game_Other_16.gml
@@ -13,4 +13,5 @@
         subState = 1.5;
         hudShiftX = 0;
+        miniHudShiftX = 0;
     }
 }
@@ -136,5 +137,5 @@
     {
         scrSfx(soundFlagGained, 80);
-        if (numPlayers == 1 && player == 1)
+        if ((numPlayers == 1 && player == 1) || (numPlayers == 2 && scr50_IsOnlineLocalPlayer(player)))
         {
             if (NUM_CONTROL_POINTS == 0 || flagCounter[player] >= 2)
@@ -147,5 +148,5 @@
             }
         }
-        if (numPlayers == 1 && player == 2)
+        else if ((numPlayers == 1 && player == 2) || (numPlayers == 2 && scr50_IsOnlineRemotePlayer(player)))
         {
             if (NUM_CONTROL_POINTS == 0 || flagCounter[player] >= 2)
@@ -158,5 +159,5 @@
             }
         }
-        if (numPlayers == 2)
+        else if (numPlayers == 2)
         {
             scrMessageSet(scrStringExt("hold_castle_p_" + string(flagCounter[player]), player, 35, 0));
@@ -186,4 +187,5 @@
     {
         hudShiftX -= 8;
+        miniHudShiftX -= 8;
     }
     if (abs(hudShiftX) >= HUD_WIDTH)
@@ -213,9 +215,5 @@
             }
         }
-        if (numPlayers == 1)
-        {
-            futureState = STATE_START_TURN;
-        }
-        else if (!fogOfWar)
+        if (numPlayers == 1 || !fogOfWar || scr50_HasOnlinePlayers())
         {
             futureState = STATE_START_TURN;
--- original/gml_Object_o50_Game_Other_18.gml
+++ patched/gml_Object_o50_Game_Other_18.gml
@@ -616,5 +616,8 @@
                 if (bPressLeft[inp])
                 {
+                    if (!(numPlayers == 2 && scr50_IsOnlineRemotePlayer(p)))
+                    {
                     scr50_Sfx(soundBattleNavi, 60);
+                    }
                     orderSel[p]--;
                     if (orderSel[p] < 1)
@@ -625,5 +628,8 @@
                 if (bPressRight[inp])
                 {
+                    if (!(numPlayers == 2 && scr50_IsOnlineRemotePlayer(p)))
+                    {
                     scr50_Sfx(soundBattleNavi, 60);
+                    }
                     orderSel[p]++;
                     if (orderSel[p] > maxOrder)
@@ -634,5 +640,8 @@
                 if (bFire2pressed[inp] && currOrder[p] != orderSel[p])
                 {
+                    if (!(numPlayers == 2 && scr50_IsOnlineRemotePlayer(p)))
+                    {
                     scr50_Sfx(soundBattleSelect, 65);
+                    }
                     currOrder[p] = orderSel[p];
                     orderFlicker[p] = 40;
--- original/gml_Object_o50_Game_Other_19.gml
+++ patched/gml_Object_o50_Game_Other_19.gml
@@ -24,4 +24,5 @@
         }
         defendLoc.owner = player;
+        defendLoc.frozen = false;
         if (defendLoc.terrain == CASTLE || defendLoc.terrain == CASTLE_P1 || defendLoc.terrain == CASTLE_P2)
         {
@@ -39,11 +40,11 @@
             }
         }
-        if (numPlayers == 1 || !fogOfWar || (actionIndex == 2 && action.type != MIRACLE))
+        if (numPlayers == 1 || !fogOfWar || scr50_HasOnlinePlayers() || (actionIndex == 2 && action.type != MIRACLE))
         {
-            scr50_MessageSlow(scrString("attack_successful"));
+            scrMessageSet(scrString("attack_successful"));
         }
         else
         {
-            scr50_MessageSlow(scrStringExt("look_away", !(player - 1) + 1, 42, 0));
+            scrMessageSet(scrStringExt("look_away", !(player - 1) + 1, 42, 0));
         }
     }
@@ -62,11 +63,11 @@
             defendLoc.units[i] += defendRunners[i];
         }
-        if (numPlayers == 1 || !fogOfWar || (actionIndex == 2 && action.type != MIRACLE))
+        if (numPlayers == 1 || !fogOfWar || scr50_HasOnlinePlayers() || (actionIndex == 2 && action.type != MIRACLE))
         {
-            scr50_MessageSlow(scrString("attack_failed"));
+            scrMessageSet(scrString("attack_failed"));
         }
         else
         {
-            scr50_MessageSlow(scrStringExt("look_away", !(player - 1) + 1, 42, 0));
+            scrMessageSet(scrStringExt("look_away", !(player - 1) + 1, 42, 0));
         }
     }
@@ -75,5 +76,5 @@
 {
     stateTimer++;
-    if (numPlayers == 1 || !fogOfWar)
+    if (numPlayers == 1 || !fogOfWar || scr50_HasOnlinePlayers())
     {
         if (stateTimer >= 152)
--- original/gml_Object_o50_Game_Other_20.gml
+++ patched/gml_Object_o50_Game_Other_20.gml
@@ -28,7 +28,7 @@
     mirChoice = false;
     recruitChioce = false;
-    if (numPlayers == 1)
+    if (numPlayers == 1 || (numPlayers == 2 && (scr50_IsOnlineLocalPlayer(winner) || scr50_IsOnlineRemotePlayer(winner))))
     {
-        if (winner == 1)
+        if ((numPlayers == 1 && winner == 1) || (numPlayers == 2 && scr50_IsOnlineLocalPlayer(winner)))
         {
             if (scenarioY != 2)
@@ -59,5 +59,5 @@
                 endSubMsg = scrString("enemy_lost_all_alt");
             }
-            if (!custom)
+            if (numPlayers == 1 && !custom)
             {
                 scenarioWins[scenarioY] = true;
@@ -96,5 +96,8 @@
                 endSubMsg = scrStringExt("lost_all_alt", "*", 40, 0);
             }
+            if (numPlayers == 1 && !custom)
+            {
             scrSaveGame(0);
+            }
         }
     }
--- original/gml_Object_o50_Location_Create_0.gml
+++ patched/gml_Object_o50_Location_Create_0.gml
@@ -14,5 +14,8 @@
 }
 owner = false;
-revealed = false;
+for (var p = 1; p <= 2; p++)
+{
+    revealed[p] = false;
+}
 frozen = false;
 terrain = o50_Game.GRASS;
--- original/gml_Object_oAudioHandler_Step_0.gml
+++ patched/gml_Object_oAudioHandler_Step_0.gml
@@ -14,5 +14,5 @@
             _inLibraryTitle = true;
         }
-        else if (state == STATE_PROFILE)
+        else if (state == STATE_PROFILE || state == STATE_ONLINE_LOBBY_LIST || state == STATE_ONLINE_LOBBY)
         {
             _inLibraryProfile = true;
@@ -83,8 +83,11 @@
 with (oPauseMenu)
 {
+    if (menuType == 0)
+    {
     _inPauseMenu = true;
     if (state == STATE_TERMINAL)
     {
         _inTerminal = true;
+        }
     }
 }
--- original/gml_Object_oConfirm_Step_1.gml
+++ patched/gml_Object_oConfirm_Step_1.gml
@@ -12,9 +12,9 @@
     exit;
 }
-if (pressLeft || pressRight || keyboard_check_pressed(vk_left) || keyboard_check_pressed(vk_right))
+if (pressLeft || pressRight)
 {
     sel = !sel;
 }
-if (fire2pressed || keyboard_check_pressed(vk_enter))
+if (fire2pressed)
 {
     choice = !sel;
--- original/gml_Object_oController_Create_0.gml
+++ patched/gml_Object_oController_Create_0.gml
@@ -4,4 +4,37 @@
     exit;
 }
+global.ONLINE_MAX_AUTOMATIC_DELAY = pfo_get_max_automatic_input_delay();
+if (!global.ONLINE_MAX_AUTOMATIC_DELAY)
+{
+    show_message("Error: PFO.dll does not exist or failed to load.\n\nPlease make sure you have copied PFO.dll into the same folder as data.win.");
+    game_end();
+}
 depth = -10000;
-totalTime = 0;
+global.ufoVersion = extension_get_option_value("PFO", "gameVersion");
+global.pfoVersion = extension_get_version("PFO");
+if (string_ends_with(global.ufoVersion, ".0"))
+{
+    global.ufoVersion = string_copy(global.ufoVersion, 1, string_length(global.ufoVersion) - 2);
+}
+if (string_ends_with(global.pfoVersion, ".0"))
+{
+    global.pfoVersion = string_copy(global.pfoVersion, 1, string_length(global.pfoVersion) - 2);
+}
+global.betaVersion = "v" + global.ufoVersion + " - PFO 50 v" + global.pfoVersion;
+alertMessageText = "";
+alertMessageTimer = -1;
+showFps = false;
+showPing = false;
+showFrame = false;
+showDelay = true;
+prevGame = 0;
+showingCatchupWarning = false;
+global.onlineSettings = undefined;
+global.onlineBackupDefaultLanguage = undefined;
+global.onlinePlayers = undefined;
+global.disableSettingOnlinePlayers = false;
+global.onlineRunUpdate = true;
+global.onlineSimultaneousTurns = false;
+global.onlineSpectatorPauseMenu = -4;
+scrInitCompatibilityInfo();
+scrInputClear();
--- original/gml_Object_oController_Other_3.gml
+++ patched/gml_Object_oController_Other_3.gml
@@ -1,2 +1,3 @@
+pfo_disconnect();
 steam_shutdown();
 scrQuit();
--- original/gml_Object_oController_Step_0.gml
+++ patched/gml_Object_oController_Step_0.gml
@@ -1,23 +0,0 @@
-if (delta_time < 2000000)
-{
-    totalTime += delta_time;
-}
-steam_update();
-if (!global.steamReady)
-{
-    if (steam_initialised())
-    {
-        if (steam_stats_ready())
-        {
-            global.steamReady = true;
-        }
-    }
-}
-if (steam_is_overlay_activated())
-{
-    global.steamOverlayActivated = true;
-}
-else
-{
-    global.steamOverlayActivated = false;
-}
--- original/gml_Object_oFFrame_Other_5.gml
+++ patched/gml_Object_oFFrame_Other_5.gml
@@ -1,2 +1,2 @@
-game_set_speed(fSpeed, gamespeed_fps);
+pfo_game_set_speed(fSpeed, 0);
 instance_destroy();
--- original/gml_Object_oFFrame_Step_0.gml
+++ patched/gml_Object_oFFrame_Step_0.gml
@@ -1,2 +1,2 @@
-game_set_speed(fSpeed, gamespeed_fps);
+pfo_game_set_speed(fSpeed, 0);
 instance_destroy();
--- original/gml_Object_oGame_Step_0.gml
+++ patched/gml_Object_oGame_Step_0.gml
@@ -37,12 +37,5 @@
 if (!global.attractModeLibrary)
 {
-    if (bPressStart[0] || global.steamOverlayActivated)
-    {
-        scrPause(0);
-    }
-    else if (bPressStart[1])
-    {
-        scrPause(1);
-    }
+    scrCheckPause();
 }
 if (global.attractModeLibrary)
--- original/gml_Object_oLibrary_Create_0.gml
+++ patched/gml_Object_oLibrary_Create_0.gml
@@ -1,6 +1,8 @@
 depth = -1;
-game_set_speed(global.STANDARD_FPS, gamespeed_fps);
+pfo_game_set_speed(global.STANDARD_FPS, 0);
 sortMap = ds_map_create();
 sortCategory = ds_list_create();
+lobbyList = ds_list_create();
+lobbyUsers = ds_list_create();
 if (room != rmLibrary)
 {
@@ -26,4 +28,7 @@
 STATE_DEBUG = 7;
 STATE_CORRUPT_FILE = 8;
+STATE_ONLINE_LOBBY_LIST = 9;
+STATE_ONLINE_LOBBY = 10;
+STATE_ONLINE_CONNECTING = 11;
 debugSelX = 0;
 debugSelY = 0;
@@ -31,9 +36,30 @@
 maxTime = false;
 cheevosResetAll = 0;
+automaticallyJoinLobby = UnknownEnum.Value_0;
 scrInitStateMachine();
 if (global.roomPrev == rmInit)
 {
-    if (global.SKIP_INTRO)
+    var _count = parameter_count() + 1;
+    var _connectLobby = UnknownEnum.Value_0;
+    for (var i = 1; i < _count; i++)
     {
+        if (parameter_string(i) == "+connect_lobby" && (i + 1) < _count)
+        {
+            try
+            {
+                _connectLobby = int64(parameter_string(i + 1));
+            }
+            catch (e)
+            {
+            }
+        }
+    }
+    if (_connectLobby != UnknownEnum.Value_0)
+    {
+        automaticallyJoinLobby = _connectLobby;
+        state = STATE_ONLINE_LOBBY_LIST;
+    }
+    else if (global.SKIP_INTRO)
+    {
         state = STATE_LOGO;
     }
@@ -43,6 +69,22 @@
     }
 }
+else if (global.roomPrev == rmLibrary)
+{
+    if (is_int64(global.SKIP_INTRO))
+    {
+        automaticallyJoinLobby = global.SKIP_INTRO;
+        state = STATE_ONLINE_LOBBY_LIST;
+    }
+    else if (global.SKIP_INTRO == 2)
+    {
+        state = STATE_ONLINE_CONNECTING;
+    }
 else
 {
+        state = STATE_LOGO;
+    }
+}
+else
+{
     state = STATE_PRE_GAMES_BUFFER;
 }
@@ -125,3 +167,9 @@
     scrLoadProfile(global.ARCADE_MODE_PROFILE);
     state = STATE_PRE_GAMES_BUFFER;
+}
+lastInputFrame = 0;
+
+enum UnknownEnum
+{
+    Value_0
 }
--- original/gml_Object_oLibrary_Destroy_0.gml
+++ patched/gml_Object_oLibrary_Destroy_0.gml
@@ -9,2 +9,12 @@
     sortCategory = -1;
 }
+if (ds_exists(lobbyList, ds_type_list))
+{
+    ds_list_destroy(lobbyList);
+    lobbyList = -1;
+}
+if (ds_exists(lobbyUsers, ds_type_list))
+{
+    ds_list_destroy(lobbyUsers);
+    lobbyUsers = -1;
+}
--- original/gml_Object_oLibrary_Draw_0.gml
+++ patched/gml_Object_oLibrary_Draw_0.gml
@@ -1,5 +1,5 @@
 var _xv = camera_get_view_x(view_get_camera(0));
 var _yv = camera_get_view_y(view_get_camera(0));
-if (state == STATE_GAMES && substate == SUB_INIT)
+if ((state == STATE_GAMES && substate == SUB_INIT) || global.attractModeLibraryTimer >= global.AM_LIB_TIME)
 {
     draw_clear_alpha(c_black, 1);
@@ -82,6 +82,8 @@
     if (substate == SUB_CRACK_WAIT)
     {
-        scrSetFont(global.fontHandwriting);
         draw_set_color(global.palette[17]);
+        scrSetFont(global.fontThinOutline);
+        draw_text(8, 200, global.betaVersion);
+        scrSetFont(global.fontHandwriting);
         var _str = scrStringVal("crack_launch_text", "[2");
         _str = string_line_breaks(_str, 16, 2);
@@ -127,8 +129,20 @@
     scrFillScreen(0);
     draw_sprite(sLaserX, 0, 192, 92);
+    var showOnlineLogo = true;
+    if (showOnlineLogo)
+    {
+        draw_sprite(sLaserXOnline, 0, 96, 12);
+    }
     scrSetFont(global.fontTall);
     draw_set_color(c_white);
+    if (showOnlineLogo)
+    {
+        draw_text_centered(192, 176, "PLAY FOREVER ONLINE", 8);
+    }
+    else
+    {
     scrStringDrawCE(192, 176, "laser_x_slogan", 2);
 }
+}
 if (state == STATE_INTRO)
 {
@@ -250,6 +264,9 @@
         fileHeight = 56;
         var f = menuSel + 1;
-        scrDrawProfile(left, top, f);
+        scrDrawProfile(left, top, f, 0);
     }
+    var onlineXoff = showOnlineMenu ? -38 : 0;
+    var onlineXshorten = showOnlineMenu ? -20 : 0;
+    var onlineXcenteroff = onlineXoff + (onlineXshorten / 2);
     if (substate != SUB_CONFIRM_DELETE && substate != SUB_CONFIRM_OVERWRITE)
     {
@@ -259,38 +276,67 @@
         {
             var menuSelXoff = 0;
-            if (i == menuSel)
+            if (onlineSel == 0 && i == menuSel)
             {
                 menuSelXoff = 4;
             }
             var f = i + 1;
-            scrDrawProfile(64 + menuSelXoff, top + (i * fileHeight), f);
+            scrDrawProfile(64 + menuSelXoff + onlineXoff, top + (i * fileHeight), f, onlineXshorten);
         }
+        if (showOnlineMenu)
+        {
+            var buttonLeft = 292;
+            var buttonWidth = 66;
+            var buttonHeight = 54;
+            scrSetFont(global.fontTall);
+            scrDrawMenuBorder(buttonLeft, 48 + (112 - buttonHeight), buttonWidth, buttonHeight);
+            draw_sprite(sOnlineIconLarge, 0, (buttonLeft + (buttonWidth / 2)) - 24, 50);
+            if (onlineSel == 1)
+            {
+                draw_set_color(global.palette[3]);
     }
-    var _handX = 50;
+            var buttonTextY = 117;
+            draw_text_centered(buttonLeft + (buttonWidth / 2), buttonTextY, "PLAY", 8);
+            draw_text_centered(buttonLeft + (buttonWidth / 2), buttonTextY + 16, "ONLINE", 8);
+            if (onlineSel == 1)
+            {
+                draw_sprite(sMenuHand, 0, buttonLeft - 18, buttonTextY + 9);
+            }
+            scrSetFont(global.fontDefault);
+            draw_set_color(c_white);
+        }
+    }
+    var _handX = 50 + onlineXoff;
     if (substate == SUB_CLEANUP || substate == SUB_LOAD)
     {
-        scrDrawMenuBorder(96, 8, 192, 24);
+        scrDrawMenuBorder(96 + onlineXcenteroff, 8, 192, 24);
         scrSetFont(global.fontDefault);
-        scrStringDrawCE(192, 16, "prof_head_select_profile", 4);
-        if (menuSel < global.NUM_PROFILES_ACCESSIBLE)
+        if (showOnlineMenu)
         {
+            draw_text_centered(192 + onlineXcenteroff, 16, "LOCAL PROFILES", 8);
+        }
+        else
+        {
+            scrStringDrawCE(192 + onlineXcenteroff, 16, "prof_head_select_profile", 4);
+        }
+        if (onlineSel == 0 && menuSel < global.NUM_PROFILES_ACCESSIBLE)
+        {
             draw_sprite(sMenuHand, 0, _handX, top + (menuSel * fileHeight) + 9);
         }
         var selPos = global.NUM_PROFILES_ACCESSIBLE;
         var yPos = top + (selPos * fileHeight);
-        draw_sprite(sMenuCopy, 0, 72 + ((menuSel == selPos) * 10), yPos + 8);
-        if (menuSel == selPos)
+        draw_sprite(sMenuCopy, 0, 72 + ((onlineSel == 0 && menuSel == selPos) * 10) + onlineXoff, yPos + 8);
+        if (onlineSel == 0 && menuSel == selPos)
         {
             scrSetFont(global.fontTall);
-            scrStringDraw(104, yPos, "prof_menu_copy");
+            scrStringDraw(104 + onlineXoff, yPos, "prof_menu_copy");
             draw_sprite(sMenuHand, 0, _handX, yPos + 1);
         }
         selPos = global.NUM_PROFILES_ACCESSIBLE + 1;
         yPos = (top + (selPos * fileHeight)) - 16;
-        draw_sprite(sMenuBomb, 0, 72 + ((menuSel == selPos) * 10), yPos + 8);
-        if (menuSel == selPos)
+        draw_sprite(sMenuBomb, 0, 72 + ((onlineSel == 0 && menuSel == selPos) * 10) + onlineXoff, yPos + 8);
+        if (onlineSel == 0 && menuSel == selPos)
         {
             scrSetFont(global.fontTall);
-            scrStringDraw(104, yPos, "prof_menu_delete");
+            scrStringDraw(104 + onlineXoff, yPos, "prof_menu_delete");
             draw_sprite(sMenuHand, 0, _handX, yPos + 1);
         }
@@ -298,15 +344,15 @@
     if (substate == SUB_DELETE)
     {
-        scrDrawMenuBorder(96, 8, 192, 24);
+        scrDrawMenuBorder(96 + onlineXcenteroff, 8, 192, 24);
         scrSetFont(global.fontDefault);
-        scrStringDrawCE(192, 16, "prof_head_delete", 4);
+        scrStringDrawCE(192 + onlineXcenteroff, 16, "prof_head_delete", 4);
         if (menuSel < global.NUM_PROFILES_ACCESSIBLE)
         {
-            draw_sprite(sMenuBomb, 0, 48, top + (menuSel * fileHeight) + 16);
+            draw_sprite(sMenuBomb, 0, _handX + 2, top + (menuSel * fileHeight) + 16);
         }
         var selPos = global.NUM_PROFILES_ACCESSIBLE;
         var yPos = top + (selPos * fileHeight);
         scrSetFont(global.fontTall);
-        scrStringDraw(72 + ((menuSel == selPos) * 8), yPos, "prof_menu_back");
+        scrStringDraw(72 + ((menuSel == selPos) * 8) + onlineXoff, yPos, "prof_menu_back");
         if (menuSel == selPos)
         {
@@ -316,15 +362,15 @@
     if (substate == SUB_COPY_FROM)
     {
-        scrDrawMenuBorder(96, 8, 192, 24);
+        scrDrawMenuBorder(96 + onlineXcenteroff, 8, 192, 24);
         scrSetFont(global.fontDefault);
-        scrStringDrawCE(192, 16, "prof_head_copy_from", 4);
+        scrStringDrawCE(192 + onlineXcenteroff, 16, "prof_head_copy_from", 4);
         if (menuSel < global.NUM_PROFILES_ACCESSIBLE)
         {
-            draw_sprite(sMenuCopy, 0, 48, top + (menuSel * fileHeight) + 16);
+            draw_sprite(sMenuCopy, 0, _handX + 2, top + (menuSel * fileHeight) + 16);
         }
         var selPos = global.NUM_PROFILES_ACCESSIBLE;
         var yPos = top + (selPos * fileHeight);
         scrSetFont(global.fontTall);
-        scrStringDraw(72 + ((menuSel == selPos) * 8), yPos, "prof_menu_back");
+        scrStringDraw(72 + ((menuSel == selPos) * 8) + onlineXoff, yPos, "prof_menu_back");
         if (menuSel == selPos)
         {
@@ -334,16 +380,16 @@
     if (substate == SUB_COPY_TO)
     {
-        scrDrawMenuBorder(96, 8, 192, 24);
+        scrDrawMenuBorder(96 + onlineXcenteroff, 8, 192, 24);
         scrSetFont(global.fontDefault);
-        scrStringDrawCE(192, 16, "prof_head_copy_to", 4);
+        scrStringDrawCE(192 + onlineXcenteroff, 16, "prof_head_copy_to", 4);
         if (menuSel < global.NUM_PROFILES_ACCESSIBLE)
         {
             draw_sprite(sMenuHand, 0, _handX, top + (menuSel * fileHeight) + 9);
         }
-        draw_sprite(sMenuCopy, 0, 48, top + (copyFrom * fileHeight) + 16);
+        draw_sprite(sMenuCopy, 0, _handX + 2, top + (copyFrom * fileHeight) + 16);
         var selPos = global.NUM_PROFILES_ACCESSIBLE;
         var yPos = top + (selPos * fileHeight);
         scrSetFont(global.fontTall);
-        scrStringDraw(72 + ((menuSel == selPos) * 8), yPos, "prof_menu_back");
+        scrStringDraw(72 + ((menuSel == selPos) * 8) + onlineXoff, yPos, "prof_menu_back");
         if (menuSel == selPos)
         {
@@ -973,3 +1019,15 @@
 {
     scrFillScreen(0);
+}
+else if (state == STATE_ONLINE_LOBBY_LIST)
+{
+    scrLibraryOnlineStateDraw();
+}
+else if (state == STATE_ONLINE_LOBBY)
+{
+    scrLibraryOnlineLobbyDraw();
+}
+else if (state == STATE_ONLINE_CONNECTING)
+{
+    scrLibraryOnlineConnectingDraw();
 }
--- original/gml_Object_oLibrary_Other_12.gml
+++ patched/gml_Object_oLibrary_Other_12.gml
@@ -313,5 +313,5 @@
             break;
     }
-    if ((fire2pressed || pressStart || keyboard_check_pressed(vk_space) || keyboard_check_pressed(vk_enter)) && (INTRO_TIME > 10 || INTRO_SCENE > 1))
+    if ((fire2pressed || pressStart) && (INTRO_TIME > 10 || INTRO_SCENE > 1))
     {
         if (INTRO_SCENE < 24)
--- original/gml_Object_oLibrary_Other_14.gml
+++ patched/gml_Object_oLibrary_Other_14.gml
@@ -15,4 +15,26 @@
     parallaxCounter += PARALLAX_SPEED;
 }
+if (pfo_is_online() && lastInputFrame != pfo_get_frame())
+{
+    lastInputFrame = pfo_get_frame();
+    var _player = pfo_client_get_player_index(0);
+    if (_player < 0)
+    {
+        var _count = pfo_get_client_count();
+        var _newOnlinePlayers = [];
+        for (var i = 0; i < _count; i++)
+        {
+            _newOnlinePlayers[i] = pfo_player_get_client_index(i);
+        }
+        _newOnlinePlayers[0] = 0;
+        scrSetOnlinePlayers(_newOnlinePlayers);
+        scrInputClear();
+        global.disableSettingOnlinePlayers = true;
+    }
+    else
+    {
+        scrGetInput(_player);
+    }
+}
 if (substate == SUB_INIT)
 {
@@ -72,4 +94,6 @@
     menuSel = profileSelFinal;
     menuSelOrig = 0;
+    onlineSel = 0;
+    showOnlineMenu = !pfo_is_online();
     if (subPrev == SUB_COPYING)
     {
@@ -95,8 +119,21 @@
 else if (substate == SUB_LOAD)
 {
+    showOnlineMenu = !pfo_is_online();
+    if (!showOnlineMenu)
+    {
+        onlineSel = 0;
+    }
+    var _startOnlineSel = onlineSel;
+    if (showOnlineMenu)
+    {
+        onlineSel = scrHorzNav(onlineSel, 1);
+    }
     menuSelBot = global.NUM_PROFILES_ACCESSIBLE + 1;
     var _startSel = menuSel;
+    if (onlineSel == 0)
+    {
     menuSel = scrVertNav(menuSel, menuSelBot);
-    if (menuSel != _startSel)
+    }
+    if (onlineSel != _startOnlineSel || menuSel != _startSel)
     {
         scrSfxLibrary(soundProfileNavi);
@@ -104,7 +141,12 @@
     if (fire2pressed)
     {
-        if (menuSel < global.NUM_PROFILES_ACCESSIBLE)
+        if (onlineSel == 1)
         {
             scrSfxLibrary(soundProfileLoad);
+            scrSwitchState(STATE_ONLINE_LOBBY_LIST);
+        }
+        else if (menuSel < global.NUM_PROFILES_ACCESSIBLE)
+        {
+            scrSfxLibrary(soundProfileLoad);
             if (scrLoadProfile(menuSel + 1))
             {
@@ -217,7 +259,7 @@
 {
     var filename = string_replace(global.SAVE_FILE, "*", string(menuSel + 1));
-    if (file_exists(filename))
+    if (pfo_file_exists(filename))
     {
-        file_delete(filename);
+        pfo_file_delete(filename);
     }
     for (var i = 0; i < 22; i++)
@@ -361,12 +403,13 @@
 {
     var fileFrom = string_replace(global.SAVE_FILE, "*", string(copyFrom + 1));
-    var fileTo = string_replace(global.SAVE_FILE, "*", string(menuSel + 1));
-    if (file_exists(fileTo))
+    var fileTo = string_replace(global.SAVE_FILE, "*", string(copyTo + 1));
+    pfo_file_exists(fileFrom);
+    if (pfo_file_exists(fileTo))
     {
-        file_delete(fileTo);
+        pfo_file_delete(fileTo);
     }
-    if (file_exists(fileFrom))
+    if (pfo_file_exists(fileFrom))
     {
-        file_copy(fileFrom, fileTo);
+        pfo_file_copy(fileFrom, fileTo);
     }
     scrOpenFileManual(copyTo + 1);
--- original/gml_Object_oLibrary_Other_15.gml
+++ patched/gml_Object_oLibrary_Other_15.gml
@@ -131,4 +131,5 @@
     if (global.attractModeLibraryTimer++ >= global.AM_LIB_TIME)
     {
+        global.attractModeLibraryTimer = global.AM_LIB_TIME;
         event_user(15);
         exit;
--- original/gml_Object_oLibrary_Other_17.gml
+++ patched/gml_Object_oLibrary_Other_17.gml
@@ -87,4 +87,8 @@
         else if (debugSelY == 4)
         {
+            if (pfo_is_online())
+            {
+                exit;
+            }
             if (cheevosResetAll == 0)
             {
--- original/gml_Object_oLibrary_Other_25.gml
+++ patched/gml_Object_oLibrary_Other_25.gml
@@ -9,10 +9,12 @@
 {
     load_datagroup(_gameID, true, false);
+    scrPetCleanup();
     attractModeInitState = 1;
 }
-if (attractModeInitState == 1)
+else if (attractModeInitState == 1)
 {
     if (texturegroup_get_status(global.TEXTUREGROUP[_gameID]) != 3)
     {
+        global.onlineRunUpdate = false;
         exit;
     }
--- original/gml_Object_oLibrary_Step_1.gml
+++ patched/gml_Object_oLibrary_Step_1.gml
@@ -21,4 +21,8 @@
     pauseAvail = false;
 }
+if (state == STATE_ONLINE_LOBBY_LIST || state == STATE_ONLINE_LOBBY || state == STATE_ONLINE_CONNECTING)
+{
+    pauseAvail = false;
+}
 if (global.all50 > 0)
 {
@@ -30,19 +34,20 @@
     scrInputClear();
 }
-if (pauseAvail && (pressStart || global.steamOverlayActivated))
+if (pauseAvail)
 {
-    if (bPressStart[0] || global.steamOverlayActivated)
+    scrCheckPause();
+    if (global.paused)
     {
-        scrPause(0);
-    }
-    else if (bPressStart[1])
-    {
-        scrPause(1);
-    }
     exit;
 }
+}
 scrRunStateMachine();
 if (global.currFile >= 1 && global.currFile <= global.NUM_PROFILES_ACCESSIBLE)
 {
     global.backupTimer++;
+}
+if (state != STATE_PROFILE && state != STATE_ONLINE_CONNECTING && global.disableSettingOnlinePlayers)
+{
+    global.disableSettingOnlinePlayers = false;
+    scrSetOnlinePlayers(global.onlinePlayers);
 }
--- original/gml_Object_oOkay_Create_0.gml
+++ patched/gml_Object_oOkay_Create_0.gml
@@ -2,4 +2,5 @@
 okay = false;
 delayInput = true;
+menuType = 0;
+inEndDraw = false;
 scrInputClear();
-depth = -1001;
--- original/gml_Object_oOkay_Draw_0.gml
+++ patched/gml_Object_oOkay_Draw_0.gml
@@ -1,8 +1,33 @@
+if (menuType == 1 && !inEndDraw)
+{
+    exit;
+}
+var origColor = draw_get_color();
 var tx = camera_get_view_x(view_get_camera(0)) + 192;
 var ty = camera_get_view_y(view_get_camera(0)) + 108;
-scrDrawMenuBorderCentered(64, 256, 64);
+var drawExtra = !is_undefined(ours);
+var extraHeight = drawExtra ? 24 : 0;
+var extraOffset = extraHeight / 2;
+var extraBottomOffset = drawExtra ? (extraHeight - 8) : 0;
+scrDrawMenuBorderCentered(64 - extraOffset, 256, 64 + extraHeight);
 var displayText = string_line_breaks(str, 24, 2);
-draw_text_centered(tx, ty - 32, string_even(displayText[0], 1), 8);
-draw_text_centered(tx, ty - 16, string_even(displayText[1], 1), 8);
+draw_text_centered(tx, ty - 32 - extraOffset, string_even(displayText[0], 1), 8);
+draw_text_centered(tx, ty - 16 - extraOffset, string_even(displayText[1], 1), 8);
+if (drawExtra)
+{
+    var s1 = string_even("OURS:   " + ours.ufoVersion + " - " + ours.pfoVersion, 1);
+    var s2 = string_even("THEIRS: " + theirs.ufoVersion + " - " + theirs.pfoVersion, 1);
+    while (string_length(s1) < string_length(s2))
+    {
+        s1 += " ";
+    }
+    while (string_length(s2) < string_length(s1))
+    {
+        s2 += " ";
+    }
+    draw_text_centered(tx, (ty + extraBottomOffset) - 24, s1, 8);
+    draw_text_centered(tx, (ty + extraBottomOffset) - 16, s2, 8);
+}
 draw_set_color(global.palette[3]);
-draw_text(tx - 8, ty, scrStringManual("option_ok", 0));
+draw_text(tx - 8, ty + extraBottomOffset, scrStringManual("option_ok", 0));
+draw_set_color(origColor);
--- original/gml_Object_oOkay_Step_0.gml
+++ patched/gml_Object_oOkay_Step_0.gml
@@ -6,5 +6,5 @@
 if (!delayInput)
 {
-    scrGetInputMerged();
+    scrGetInputMerged(inputMode);
 }
 else
@@ -12,5 +12,5 @@
     delayInput = false;
 }
-if (fire2pressed || keyboard_check_pressed(vk_enter))
+if (fire2pressed || ((inputMode & UnknownEnum.Value_1) != 0 && keyboard_check_pressed(vk_enter)))
 {
     okay = true;
@@ -18,2 +18,7 @@
 x = camera_get_view_x(view_get_camera(0)) + 192;
 y = camera_get_view_y(view_get_camera(0)) + 108;
+
+enum UnknownEnum
+{
+    Value_1 = 1
+}
--- original/gml_Object_oPauseMenu_Create_0.gml
+++ patched/gml_Object_oPauseMenu_Create_0.gml
@@ -1,12 +1,7 @@
-if (!global.paused)
+if (!variable_instance_exists(id, "menuType"))
 {
     instance_destroy();
     exit;
 }
-if (instance_number(oPauseMenu) > 1)
-{
-    instance_destroy();
-    exit;
-}
 STATE_ROOT_LIBRARY = 0;
 STATE_ROOT_GAME = 1;
@@ -24,5 +19,12 @@
 STATE_GAMEINFO = 13;
 STATE_BUTTON_LAYOUT = 14;
+STATE_ONLINE = 15;
 scrInitStateMachine();
+if (menuType != 0)
+{
+    state = STATE_ROOT_LIBRARY;
+}
+else
+{
 if (scrInGame())
 {
@@ -116,4 +118,5 @@
     }
 }
+}
 MENU_WIDTH = 240;
 MENU_HEIGHT = 160;
@@ -122,4 +125,6 @@
 MENU_BIG_SPACER = 20;
 scrMenuInit();
+if (menuType == 0)
+{
 if (global.cherryWins >= 50)
 {
@@ -142,4 +147,5 @@
     global.libraryBG = 0;
 }
+}
 currentSound = -1;
 currentSoundSet = 0;
@@ -158,3 +164,54 @@
 inputListenerTimer = 0;
 padCount = scrGetJoyCount();
-depth = -1000;
+if (menuType == 0)
+{
+    localInputDisabled = function()
+    {
+        return player != -1 && pfo_is_online() && pfo_client_get_player_index() != player;
+    };
+    
+    usingLocalInput = function()
+    {
+        return !pfo_is_online() || (state >= STATE_KEYCONFIG && state <= STATE_AUDIO) || (state == STATE_UNPAUSE && statePrev >= STATE_KEYCONFIG && statePrev <= STATE_AUDIO);
+    };
+    
+    sendCommandInProgress = pfo_send_command_in_progress;
+    sendCommand = pfo_send_command;
+    receiveCommand = pfo_receive_command;
+}
+else
+{
+    localInputDisabled = function()
+    {
+        return false;
+    };
+    
+    usingLocalInput = function()
+    {
+        return true;
+    };
+    
+    sendCommandInProgress = function()
+    {
+        commandSent = UnknownEnum.Value_0;
+        return false;
+    };
+    
+    sendCommand = function(arg0)
+    {
+        commandSent = arg0;
+    };
+    
+    receiveCommand = function(arg0)
+    {
+        return commandSent == arg0;
+    };
+    
+    commandSent = UnknownEnum.Value_0;
+}
+inEndDraw = false;
+
+enum UnknownEnum
+{
+    Value_0
+}
--- original/gml_Object_oPauseMenu_Draw_0.gml
+++ patched/gml_Object_oPauseMenu_Draw_0.gml
@@ -1,2 +1,6 @@
+if (menuType == 1 && !inEndDraw)
+{
+    exit;
+}
 if (state == STATE_TERMINAL && substate == SUB_INIT)
 {
@@ -4,4 +8,8 @@
     exit;
 }
+var _origFont = draw_get_font();
+var _origColor = draw_get_color();
+var _origHAlign = draw_get_halign();
+var _origVAlign = draw_get_valign();
 var _xview = camera_get_view_x(view_get_camera(0));
 var _yview = camera_get_view_y(view_get_camera(0));
@@ -14,6 +22,12 @@
     var LEFT_TEXT_X = ((_xview + 192) - floor(MENU_WIDTH / 2)) + 16;
     var RIGHT_TEXT_X = (_xview + 192 + floor(MENU_WIDTH / 2)) - 16;
-    if (scrInGame())
+    if (menuType == 1)
     {
+        draw_set_alpha(0.5);
+        draw_rectangle_color(_xview, _yview, _xview + 384, _yview + 216, c_black, c_black, c_black, c_black, 0);
+        draw_set_alpha(1);
+    }
+    else if (scrInGame())
+    {
         scrFillScreen(0);
         draw_sprite(sPauseMenuBG, 0, _xview, _yview);
@@ -31,10 +45,26 @@
     }
     scrDrawMenuBorderCentered(32, MENU_WIDTH, MENU_HEIGHT);
+    if (menuType == 1)
+    {
+        draw_set_color(c_white);
+    }
+    else
+    {
     draw_set_color(global.palette[16]);
+    }
     scrSetFont(global.fontTall);
     scrDrawTextCentered(string_even(menuHeader, 3), _xview, _yview + 8, 8, 384);
     scrFontDefault();
-    if (is_array(itemType))
+    if (usingLocalInput() && localInputDisabled())
     {
+        scrSetFont(global.fontTall);
+        draw_set_color(global.palette[0]);
+        var _textLines = string_line_breaks("PLAYER " + string(player + 1) + " CHANGING LOCAL SETTINGS", 20, 2);
+        draw_text_centered(_xview + 192, TOP + 48, _textLines[0], 8);
+        draw_text_centered(_xview + 192, TOP + 64, _textLines[1], 8);
+        scrFontDefault();
+    }
+    else if (is_array(itemType))
+    {
         var _yPos = TOP;
         for (var i = 0; i < array_length(itemType); i++)
@@ -129,8 +159,15 @@
                 if (_selBarIconID > -1)
                 {
+                    if (_selBarIconID == 7 && pfo_is_online())
+                    {
+                        draw_sprite(sPauseMenuOnlineIcon, 0, _xview + _selBarX + 4, _yPos);
+                    }
+                    else
+                    {
                     draw_sprite(sPauseMenuIcons, _selBarIconID, _xview + _selBarX + 4, _yPos);
                 }
+                }
                 draw_set_color(global.palette[3]);
-                if (itemType[menuSel] == TYPE_DUAL || itemType[menuSel] == TYPE_DUAL_INT || itemType[menuSel] == TYPE_PLAYER || itemType[menuSel] == TYPE_METER || itemType[menuSel] == TYPE_DUAL_CENTER)
+                if (itemType[menuSel] == TYPE_DUAL || itemType[menuSel] == TYPE_DUAL_INT || itemType[menuSel] == TYPE_PLAYER || itemType[menuSel] == TYPE_METER || itemType[menuSel] == TYPE_DUAL_CENTER || itemType[menuSel] == TYPE_DUAL_ONLINE_PLAYER)
                 {
                     var _values = itemValues[i];
@@ -185,5 +222,5 @@
                 _yPos += 20;
             }
-            else if (itemType[i] == TYPE_DUAL || itemType[i] == TYPE_DUAL_INT)
+            else if (itemType[i] == TYPE_DUAL || itemType[i] == TYPE_DUAL_INT || itemType[i] == TYPE_DUAL_ONLINE_PLAYER)
             {
                 draw_text(LEFT_TEXT_X, _yPos, itemName[i]);
@@ -270,5 +307,12 @@
                 draw_text(LEFT_TEXT_X, _yPos, itemName[i]);
                 draw_set_halign(fa_right);
+                if (!pfo_is_online())
+                {
                 draw_sprite(sMenuP1P2, itemIndex[i], RIGHT_TEXT_X - 32, TOP);
+                }
+                else
+                {
+                    draw_sprite_part(sMenuP1P2, 0, 0, 0, 16, 8, RIGHT_TEXT_X - 16, TOP);
+                }
                 draw_set_halign(fa_left);
                 _yPos += 12;
@@ -293,19 +337,21 @@
         scrFontDefault();
     }
+    if (!localInputDisabled())
+    {
     if (state == STATE_ROOT_LIBRARY || state == STATE_ROOT_GAME)
     {
-        if (scrInGame())
+            if (menuType != 1 && scrInGame())
         {
             draw_set_color(global.palette[16]);
         }
-        scrDrawTextInput(_xTip, _yTip, scrStringVal("menu_tip_confirm", "[2]") + "    " + scrStringVal("menu_tip_back", "[1]"), 0, 0, 2);
+            scrDrawTextInput(_xTip, _yTip, scrStringValManual("menu_tip_confirm", 0, "[2]") + "    " + scrStringValManual("menu_tip_back", 0, "[1]"), 0, 0, 2);
     }
     if (state == STATE_OPTIONS)
     {
-        if (scrInGame())
+            if (menuType != 1 && scrInGame())
         {
             draw_set_color(global.palette[16]);
         }
-        scrDrawTextInput(_xTip, _yTip, scrStringVal("menu_tip_confirm", "[2]") + "    " + scrStringVal("menu_tip_back", "[1]"), 0, 0, 2);
+            scrDrawTextInput(_xTip, _yTip, scrStringValManual("menu_tip_confirm", 0, "[2]") + "    " + scrStringValManual("menu_tip_back", 0, "[1]"), 0, 0, 2);
     }
     if (state == STATE_VIDEO)
@@ -313,17 +359,17 @@
         if (!global.ON_STEAM_DECK)
         {
-            if (scrInGame())
+                if (menuType != 1 && scrInGame())
             {
                 draw_set_color(global.palette[16]);
             }
-            scrDrawTextInput(_xTip, _yTip, scrStringVal("menu_tip_restore_defaults", "[F]"), 0, 1, 4);
+                scrDrawTextInput(_xTip, _yTip, scrStringValManual("menu_tip_restore_defaults", 0, "[F]"), 0, 1, 4);
         }
         else
         {
-            if (scrInGame())
+                if (menuType != 1 && scrInGame())
             {
                 draw_set_color(global.palette[16]);
             }
-            scrDrawTextInput(_xTip, _yTip, scrStringVal("menu_tip_confirm", "[2]") + "    " + scrStringVal("menu_tip_back", "[1]"), 0, 0, 2);
+                scrDrawTextInput(_xTip, _yTip, scrStringValManual("menu_tip_confirm", 0, "[2]") + "    " + scrStringValManual("menu_tip_back", 0, "[1]"), 0, 0, 2);
         }
         if (menuSel == OP_SCALE)
@@ -335,21 +381,21 @@
         }
     }
-    if (state == STATE_AUDIO)
+        if (state == STATE_AUDIO || state == STATE_ONLINE)
     {
         if (!global.ON_STEAM_DECK)
         {
-            if (scrInGame())
+                if (menuType != 1 && scrInGame())
             {
                 draw_set_color(global.palette[16]);
             }
-            scrDrawTextInput(_xTip, _yTip, scrStringVal("menu_tip_restore_defaults", "[F]"), 0, 1, 4);
+                scrDrawTextInput(_xTip, _yTip, scrStringValManual("menu_tip_restore_defaults", 0, "[F]"), 0, 1, 4);
         }
         else
         {
-            if (scrInGame())
+                if (menuType != 1 && scrInGame())
             {
                 draw_set_color(global.palette[16]);
             }
-            scrDrawTextInput(_xTip, _yTip, scrStringVal("menu_tip_confirm", "[2]") + "    " + scrStringVal("menu_tip_back", "[1]"), 0, 0, 2);
+                scrDrawTextInput(_xTip, _yTip, scrStringValManual("menu_tip_confirm", 0, "[2]") + "    " + scrStringValManual("menu_tip_back", 0, "[1]"), 0, 0, 2);
         }
     }
@@ -358,18 +404,18 @@
         if (!global.ON_STEAM_DECK)
         {
-            if (scrInGame())
+                if (menuType != 1 && scrInGame())
             {
                 draw_set_color(global.palette[16]);
             }
-            scrDrawTextInput(_xTip, _yTip - 4, scrStringVal("menu_tip_change_key", "[e]"), 0, 1, 2);
-            scrDrawTextInput(_xTip, _yTip + 4, scrStringVal("menu_tip_restore_defaults", "[F]"), 0, 1, 4);
+                scrDrawTextInput(_xTip, _yTip - 4, scrStringValManual("menu_tip_change_key", 0, "[e]"), 0, 1, 2);
+                scrDrawTextInput(_xTip, _yTip + 4, scrStringValManual("menu_tip_restore_defaults", 0, "[F]"), 0, 1, 4);
         }
         else
         {
-            if (scrInGame())
+                if (menuType != 1 && scrInGame())
             {
                 draw_set_color(global.palette[16]);
             }
-            scrDrawTextInput(_xTip, _yTip, scrStringVal("menu_tip_confirm", "[2]") + "    " + scrStringVal("menu_tip_back", "[1]"), 0, 0, 2);
+                scrDrawTextInput(_xTip, _yTip, scrStringValManual("menu_tip_confirm", 0, "[2]") + "    " + scrStringValManual("menu_tip_back", 0, "[1]"), 0, 0, 2);
         }
         if (substate == SUB_GET_KEY)
@@ -378,5 +424,5 @@
             var ty = _yview + 108;
             scrDrawMenuBorderCentered(80, 288, 48);
-            scrDrawTextInput(tx, ty - 8, scrStringVal("menu_misc_key_waiting", strInput), 0, 0, 2);
+                scrDrawTextInput(tx, ty - 8, scrStringValManual("menu_misc_key_waiting", 0, strInput), 0, 0, 2);
         }
     }
@@ -385,34 +431,34 @@
         if (global.inputFocus == 1)
         {
-            if (scrInGame())
+                if (menuType != 1 && scrInGame())
             {
                 draw_set_color(global.palette[16]);
             }
-            scrDrawTextInput(_xTip, _yTip - 4, scrStringVal("menu_tip_change_button", "[e]"), 0, 1, 2);
-            scrDrawTextInput(_xTip, _yTip + 4, scrStringVal("menu_tip_restore_defaults", "[F]"), 0, 1, 4);
+                scrDrawTextInput(_xTip, _yTip - 4, scrStringValManual("menu_tip_change_button", 0, "[e]"), 0, 1, 2);
+                scrDrawTextInput(_xTip, _yTip + 4, scrStringValManual("menu_tip_restore_defaults", 0, "[F]"), 0, 1, 4);
         }
         else if (global.inputFocus == 2)
         {
-            if (scrInGame())
+                if (menuType != 1 && scrInGame())
             {
                 draw_set_color(global.palette[16]);
             }
-            var _len = string_length(scrString("menu_tip_restore_joy_defaults"));
-            if (player < 0 || player > 1)
+                var _len = string_length(scrStringManual("menu_tip_restore_joy_defaults", 0));
+                if (player < 0 || player >= array_length(global.joyType) || pfo_is_online())
             {
-                draw_text_with_sprites(_xTip - floor((8 * _len) / 2), _yTip, scrString("menu_tip_restore_joy_defaults"), global.JOY_TYPE_SPRITE[global.joyType[0]], 8, global.JOY_TYPE_SPRITE[global.joyType[0]], 9);
+                    draw_text_with_sprites(_xTip - floor((8 * _len) / 2), _yTip, scrStringManual("menu_tip_restore_joy_defaults", 0), global.JOY_TYPE_SPRITE[global.joyType[0]], 8, global.JOY_TYPE_SPRITE[global.joyType[0]], 9);
             }
             else
             {
-                draw_text_with_sprites(_xTip - floor((8 * _len) / 2), _yTip, scrString("menu_tip_restore_joy_defaults"), global.JOY_TYPE_SPRITE[global.joyType[player]], 8, global.JOY_TYPE_SPRITE[global.joyType[player]], 9);
+                    draw_text_with_sprites(_xTip - floor((8 * _len) / 2), _yTip, scrStringManual("menu_tip_restore_joy_defaults", 0), global.JOY_TYPE_SPRITE[global.joyType[player]], 8, global.JOY_TYPE_SPRITE[global.joyType[player]], 9);
             }
         }
         else
         {
-            if (scrInGame())
+                if (menuType != 1 && scrInGame())
             {
                 draw_set_color(global.palette[16]);
             }
-            scrDrawTextInput(_xTip, _yTip, scrStringVal("menu_tip_confirm", "[2]") + "    " + scrStringVal("menu_tip_back", "[1]"), 0, 0, 2);
+                scrDrawTextInput(_xTip, _yTip, scrStringValManual("menu_tip_confirm", 0, "[2]") + "    " + scrStringValManual("menu_tip_back", 0, "[1]"), 0, 0, 2);
         }
         if (substate == SUB_GET_JOY)
@@ -421,5 +467,5 @@
             var ty = _yview + 108;
             scrDrawMenuBorderCentered(80, 288, 48);
-            scrDrawTextInput(tx, ty - 8, scrStringVal("menu_misc_joy_waiting", strInput), 0, 0, 2);
+                scrDrawTextInput(tx, ty - 8, scrStringValManual("menu_misc_joy_waiting", 0, strInput), 0, 0, 2);
         }
         if (substate == SUB_GET_SLOT)
@@ -435,10 +481,11 @@
     if (state == STATE_BUTTON_LAYOUT)
     {
-        if (scrInGame())
+            if (menuType != 1 && scrInGame())
         {
             draw_set_color(global.palette[16]);
         }
-        scrDrawTextInput(_xTip, _yTip, scrStringVal("menu_tip_confirm", "[2]") + "    " + scrStringVal("menu_tip_back", "[1]"), 0, 0, 2);
+            scrDrawTextInput(_xTip, _yTip, scrStringValManual("menu_tip_confirm", 0, "[2]") + "    " + scrStringValManual("menu_tip_back", 0, "[1]"), 0, 0, 2);
     }
+    }
     if (state == STATE_MILESTONES)
     {
@@ -486,5 +533,5 @@
         }
         draw_set_color(c_white);
-        draw_text_centered(_xTip, _yTip, scrStringVal("menu_misc_attained", milestonesAchieved, global.ACH_TOTAL), 8);
+        draw_text_centered(_xTip, _yTip, scrStringValManual("menu_misc_attained", 0, milestonesAchieved, global.ACH_TOTAL), 8);
     }
     if (state == STATE_GAMEINFO)
@@ -493,12 +540,15 @@
         for (var i = 0; i <= (array_length(ctrlContent) - 1); i++)
         {
-            scrDrawTextInput(LEFT_TEXT_X, TOP + _infoY, ctrlContent[i], player, 0, 0);
+            scrDrawTextInput(LEFT_TEXT_X, TOP + _infoY, ctrlContent[i], max(0, player), 0, 0);
             _infoY += 8;
         }
     }
+    if (!localInputDisabled())
+    {
     if (state == STATE_PROFILESET)
     {
-        scrDrawTextInput(_xTip, _yTip, scrStringVal("menu_tip_confirm", "[2]") + "    " + scrStringVal("menu_tip_back", "[1]"), 0, 0, 2);
+            scrDrawTextInput(_xTip, _yTip, scrStringValManual("menu_tip_confirm", 0, "[2]") + "    " + scrStringValManual("menu_tip_back", 0, "[1]"), 0, 0, 2);
     }
+    }
     if (state == STATE_NAME)
     {
@@ -594,7 +644,10 @@
         }
         scrFontDefault();
-        scrDrawTextInput(_xTip, _yTip, scrStringVal("menu_tip_cancel", "[1]"), 0, 0, 2);
+        if (!localInputDisabled())
+        {
+            scrDrawTextInput(_xTip, _yTip, scrStringValManual("menu_tip_cancel", 0, "[1]"), 0, 0, 2);
     }
 }
+}
 if (state == STATE_TERMINAL)
 {
@@ -636,22 +689,22 @@
                 if (global.currGameID == 49 && global.cheatID == 3)
                 {
-                    draw_text(_x, _y, "[ " + scrStringManual("game_internal_name_color_racer", 0) + scrString("term_info_file_extension") + " ]");
+                    draw_text(_x, _y, "[ " + scrStringManual("game_internal_name_color_racer", 0) + scrStringManual("term_info_file_extension", 0) + " ]");
                 }
                 else if (global.currGameID == 3 && global.cheatID == 2)
                 {
-                    draw_text(_x, _y, "[ " + scrStringManual("game_internal_name_hover", 0) + scrString("term_info_file_extension") + " ]");
+                    draw_text(_x, _y, "[ " + scrStringManual("game_internal_name_hover", 0) + scrStringManual("term_info_file_extension", 0) + " ]");
                 }
                 else if (global.currGameID == 35 && global.cheatID == 3)
                 {
-                    draw_text(_x, _y, "[ " + scrStringManual("game_internal_name_godsblood", 0) + scrString("term_info_file_extension") + " ]");
+                    draw_text(_x, _y, "[ " + scrStringManual("game_internal_name_godsblood", 0) + scrStringManual("term_info_file_extension", 0) + " ]");
                 }
                 else
                 {
-                    draw_text(_x, _y, "[ " + scrStringManual("game_internal_name_" + string(global.currGameID), 0) + scrString("term_info_file_extension") + " ]");
+                    draw_text(_x, _y, "[ " + scrStringManual("game_internal_name_" + string(global.currGameID), 0) + scrStringManual("term_info_file_extension", 0) + " ]");
                 }
             }
             else
             {
-                draw_text(_x, _y, "[ " + scrStringManual("game_internal_name_0", 0) + scrString("term_info_file_extension") + " ]");
+                draw_text(_x, _y, "[ " + scrStringManual("game_internal_name_0", 0) + scrStringManual("term_info_file_extension", 0) + " ]");
             }
         }
@@ -738,2 +791,6 @@
     }
 }
+scrSetFont(_origFont);
+draw_set_color(_origColor);
+draw_set_halign(_origHAlign);
+draw_set_valign(_origVAlign);
--- original/gml_Object_oPauseMenu_Other_10.gml
+++ patched/gml_Object_oPauseMenu_Other_10.gml
@@ -14,11 +14,11 @@
     }
     drawMenu = true;
-    scrMenuCreate(scrString("menu_head_root"), 0);
-    if (profileOpen)
+    scrMenuCreate(scrStringManual("menu_head_root", 0), 0);
+    if (profileOpen && menuType == 0)
     {
         if (!global.arcadeCabinetMode || global.arcadeCabinetConfig)
         {
-            OP_OPTIONS = scrMenuItem(TYPE_SINGLE, scrString("menu_item_options"));
-            OP_PROSET = scrMenuItem(TYPE_SINGLE, scrString("menu_item_profile_settings"));
+            OP_OPTIONS = scrMenuItem(TYPE_SINGLE, scrStringManual("menu_item_options", 0));
+            OP_PROSET = scrMenuItem(TYPE_SINGLE, scrStringManual("menu_item_profile_settings", 0));
         }
         else
@@ -29,11 +29,20 @@
             scrMenuSpacer(MENU_BIG_SPACER);
         }
-        OP_TERMINAL = scrMenuItem(TYPE_SINGLE, scrString("menu_item_ufo_terminal"));
+        OP_TERMINAL = scrMenuItem(TYPE_SINGLE, scrStringManual("menu_item_ufo_terminal", 0));
         scrMenuSpacer(MENU_BIG_SPACER);
-        OP_LIBRARY = scrMenuItem(TYPE_SINGLE, scrString("menu_item_back_to_library"));
+        var _hideSwitchProfile = pfo_is_online() && pfo_player_get_client_index(player) != 0;
+        if (!global.arcadeCabinetMode && _hideSwitchProfile)
+        {
+            OP_PROFILE = -1;
+            scrMenuSpacer(MENU_BIG_SPACER);
+        }
+        OP_LIBRARY = scrMenuItem(TYPE_SINGLE, scrStringManual("menu_item_back_to_library", 0));
         if (!global.arcadeCabinetMode)
         {
-            OP_PROFILE = scrMenuItem(TYPE_SINGLE, scrString("menu_item_switch_profile"));
+            if (!_hideSwitchProfile)
+            {
+                OP_PROFILE = scrMenuItem(TYPE_SINGLE, scrStringManual("menu_item_switch_profile", 0));
         }
+        }
         else
         {
@@ -41,5 +50,5 @@
             scrMenuSpacer(MENU_BIG_SPACER);
         }
-        OP_QUIT = scrMenuItem(TYPE_SINGLE_CONFIRM, scrString("menu_item_quit"), scrString("menu_item_quit_confirm"));
+        OP_QUIT = scrMenuItem(TYPE_SINGLE_CONFIRM, scrStringManual("menu_item_quit", 0), scrStringManual("menu_item_quit_confirm", 0));
         OP_LANGUAGE = -1;
         OP_MILESTONES = -1;
@@ -47,11 +56,19 @@
     else
     {
-        OP_OPTIONS = scrMenuItem(TYPE_SINGLE, scrString("menu_item_options"));
-        OP_MILESTONES = scrMenuItem(TYPE_SINGLE, scrString("menu_item_view_milestones"));
-        OP_LANGUAGE = scrMenuItem(TYPE_SINGLE, scrString("menu_item_change_language"));
+        OP_OPTIONS = scrMenuItem(TYPE_SINGLE, scrStringManual("menu_item_options", 0));
+        OP_MILESTONES = scrMenuItem(TYPE_SINGLE, scrStringManual("menu_item_view_milestones", 0));
+        if (menuType == 0)
+        {
+            OP_LANGUAGE = scrMenuItem(TYPE_SINGLE, scrStringManual("menu_item_change_language", 0));
+        }
+        else
+        {
+            OP_LANGUAGE = -1;
         scrMenuSpacer(MENU_BIG_SPACER);
+        }
         scrMenuSpacer(MENU_BIG_SPACER);
-        OP_LIBRARY = scrMenuItem(TYPE_SINGLE, scrString("menu_item_resume"));
-        OP_QUIT = scrMenuItem(TYPE_SINGLE_CONFIRM, scrString("menu_item_quit"), scrString("menu_item_quit_confirm"));
+        scrMenuSpacer(MENU_BIG_SPACER);
+        OP_LIBRARY = scrMenuItem(TYPE_SINGLE, scrStringManual("menu_item_resume", 0));
+        OP_QUIT = scrMenuItem(TYPE_SINGLE_CONFIRM, scrStringManual("menu_item_quit", 0), scrStringManual("menu_item_quit_confirm", 0));
         OP_PROSET = -1;
         OP_PROFILE = -1;
@@ -83,4 +100,14 @@
 else if (substate == SUB_NAV)
 {
+    if (pfo_is_online())
+    {
+        itemName[OP_QUIT] = "Disconnect";
+        itemValues[OP_QUIT][0] = "Really Disconnect?";
+    }
+    else
+    {
+        itemName[OP_QUIT] = scrStringManual("menu_item_quit", 0);
+        itemValues[OP_QUIT][0] = scrStringManual("menu_item_quit_confirm", 0);
+    }
     var choice = scrMenuNavigation();
     if (choice == -2)
@@ -135,5 +162,12 @@
                 break;
             case OP_QUIT:
+                if (pfo_is_online())
+                {
+                    scrOnlineCleanup(true);
+                }
+                else
+                {
                 game_end();
+                }
                 break;
         }
--- original/gml_Object_oPauseMenu_Other_11.gml
+++ patched/gml_Object_oPauseMenu_Other_11.gml
@@ -9,9 +9,9 @@
     }
     drawMenu = true;
-    scrMenuCreate(scrString("menu_head_root"), 0);
+    scrMenuCreate(scrStringManual("menu_head_root", 0), 0);
     if (!global.arcadeCabinetMode || global.arcadeCabinetConfig)
     {
-        OP_OPTIONS = scrMenuItem(TYPE_SINGLE, scrString("menu_item_options"));
-        OP_GAMEINFO = scrMenuItem(TYPE_SINGLE, scrString("menu_head_game_info"));
+        OP_OPTIONS = scrMenuItem(TYPE_SINGLE, scrStringManual("menu_item_options", 0));
+        OP_GAMEINFO = scrMenuItem(TYPE_SINGLE, scrStringManual("menu_head_game_info", 0));
         OP_PROSET = -1;
     }
@@ -24,9 +24,9 @@
         scrMenuSpacer(MENU_BIG_SPACER);
     }
-    OP_TERMINAL = scrMenuItem(TYPE_SINGLE, scrString("menu_item_ufo_terminal"));
+    OP_TERMINAL = scrMenuItem(TYPE_SINGLE, scrStringManual("menu_item_ufo_terminal", 0));
     scrMenuSpacer(MENU_BIG_SPACER);
-    OP_RESUME = scrMenuItem(TYPE_SINGLE, scrString("menu_item_resume_game"));
-    OP_RESET = scrMenuItem(TYPE_SINGLE_CONFIRM, scrString("menu_item_reset"), scrString("menu_item_reset_confirm"));
-    OP_LIBRARY = scrMenuItem(TYPE_SINGLE_CONFIRM, scrString("menu_item_back_to_library"), scrString("menu_item_back_to_library_confirm"));
+    OP_RESUME = scrMenuItem(TYPE_SINGLE, scrStringManual("menu_item_resume_game", 0));
+    OP_RESET = scrMenuItem(TYPE_SINGLE_CONFIRM, scrStringManual("menu_item_reset", 0), scrStringManual("menu_item_reset_confirm", 0));
+    OP_LIBRARY = scrMenuItem(TYPE_SINGLE_CONFIRM, scrStringManual("menu_item_back_to_library", 0), scrStringManual("menu_item_back_to_library_confirm", 0));
     menuSel = OP_RESUME;
     if (statePrev == STATE_TERMINAL)
--- original/gml_Object_oPauseMenu_Other_12.gml
+++ patched/gml_Object_oPauseMenu_Other_12.gml
@@ -21,39 +21,37 @@
 {
     drawMenu = true;
-    scrMenuCreate(scrString("menu_head_keyboard_config"), menuSel);
-    OP_PLAYER = scrMenuItem(TYPE_PLAYER, scrString("menu_item_player"), currPlayer);
-    OP_UP = scrMenuItem(TYPE_DUAL_KEY, scrString("menu_item_up"), 0, keyMap[currPlayer][global.INP_UP]);
-    OP_DOWN = scrMenuItem(TYPE_DUAL_KEY, scrString("menu_item_down"), 0, keyMap[currPlayer][global.INP_DOWN]);
-    OP_LEFT = scrMenuItem(TYPE_DUAL_KEY, scrString("menu_item_left"), 0, keyMap[currPlayer][global.INP_LEFT]);
-    OP_RIGHT = scrMenuItem(TYPE_DUAL_KEY, scrString("menu_item_right"), 0, keyMap[currPlayer][global.INP_RIGHT]);
-    OP_FIRE1 = scrMenuItem(TYPE_DUAL_KEY, scrStringVal("menu_item_fire1", "{1}"), 0, keyMap[currPlayer][global.INP_FIRE1]);
-    OP_FIRE2 = scrMenuItem(TYPE_DUAL_KEY, scrStringVal("menu_item_fire2", "{2}"), 0, keyMap[currPlayer][global.INP_FIRE2]);
-    OP_START = scrMenuItem(TYPE_DUAL_KEY, scrString("menu_item_start"), 0, keyMap[currPlayer][global.INP_START]);
+    scrMenuCreate(scrStringManual("menu_head_keyboard_config", 0), menuSel);
+    OP_PLAYER = scrMenuItem(TYPE_PLAYER, scrStringManual("menu_item_player", 0), currPlayer);
+    OP_UP = scrMenuItem(TYPE_DUAL_KEY, scrStringManual("menu_item_up", 0), 0, keyMap[currPlayer][global.INP_UP]);
+    OP_DOWN = scrMenuItem(TYPE_DUAL_KEY, scrStringManual("menu_item_down", 0), 0, keyMap[currPlayer][global.INP_DOWN]);
+    OP_LEFT = scrMenuItem(TYPE_DUAL_KEY, scrStringManual("menu_item_left", 0), 0, keyMap[currPlayer][global.INP_LEFT]);
+    OP_RIGHT = scrMenuItem(TYPE_DUAL_KEY, scrStringManual("menu_item_right", 0), 0, keyMap[currPlayer][global.INP_RIGHT]);
+    OP_FIRE1 = scrMenuItem(TYPE_DUAL_KEY, scrStringValManual("menu_item_fire1", 0, "{1}"), 0, keyMap[currPlayer][global.INP_FIRE1]);
+    OP_FIRE2 = scrMenuItem(TYPE_DUAL_KEY, scrStringValManual("menu_item_fire2", 0, "{2}"), 0, keyMap[currPlayer][global.INP_FIRE2]);
+    OP_START = scrMenuItem(TYPE_DUAL_KEY, scrStringManual("menu_item_start", 0), 0, keyMap[currPlayer][global.INP_START]);
     scrMenuSpacer(MENU_MEDIUM_SPACER);
     scrMenuSpacer(MENU_MEDIUM_SPACER);
-    OP_BACK = scrMenuItem(TYPE_SINGLE, scrString("menu_item_back_to_settings"));
+    OP_BACK = scrMenuItem(TYPE_SINGLE, scrStringManual("menu_item_back_to_settings", 0));
     scrSwitchSub(SUB_NAV);
 }
 else if (substate == SUB_NAV)
 {
+    if (!sendCommandInProgress() && !localInputDisabled())
+    {
     var choice = scrMenuNavigation();
     if (choice == -2)
     {
-        scrSfxLibrary(soundSubExit[currentSoundSet]);
-        scrSwitchState(statePrev);
-        exit;
+            sendCommand(UnknownEnum.Value_1);
     }
-    if (pressStart)
+        else if (pressStart)
     {
-        scrSwitchState(STATE_UNPAUSE);
-        exit;
+            sendCommand(UnknownEnum.Value_2);
     }
-    if (keyboard_check_pressed(vk_f1))
+        else if (keyboard_check_pressed(vk_f1))
     {
         scrSfxLibrary(soundSet[currentSoundSet]);
         scrSwitchSub(SUB_RESET);
-        exit;
     }
-    if (choice >= 0)
+        else if (choice >= 0)
     {
         switch (menuSel)
@@ -61,6 +59,9 @@
             case OP_PLAYER:
                 scrSfxLibrary(soundToggle[currentSoundSet]);
+                    if (!pfo_is_online())
+                    {
                 currPlayer = choice;
                 scrSwitchSub(SUB_INIT);
+                    }
                 break;
             case OP_UP:
@@ -76,8 +77,17 @@
                 break;
             case OP_BACK:
+                    sendCommand(UnknownEnum.Value_1);
+                    break;
+            }
+        }
+    }
+    if (receiveCommand(UnknownEnum.Value_1))
+    {
                 scrSfxLibrary(soundSubExit[currentSoundSet]);
                 scrSwitchState(statePrev);
-                break;
         }
+    else if (receiveCommand(UnknownEnum.Value_2))
+    {
+        scrSwitchState(STATE_UNPAUSE);
     }
 }
@@ -205,3 +215,9 @@
         }
     }
+}
+
+enum UnknownEnum
+{
+    Value_1 = 1,
+    Value_2
 }
--- original/gml_Object_oPauseMenu_Other_13.gml
+++ patched/gml_Object_oPauseMenu_Other_13.gml
@@ -37,21 +37,21 @@
     gamepadConnect[1] = gamepad_is_connected(global.joySlot[1]);
     drawMenu = true;
-    scrMenuCreate(scrString("menu_head_joypad_config"), menuSel);
-    OP_PLAYER = scrMenuItem(TYPE_PLAYER, scrString("menu_item_player"), currPlayer);
+    scrMenuCreate(scrStringManual("menu_head_joypad_config", 0), menuSel);
+    OP_PLAYER = scrMenuItem(TYPE_PLAYER, scrStringManual("menu_item_player", 0), currPlayer);
     if (gamepadConnect[currPlayer])
     {
-        OP_SLOT = scrMenuItem(TYPE_DUAL_DETECT, scrString("menu_item_joypad_pairing"), 0, scrStringVal("menu_op_pad_num", global.joySlot[currPlayer]));
-        OP_CONTROLLER = scrMenuItem(TYPE_DUAL, scrString("menu_item_controller_type"), global.joyType[currPlayer], global.JOY_TYPE_NAME);
-        OP_UP = scrMenuItem(TYPE_DUAL_JOY, scrString("menu_item_up"), 0, joyMap[currPlayer][global.INP_UP]);
-        OP_DOWN = scrMenuItem(TYPE_DUAL_JOY, scrString("menu_item_down"), 0, joyMap[currPlayer][global.INP_DOWN]);
-        OP_LEFT = scrMenuItem(TYPE_DUAL_JOY, scrString("menu_item_left"), 0, joyMap[currPlayer][global.INP_LEFT]);
-        OP_RIGHT = scrMenuItem(TYPE_DUAL_JOY, scrString("menu_item_right"), 0, joyMap[currPlayer][global.INP_RIGHT]);
-        OP_FIRE1 = scrMenuItem(TYPE_DUAL_JOY, scrStringVal("menu_item_fire1", "{1}"), 0, joyMap[currPlayer][global.INP_FIRE1]);
-        OP_FIRE2 = scrMenuItem(TYPE_DUAL_JOY, scrStringVal("menu_item_fire2", "{2}"), 0, joyMap[currPlayer][global.INP_FIRE2]);
-        OP_START = scrMenuItem(TYPE_DUAL_JOY, scrString("menu_item_start"), 0, joyMap[currPlayer][global.INP_START]);
+        OP_SLOT = scrMenuItem(TYPE_DUAL_DETECT, scrStringManual("menu_item_joypad_pairing", 0), 0, scrStringValManual("menu_op_pad_num", 0, global.joySlot[currPlayer]));
+        OP_CONTROLLER = scrMenuItem(TYPE_DUAL, scrStringManual("menu_item_controller_type", 0), global.joyType[currPlayer], global.JOY_TYPE_NAME);
+        OP_UP = scrMenuItem(TYPE_DUAL_JOY, scrStringManual("menu_item_up", 0), 0, joyMap[currPlayer][global.INP_UP]);
+        OP_DOWN = scrMenuItem(TYPE_DUAL_JOY, scrStringManual("menu_item_down", 0), 0, joyMap[currPlayer][global.INP_DOWN]);
+        OP_LEFT = scrMenuItem(TYPE_DUAL_JOY, scrStringManual("menu_item_left", 0), 0, joyMap[currPlayer][global.INP_LEFT]);
+        OP_RIGHT = scrMenuItem(TYPE_DUAL_JOY, scrStringManual("menu_item_right", 0), 0, joyMap[currPlayer][global.INP_RIGHT]);
+        OP_FIRE1 = scrMenuItem(TYPE_DUAL_JOY, scrStringValManual("menu_item_fire1", 0, "{1}"), 0, joyMap[currPlayer][global.INP_FIRE1]);
+        OP_FIRE2 = scrMenuItem(TYPE_DUAL_JOY, scrStringValManual("menu_item_fire2", 0, "{2}"), 0, joyMap[currPlayer][global.INP_FIRE2]);
+        OP_START = scrMenuItem(TYPE_DUAL_JOY, scrStringManual("menu_item_start", 0), 0, joyMap[currPlayer][global.INP_START]);
     }
     else
     {
-        OP_SLOT = scrMenuItem(TYPE_DUAL_DETECT, scrString("menu_item_joypad_pairing"), 0, "NONE");
+        OP_SLOT = scrMenuItem(TYPE_DUAL_DETECT, scrStringManual("menu_item_joypad_pairing", 0), 0, "NONE");
         scrMenuSpacer(MENU_MEDIUM_SPACER);
         scrMenuSpacer(MENU_MEDIUM_SPACER);
@@ -71,14 +71,15 @@
         OP_START = -1;
     }
-    OP_BACK = scrMenuItem(TYPE_SINGLE, scrString("menu_item_back_to_settings"));
+    OP_BACK = scrMenuItem(TYPE_SINGLE, scrStringManual("menu_item_back_to_settings", 0));
     scrSwitchSub(SUB_NAV);
 }
 else if (substate == SUB_NAV)
 {
+    if (!sendCommandInProgress() && !localInputDisabled())
+    {
     if (scrGetJoyCount() != padCount)
     {
         scrFixJoyDisconnect();
-        player = 2;
-        joySlot = global.joySlot[0];
+            player = -1;
         scrSwitchSub(SUB_INIT);
         exit;
@@ -87,21 +88,25 @@
     if (choice == -2)
     {
-        scrSfxLibrary(soundSubExit[currentSoundSet]);
-        scrSwitchState(statePrev);
-        exit;
+            sendCommand(UnknownEnum.Value_1);
     }
-    if (pressStart)
+        else if (pressStart)
     {
-        scrSwitchState(STATE_UNPAUSE);
-        exit;
+            sendCommand(UnknownEnum.Value_2);
     }
-    if (keyboard_check_pressed(vk_f1))
+        else if (keyboard_check_pressed(vk_f1))
     {
         scrSfxLibrary(soundSet[currentSoundSet]);
         scrSwitchSub(SUB_RESET);
-        exit;
     }
-    if (gamepad_button_check(joySlot, gp_shoulderl) && gamepad_button_check(joySlot, gp_shoulderr))
+        else
     {
+            var _joyPlayer = player;
+            if (_joyPlayer < 0 || pfo_is_online())
+            {
+                _joyPlayer = 0;
+            }
+            var _joySlot = global.joySlot[_joyPlayer];
+            if (gamepad_button_check(_joySlot, gp_shoulderl) && gamepad_button_check(_joySlot, gp_shoulderr))
+            {
         resetCounter++;
     }
@@ -115,7 +120,6 @@
         scrSfxLibrary(soundSet[currentSoundSet]);
         scrSwitchSub(SUB_RESET);
-        exit;
     }
-    if (choice >= 0)
+            else if (choice >= 0)
     {
         switch (menuSel)
@@ -123,10 +127,13 @@
             case OP_PLAYER:
                 scrSfxLibrary(soundToggle[currentSoundSet]);
+                        if (!pfo_is_online())
+                        {
                 currPlayer = choice;
                 scrSwitchSub(SUB_INIT);
+                        }
                 break;
             case OP_SLOT:
                 scrSfxLibrary(soundPrime[currentSoundSet]);
-                strInput = scrStringVal("menu_misc_joy_assign", currPlayer + 1);
+                        strInput = scrStringValManual("menu_misc_joy_assign", 0, currPlayer + 1);
                 scrSwitchSub(SUB_GET_SLOT);
                 break;
@@ -148,8 +155,18 @@
                 break;
             case OP_BACK:
+                        sendCommand(UnknownEnum.Value_1);
+                        break;
+                }
+            }
+        }
+    }
+    if (receiveCommand(UnknownEnum.Value_1))
+    {
                 scrSfxLibrary(soundSubExit[currentSoundSet]);
                 scrSwitchState(statePrev);
-                break;
         }
+    else if (receiveCommand(UnknownEnum.Value_2))
+    {
+        scrSwitchState(STATE_UNPAUSE);
     }
 }
@@ -364,9 +381,5 @@
                 global.joySlot[!currPlayer] = _oldSlot;
             }
-            if (player != 2)
-            {
-                player = 2;
-                joySlot = global.joySlot[0];
-            }
+            player = -1;
             stateCounter = 2;
         }
@@ -376,3 +389,9 @@
         scrSwitchSub(SUB_INIT);
     }
+}
+
+enum UnknownEnum
+{
+    Value_1 = 1,
+    Value_2
 }
--- original/gml_Object_oPauseMenu_Other_14.gml
+++ patched/gml_Object_oPauseMenu_Other_14.gml
@@ -8,5 +8,5 @@
     prevFullScreen = window_get_fullscreen();
     drawMenu = true;
-    scrMenuCreate(scrString("menu_head_video_settings"), 0);
+    scrMenuCreate(scrStringManual("menu_head_video_settings", 0), 0);
     delayLenShort = 8;
     delayLenLong = 22;
@@ -22,7 +22,7 @@
         _dispIndex = 0;
     }
-    OP_DISPLAY = scrMenuItem(TYPE_DUAL, scrString("menu_item_display"), _dispIndex, scrString("menu_op_windowed"), scrString("menu_op_fullscreen"), scrString("menu_op_borderless"));
-    OP_SCALE = scrMenuItem(TYPE_DUAL_INT, scrString("menu_item_scale"), global.scale - 1, 1, (global.scaleFill <= global.scaleMax) ? global.scaleMax : (global.scaleMax + 1), scrString("menu_op_scale_fill"));
-    OP_CRT = scrMenuItem(TYPE_DUAL, scrString("menu_item_crt"), global.scanlines, scrString("menu_op_off"), scrString("menu_op_crt_half"), scrString("menu_op_crt_full"));
+    OP_DISPLAY = scrMenuItem(TYPE_DUAL, scrStringManual("menu_item_display", 0), _dispIndex, scrStringManual("menu_op_windowed", 0), scrStringManual("menu_op_fullscreen", 0), scrStringManual("menu_op_borderless", 0));
+    OP_SCALE = scrMenuItem(TYPE_DUAL_INT, scrStringManual("menu_item_scale", 0), global.scale - 1, 1, (global.scaleFill <= global.scaleMax) ? global.scaleMax : (global.scaleMax + 1), scrStringManual("menu_op_scale_fill", 0));
+    OP_CRT = scrMenuItem(TYPE_DUAL, scrStringManual("menu_item_crt", 0), global.scanlines, scrStringManual("menu_op_off", 0), scrStringManual("menu_op_crt_half", 0), scrStringManual("menu_op_crt_full", 0));
     scrMenuSpacer(MENU_MEDIUM_SPACER);
     scrMenuSpacer(MENU_MEDIUM_SPACER);
@@ -32,5 +32,5 @@
     scrMenuSpacer(MENU_MEDIUM_SPACER);
     scrMenuSpacer(MENU_MEDIUM_SPACER);
-    OP_BACK = scrMenuItem(TYPE_SINGLE, scrString("menu_item_back_to_settings"));
+    OP_BACK = scrMenuItem(TYPE_SINGLE, scrStringManual("menu_item_back_to_settings", 0));
     menuSel = OP_DISPLAY;
     scrSwitchSub(SUB_NAV);
@@ -38,28 +38,25 @@
 else if (substate == SUB_NAV)
 {
+    if (!sendCommandInProgress() && !localInputDisabled())
+    {
     var choice = scrMenuNavigation();
     if (prevFullScreen != window_get_fullscreen())
     {
         scrSwitchSub(SUB_INIT);
-        exit;
     }
-    if (choice == -2)
+        else if (choice == -2)
     {
-        scrSfxLibrary(soundSubExit[currentSoundSet]);
-        scrSwitchState(statePrev);
-        exit;
+            sendCommand(UnknownEnum.Value_1);
     }
-    if (pressStart)
+        else if (pressStart)
     {
-        scrSwitchState(STATE_UNPAUSE);
-        exit;
+            sendCommand(UnknownEnum.Value_2);
     }
-    if (keyboard_check_pressed(vk_f1))
+        else if (keyboard_check_pressed(vk_f1))
     {
         scrSfxLibrary(soundSet[currentSoundSet]);
         scrSwitchSub(SUB_RESET);
-        exit;
     }
-    if (choice >= 0)
+        else if (choice >= 0)
     {
         switch (menuSel)
@@ -124,8 +121,17 @@
                 break;
             case OP_BACK:
+                    sendCommand(UnknownEnum.Value_1);
+                    break;
+            }
+        }
+    }
+    if (receiveCommand(UnknownEnum.Value_1))
+    {
                 scrSfxLibrary(soundSubExit[currentSoundSet]);
                 scrSwitchState(statePrev);
-                break;
         }
+    else if (receiveCommand(UnknownEnum.Value_2))
+    {
+        scrSwitchState(STATE_UNPAUSE);
     }
 }
@@ -174,3 +180,9 @@
         }
     }
+}
+
+enum UnknownEnum
+{
+    Value_1 = 1,
+    Value_2
 }
--- original/gml_Object_oPauseMenu_Other_15.gml
+++ patched/gml_Object_oPauseMenu_Other_15.gml
@@ -6,8 +6,8 @@
 {
     drawMenu = true;
-    scrMenuCreate(scrString("menu_head_audio_settings"), 0);
-    OP_SOUND = scrMenuItem(TYPE_DUAL, scrString("menu_item_sound"), global.AUDIO_SFX_TOGGLE, scrString("menu_op_off"), scrString("menu_op_on"));
-    OP_MUSIC = scrMenuItem(TYPE_DUAL, scrString("menu_item_music"), global.AUDIO_BGM_TOGGLE, scrString("menu_op_off"), scrString("menu_op_on"));
-    OP_VOLUME = scrMenuItem(TYPE_METER, scrString("menu_item_volume"), global.AUDIO_VOLUME * 10, 0, 10);
+    scrMenuCreate(scrStringManual("menu_head_audio_settings", 0), 0);
+    OP_SOUND = scrMenuItem(TYPE_DUAL, scrStringManual("menu_item_sound", 0), global.AUDIO_SFX_TOGGLE, scrStringManual("menu_op_off", 0), scrStringManual("menu_op_on", 0));
+    OP_MUSIC = scrMenuItem(TYPE_DUAL, scrStringManual("menu_item_music", 0), global.AUDIO_BGM_TOGGLE, scrStringManual("menu_op_off", 0), scrStringManual("menu_op_on", 0));
+    OP_VOLUME = scrMenuItem(TYPE_METER, scrStringManual("menu_item_volume", 0), global.AUDIO_VOLUME * 10, 0, 10);
     scrMenuSpacer(MENU_MEDIUM_SPACER);
     scrMenuSpacer(MENU_MEDIUM_SPACER);
@@ -17,28 +17,26 @@
     scrMenuSpacer(MENU_MEDIUM_SPACER);
     scrMenuSpacer(MENU_MEDIUM_SPACER);
-    OP_BACK = scrMenuItem(TYPE_SINGLE, scrString("menu_item_back_to_settings"));
+    OP_BACK = scrMenuItem(TYPE_SINGLE, scrStringManual("menu_item_back_to_settings", 0));
     scrSwitchSub(SUB_NAV);
 }
 else if (substate == SUB_NAV)
 {
+    if (!sendCommandInProgress() && !localInputDisabled())
+    {
     var choice = scrMenuNavigation();
     if (choice == -2)
     {
-        scrSfxLibrary(soundSubExit[currentSoundSet]);
-        scrSwitchState(statePrev);
-        exit;
+            sendCommand(UnknownEnum.Value_1);
     }
-    if (pressStart)
+        else if (pressStart)
     {
-        scrSwitchState(STATE_UNPAUSE);
-        exit;
+            sendCommand(UnknownEnum.Value_2);
     }
-    if (keyboard_check_pressed(vk_f1))
+        else if (keyboard_check_pressed(vk_f1))
     {
         scrSfxLibrary(soundSet[currentSoundSet]);
         scrSwitchSub(SUB_RESET);
-        exit;
     }
-    if (choice >= 0)
+        else if (choice >= 0)
     {
         switch (menuSel)
@@ -76,8 +74,17 @@
                 break;
             case OP_BACK:
+                    sendCommand(UnknownEnum.Value_1);
+                    break;
+            }
+        }
+    }
+    if (receiveCommand(UnknownEnum.Value_1))
+    {
                 scrSfxLibrary(soundSubExit[currentSoundSet]);
                 scrSwitchState(statePrev);
-                break;
         }
+    else if (receiveCommand(UnknownEnum.Value_2))
+    {
+        scrSwitchState(STATE_UNPAUSE);
     }
 }
@@ -100,3 +107,9 @@
         }
     }
+}
+
+enum UnknownEnum
+{
+    Value_1 = 1,
+    Value_2
 }
--- original/gml_Object_oPauseMenu_Other_17.gml
+++ patched/gml_Object_oPauseMenu_Other_17.gml
@@ -2,4 +2,11 @@
 if (stateCounter == 2)
 {
+    if (menuType == 0)
+    {
     scrUnpause();
+    }
+    else
+    {
+        instance_destroy();
+    }
 }
--- original/gml_Object_oPauseMenu_Other_19.gml
+++ patched/gml_Object_oPauseMenu_Other_19.gml
@@ -108,5 +108,5 @@
     else if (stateCounter == 1)
     {
-        var _okay = scrGetOkay("menu_misc_defaults_restored");
+        var _okay = scrGetOkay("menu_misc_defaults_restored", UnknownEnum.Value_0);
         if (_okay == true)
         {
@@ -143,3 +143,8 @@
     }
     scrSwitchSub(SUB_INIT);
+}
+
+enum UnknownEnum
+{
+    Value_0
 }
--- original/gml_Object_oPauseMenu_Other_20.gml
+++ patched/gml_Object_oPauseMenu_Other_20.gml
@@ -20,5 +20,5 @@
 else if (substate == SUB_INIT_MENU)
 {
-    scrMenuCreate(scrString("menu_head_milestones"), 0);
+    scrMenuCreate(scrStringManual("menu_head_milestones", 0), 0);
     scrMenuSpacer(MENU_MEDIUM_SPACER);
     scrMenuSpacer(MENU_MEDIUM_SPACER);
@@ -38,6 +38,6 @@
         }
     }
-    OP_PAGE = scrMenuItem(TYPE_DUAL_CENTER, scrString("menu_item_page"), 0, pageArray);
-    OP_BACK = scrMenuItem(TYPE_SINGLE, scrString("menu_item_back_to_root"));
+    OP_PAGE = scrMenuItem(TYPE_DUAL_CENTER, scrStringManual("menu_item_page", 0), 0, pageArray);
+    OP_BACK = scrMenuItem(TYPE_SINGLE, scrStringManual("menu_item_back_to_root", 0));
     menuSel = OP_PAGE;
     scrSwitchSub(SUB_NAV);
--- original/gml_Object_oPauseMenu_Other_22.gml
+++ patched/gml_Object_oPauseMenu_Other_22.gml
@@ -4,14 +4,28 @@
 {
     drawMenu = true;
-    scrMenuCreate(scrString("menu_head_options"), 0);
-    OP_KEYCONFIG = scrMenuItem(TYPE_SINGLE, scrString("menu_item_keyboard_config"));
-    OP_JOYCONFIG = scrMenuItem(TYPE_SINGLE, scrString("menu_item_joypad_config"));
-    OP_VIDEO = scrMenuItem(TYPE_SINGLE, scrString("menu_item_video_settings"));
-    OP_AUDIO = scrMenuItem(TYPE_SINGLE, scrString("menu_item_audio_settings"));
+    if (statePrev != STATE_OPTIONS)
+    {
+        menuSel = 0;
+    }
+    scrMenuCreate(scrStringManual("menu_head_options", 0), menuSel);
+    if (menuType == 0 && pfo_is_online())
+    {
+        OP_ONLINE = scrMenuItem(TYPE_SINGLE, "ONLINE SETTINGS");
+    }
+    else
+    {
+        OP_ONLINE = -1;
+    }
+    OP_KEYCONFIG = scrMenuItem(TYPE_SINGLE, scrStringManual("menu_item_keyboard_config", 0));
+    OP_JOYCONFIG = scrMenuItem(TYPE_SINGLE, scrStringManual("menu_item_joypad_config", 0));
+    OP_VIDEO = scrMenuItem(TYPE_SINGLE, scrStringManual("menu_item_video_settings", 0));
+    OP_AUDIO = scrMenuItem(TYPE_SINGLE, scrStringManual("menu_item_audio_settings", 0));
     scrMenuSpacer(MENU_BIG_SPACER);
+    if (OP_ONLINE == -1)
+    {
     scrMenuSpacer(MENU_BIG_SPACER);
-    OP_BACK = scrMenuItem(TYPE_SINGLE, scrString("menu_item_back_to_root"));
+    }
+    OP_BACK = scrMenuItem(TYPE_SINGLE, scrStringManual("menu_item_back_to_root", 0));
     OP_BUTTON_LAYOUT = -1;
-    menuSel = 0;
     if (statePrev == STATE_KEYCONFIG)
     {
@@ -38,9 +52,18 @@
 else if (substate == SUB_NAV)
 {
+    if (OP_ONLINE != -1 && !pfo_is_online())
+    {
+        if (menuSel > OP_ONLINE && menuSel <= OP_AUDIO)
+        {
+            menuSel--;
+        }
+        statePrev = STATE_OPTIONS;
+        scrSwitchSub(SUB_INIT);
+    }
     var choice = scrMenuNavigation();
     if (choice == -2)
     {
         scrSfxLibrary(soundSubExit[currentSoundSet]);
-        if (scrInGame())
+        if (scrInGame() && menuType == 0)
         {
             scrSwitchState(STATE_ROOT_GAME);
@@ -61,4 +84,8 @@
         switch (menuSel)
         {
+            case OP_ONLINE:
+                scrSwitchState(STATE_ONLINE);
+                scrSfxLibrary(soundSubEnter[currentSoundSet]);
+                break;
             case OP_KEYCONFIG:
                 scrSwitchState(STATE_KEYCONFIG);
@@ -83,5 +110,5 @@
             case OP_BACK:
                 scrSfxLibrary(soundSubExit[currentSoundSet]);
-                if (scrInGame())
+                if (scrInGame() && menuType == 0)
                 {
                     scrSwitchState(STATE_ROOT_GAME);
--- original/gml_Object_oPauseMenu_Step_1.gml
+++ patched/gml_Object_oPauseMenu_Step_1.gml
@@ -1,19 +1,98 @@
-if (state != STATE_JOYCONFIG && (inputListenerTimer++ % 30) == 0 && scrGetJoyCount() != padCount)
+if (!localInputDisabled() && state != STATE_JOYCONFIG && (inputListenerTimer++ % 30) == 0 && scrGetJoyCount() != padCount)
 {
     scrFixJoyDisconnect();
-    player = 2;
-    joySlot = global.joySlot[0];
+    player = -1;
 }
-if (player <= 1)
+if (usingLocalInput())
 {
-    scrGetInput(player);
+    if (player != -1)
+    {
+        scrGetInput(player, UnknownEnum.Value_3);
 }
 else
 {
-    scrGetInputMerged();
+        scrGetInputMerged(UnknownEnum.Value_3);
 }
-if (global.crashToTerminal)
+}
+else if (player != -1)
 {
+    scrGetInput(player, UnknownEnum.Value_2);
+}
+else
+{
+    scrGetInput(pfo_client_get_player_index(), UnknownEnum.Value_2);
+}
+if (menuType == 0 && global.crashToTerminal)
+{
     state = STATE_TERMINAL;
 }
+if (pfo_is_online())
+{
+    if (!localInputDisabled() && !pfo_send_command_in_progress())
+    {
+        var _playerMaskToRemove = UnknownEnum.Value_0;
+        for (var i = 0; i < global.MAX_PLAYERS_SUPPORTED; i++)
+        {
+            if (global.onlinePlayers[i] >= 0 && !pfo_client_is_connected(global.onlinePlayers[i]))
+            {
+                _playerMaskToRemove |= (UnknownEnum.Value_1 << int64(i));
+            }
+        }
+        if (_playerMaskToRemove != UnknownEnum.Value_0)
+        {
+            sendCommand(UnknownEnum.Value_5, _playerMaskToRemove);
+        }
+    }
+    var _arg = [UnknownEnum.Value_0];
+    if (receiveCommand(UnknownEnum.Value_4, _arg))
+    {
+        var _playerIndex = real(_arg[0] & UnknownEnum.Value_15);
+        var _value = real(_arg[0] >> UnknownEnum.Value_4);
+        if (_value == UnknownEnum.Value_15)
+        {
+            _value = -1;
+        }
+        if (_value >= 0)
+        {
+            var _previousValue = global.onlinePlayers[_playerIndex];
+            for (var i = 0; i < global.MAX_PLAYERS_SUPPORTED; i++)
+            {
+                if (global.onlinePlayers[i] == _value)
+                {
+                    global.onlinePlayers[i] = _previousValue;
+                }
+            }
+        }
+        global.onlinePlayers[_playerIndex] = _value;
+    }
+    else if (receiveCommand(UnknownEnum.Value_5, _arg))
+    {
+        var _playerMaskToRemove = _arg[0];
+        var i = global.MAX_PLAYERS_SUPPORTED - 1;
+        while (i >= 0)
+        {
+            if ((_playerMaskToRemove & (UnknownEnum.Value_1 << int64(i))) != UnknownEnum.Value_0)
+            {
+                array_delete(global.onlinePlayers, i, 1);
+                array_push(global.onlinePlayers, -1);
+            }
+            i--;
+        }
+        if (global.onlinePlayers[0] < 0)
+        {
+            global.onlinePlayers[0] = pfo_player_get_client_index(player);
+        }
+    }
+}
 scrRunStateMachine();
+
+enum UnknownEnum
+{
+    Value_0,
+    Value_1,
+    Value_2,
+    Value_3,
+    Value_4,
+    Value_5,
+    Value_15 = 15
+}
--- original/gml_Object_oTitleScreens_Create_0.gml
+++ patched/gml_Object_oTitleScreens_Create_0.gml
@@ -1,3 +1,3 @@
-game_set_speed(global.STANDARD_FPS, gamespeed_fps);
+pfo_game_set_speed(global.STANDARD_FPS, 0);
 global.playIntro = false;
 global.attractMode = false;
@@ -79,8 +79,10 @@
     if (global.currGameID != 40 && !audio_group_is_loaded(global.AUDIOGROUP_BGM[global.currGameID]))
     {
+        global.onlineRunUpdate = false;
         return false;
     }
     if (texturegroup_get_status(global.TEXTUREGROUP[global.currGameID]) != 3)
     {
+        global.onlineRunUpdate = false;
         return false;
     }
--- original/gml_Object_oTitleScreens_Step_1.gml
+++ patched/gml_Object_oTitleScreens_Step_1.gml
@@ -10,12 +10,5 @@
 if (state != STATE_BLACKSCREEN && state != STATE_START_GAME)
 {
-    if (bPressStart[0] || global.steamOverlayActivated)
-    {
-        scrPause(0);
-    }
-    else if (bPressStart[1])
-    {
-        scrPause(1);
-    }
+    scrCheckPause();
 }
 if (global.currFile >= 1 && global.currFile <= global.NUM_PROFILES_ACCESSIBLE)
